node_modules/
dist/
out/
# Per-surface static exports (scripts/build-surface.mjs) — desktop|web|mobile.
out-desktop/
out-web/
out-mobile/
# Transient route-group staging used while building a single surface.
frontend/.surface-staging/
.surface-staging/
.next/
publish/
desktop/src-tauri/binaries/
desktop/src-tauri/target/
bin/
obj/
**/bin/
**/obj/
.DS_Store
*.log
.vite

tsconfig.tsbuildinfo

.claude-sessions/*

.cursor/
.agents/
.opencode/
.claude/
.codex/

# Commander task scratchpad; keep local task notes out of git by default.
.task/

# Local CodeGraph index — large local DB, never commit.
.codegraph/

# Secrets & env files — NEVER commit (this is a PHI app). `.env.example` is the
# only env file that may be tracked.
.env
.env.*
!.env.example
.secrets.env
*.secret
*.key
*.pem
*.pfx
*.p12
*.sec
# Private signing material (also matched by *.sec above; kept explicit).
mcp-connectors/_signing/release.sec

# Generated gap-analysis / audit scratch artifacts.
.gap_*

# Test output logs
tsc_output.txt

# Dev databases
*.dev.db
*.dev.db-shm
*.dev.db-wal
