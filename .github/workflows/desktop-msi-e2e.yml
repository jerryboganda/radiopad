name: desktop-msi-e2e

# The MSI-install + renderer-driven E2E, extracted as a REUSABLE workflow so it runs two ways:
#
#   1. Called by desktop-bundle after every bundle (workflow_call, same-run artifact) — where it
#      gates `release` on tag builds.
#   2. Dispatched standalone against an ALREADY-BUILT bundle artifact (workflow_dispatch with the
#      producing run's id) — a ~10-minute iteration loop for debugging the E2E itself without
#      paying a full Tauri build each attempt.
#
# What it does: installs the actual .msi, pre-places the pinned on-device models where the
# sidecar resolves them (%LOCALAPPDATA%\com.radiopad.desktop), then scripts/desktop-msi-e2e.mjs
# drives the INSTALLED renderer over the WebView2 devtools protocol: login (incl. mandatory TOTP
# enrollment computed from the secret the UI shows) → report → dictation draft panel → on-device
# MedGemma format → apply → microphone dictation (Chromium's fake audio device plays the MedASR
# bundle's radiology sample into the real HQ mic; every /api/stt/transcribe response must come
# from MedASR).
#
# Strictness: on tag refs the script's headless-webview tolerance is disabled (a renderer that
# never came up FAILS). github.ref inside a called workflow is the CALLER's ref, so tag builds
# are strict and manual dispatches are tolerant, automatically.

on:
  workflow_call:
    inputs:
      run_id:
        description: 'Run id to download the radiopad-windows-latest artifact from (empty = this run)'
        required: false
        type: string
      advisory:
        description: 'Report a drive failure as a warning instead of failing the job'
        required: false
        default: true
        type: boolean
      mode:
        description: '"shipped" = install the released MSI (install phase only); "e2e-build" = build a throwaway CDP-enabled MSI and drive the renderer'
        required: false
        default: shipped
        type: string
  workflow_dispatch:
    inputs:
      run_id:
        description: 'desktop-bundle run id with the radiopad-windows-latest artifact (mode=shipped only)'
        required: false
        type: string
      advisory:
        description: 'Report a drive failure as a warning instead of failing the job'
        required: false
        default: false
        type: boolean
      mode:
        description: 'shipped | e2e-build'
        required: false
        default: e2e-build
        type: string
  schedule:
    # Nightly full renderer + microphone run. Too heavy for a release (it builds its own
    # bundle and loads a 2.5 GB model), but cheap enough once a day to catch UI regressions.
    - cron: '0 3 * * *'

jobs:
  msi-e2e:
    name: install MSI + drive the renderer
    runs-on: windows-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read # cross-run artifact download
    env:
      MEDGEMMA_SHA: b31becdf4f39561800505514cce67681604fe449d04dd35c8c92fd7848c6d7bd
      LLAMA_TAG: b10068
      LLAMA_WIN_SHA: 01d5f30876acfb4a0be59396710f450213495c7181d8fbcce2fad045835ceb89
      MEDASR_SHA: 2c20f03265ee6144c566fd18b0f7bbb4f0d005d11ce9440dd641920210f4c33a
      # schedule/dispatch default to the full renderer run; desktop-bundle passes mode=shipped.
      E2E_MODE: ${{ inputs.mode || 'e2e-build' }}
      # Absolute, deterministic path — this string is BAKED into the e2e build's
      # --use-file-for-fake-audio-capture flag, so the runtime copy must match it exactly.
      MIC_WAV: C:\radiopad-e2e\mic.wav
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22' # global WebSocket + fetch — the CDP driver has zero npm deps

      - name: Download installer artifact (shipped mode)
        if: env.E2E_MODE == 'shipped'
        uses: actions/download-artifact@v4
        with:
          name: radiopad-windows-latest
          path: artifacts
          run-id: ${{ inputs.run_id || github.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      # --- e2e-build mode: a throwaway MSI that can be driven ------------------------------
      #
      # Neither WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS nor the WebView2 registry policy reaches
      # this app (both verified inert — Tauri sets additionalBrowserArguments itself and
      # WebView2 does not append to it). The ONLY way to open a CDP port is Tauri's own
      # additionalBrowserArgs, so it is patched in here, at build time, for a bundle that is
      # installed, driven, and discarded within this job. The shipped installer produced by
      # desktop-bundle never contains a debug port — deliberately, because a loopback CDP port
      # exposes the rendered DOM (PHI) to any local process.
      - name: Setup pnpm
        if: env.E2E_MODE == 'e2e-build'
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - uses: dtolnay/rust-toolchain@stable
        if: env.E2E_MODE == 'e2e-build'

      - name: Cache cargo
        if: env.E2E_MODE == 'e2e-build'
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: desktop/src-tauri

      - uses: actions/setup-dotnet@v4
        if: env.E2E_MODE == 'e2e-build'
        with:
          dotnet-version: '8.0.x'

      - name: Publish .NET sidecar
        if: env.E2E_MODE == 'e2e-build'
        shell: bash
        run: |
          set -euo pipefail
          dotnet publish backend/RadioPad.Api/src/RadioPad.Api/RadioPad.Api.csproj \
            -c Release -r win-x64 --self-contained true \
            -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true \
            -o publish/sidecar
          mkdir -p desktop/src-tauri/binaries
          cp publish/sidecar/RadioPad.Api.exe desktop/src-tauri/binaries/radiopad-api-x86_64-pc-windows-msvc.exe

      - name: Build frontend (desktop surface)
        if: env.E2E_MODE == 'e2e-build'
        working-directory: frontend
        run: |
          pnpm install --frozen-lockfile
          pnpm build:desktop

      - name: Bake the CDP + fake-media browser args into this build only
        if: env.E2E_MODE == 'e2e-build'
        shell: bash
        run: |
          set -euo pipefail
          node -e '
            const fs = require("fs");
            const p = "desktop/src-tauri/tauri.conf.json";
            const c = JSON.parse(fs.readFileSync(p, "utf8"));
            const w = c.app.windows[0];
            if (w.additionalBrowserArgs) throw new Error("additionalBrowserArgs is already set in the shipped config — refusing to clobber it; merge by hand");
            // additionalBrowserArgs REPLACES wry defaults, so restate them verbatim.
            w.additionalBrowserArgs = [
              "--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection",
              "--autoplay-policy=no-user-gesture-required",
              "--remote-debugging-port=9223",
              "--use-fake-device-for-media-stream",
              "--use-fake-ui-for-media-stream",
              "--use-file-for-fake-audio-capture=C:\\\\radiopad-e2e\\\\mic.wav%noloop",
            ].join(" ");
            fs.writeFileSync(p, JSON.stringify(c, null, 2) + "\n");
            console.log("patched additionalBrowserArgs:", w.additionalBrowserArgs);
          '
          # Loud, because a human reading this log must never mistake it for the shipped build.
          echo "::warning::THROWAWAY e2e build — CDP port 9223 enabled. This MSI is never released."

      - name: Install tauri-cli
        if: env.E2E_MODE == 'e2e-build'
        uses: taiki-e/install-action@v2
        with:
          tool: tauri-cli

      - name: Build the throwaway MSI
        if: env.E2E_MODE == 'e2e-build'
        working-directory: desktop
        run: cargo tauri build --config '{"bundle":{"createUpdaterArtifacts":false}}'

      - name: Collect the built MSI
        if: env.E2E_MODE == 'e2e-build'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force artifacts | Out-Null
          $msi = Get-ChildItem desktop/src-tauri/target/release/bundle -Recurse -Filter '*.msi' | Select-Object -First 1
          if (-not $msi) { throw 'the e2e build produced no MSI' }
          Copy-Item $msi.FullName artifacts/
          Write-Host "e2e MSI: $($msi.Name)"

      # Model/runtime downloads are cached and the MedGemma/MedASR keys deliberately match the
      # ubuntu smoke workflows' keys — the blobs are platform-independent, so a weekly
      # offline-formatter-smoke run keeps this job's 2.5 GB restore warm.
      - name: Cache MedGemma GGUF
        id: medgemma
        uses: actions/cache@v4
        with:
          path: medgemma
          key: medgemma-1.5-4b-q4-${{ env.MEDGEMMA_SHA }}

      - name: Download + verify MedGemma
        if: steps.medgemma.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force medgemma | Out-Null
          curl.exe -fL --retry 5 --retry-all-errors -C - `
            "https://huggingface.co/unsloth/medgemma-1.5-4b-it-GGUF/resolve/main/medgemma-1.5-4b-it-Q4_K_M.gguf" `
            -o medgemma/medgemma-1.5-4b-it-Q4_K_M.gguf
          if ($LASTEXITCODE -ne 0) { throw "MedGemma download failed" }
          $h = (Get-FileHash -Algorithm SHA256 medgemma/medgemma-1.5-4b-it-Q4_K_M.gguf).Hash
          if ($h -ine $env:MEDGEMMA_SHA) { throw "MedGemma SHA mismatch: $h" }

      - name: Cache MedASR bundle
        id: medasr
        uses: actions/cache@v4
        with:
          path: medasr-model
          key: stt-medasr-ctc-en-int8-2c20f032

      - name: Download + verify MedASR
        if: steps.medasr.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force medasr-model/test_wavs | Out-Null
          $base = "https://huggingface.co/csukuangfj/sherpa-onnx-medasr-ctc-en-int8-2025-12-25/resolve/main"
          curl.exe -fL "$base/model.int8.onnx" -o medasr-model/model.int8.onnx
          if ($LASTEXITCODE -ne 0) { throw "MedASR model download failed" }
          curl.exe -fL "$base/tokens.txt" -o medasr-model/tokens.txt
          if ($LASTEXITCODE -ne 0) { throw "MedASR tokens download failed" }
          # The sample WAV is NOT best-effort here: the E2E's microphone phase plays it as
          # the fake capture device, so a missing file must fail now, not mid-drive.
          curl.exe -fL "$base/test_wavs/0.wav" -o medasr-model/test_wavs/0.wav
          if ($LASTEXITCODE -ne 0) { throw "MedASR sample wav download failed" }
          $h = (Get-FileHash -Algorithm SHA256 medasr-model/model.int8.onnx).Hash
          if ($h -ine $env:MEDASR_SHA) { throw "MedASR SHA mismatch: $h" }

      - name: Cache llama-server runtime (windows)
        id: llama
        uses: actions/cache@v4
        with:
          path: llama-runtime-win
          key: llama-server-${{ env.LLAMA_TAG }}-windows-x64

      - name: Download + verify llama-server
        if: steps.llama.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          # Pin matches LocalRuntimes.LlamaServerArchive() for Windows. The WHOLE zip is kept:
          # llama-server.exe is a 9 KB stub over llama-server-impl.dll + ggml-cpu-*.dll backends.
          curl.exe -fL --retry 5 --retry-all-errors `
            "https://github.com/ggml-org/llama.cpp/releases/download/$env:LLAMA_TAG/llama-$env:LLAMA_TAG-bin-win-cpu-x64.zip" `
            -o llama.zip
          if ($LASTEXITCODE -ne 0) { throw "llama-server download failed" }
          $h = (Get-FileHash -Algorithm SHA256 llama.zip).Hash
          if ($h -ine $env:LLAMA_WIN_SHA) { throw "llama-server SHA mismatch: $h" }
          Expand-Archive llama.zip -DestinationPath llama-runtime-win
          Remove-Item llama.zip

      - name: Place models + runtime where the sidecar resolves them
        shell: pwsh
        run: |
          # Mirrors LocalSttModels.ResolveModelDir / LocalRuntimes.ResolveRuntimeDir exactly —
          # the script later asserts the sidecar reports these as downloaded, so a layout drift
          # fails loudly rather than silently exercising the 502 path.
          $root = Join-Path $env:LOCALAPPDATA 'com.radiopad.desktop'
          New-Item -ItemType Directory -Force "$root/models/medgemma-1.5-4b-q4" | Out-Null
          Copy-Item medgemma/medgemma-1.5-4b-it-Q4_K_M.gguf "$root/models/medgemma-1.5-4b-q4/"
          New-Item -ItemType Directory -Force "$root/models/medasr-ctc-en-int8/test_wavs" | Out-Null
          Copy-Item medasr-model/model.int8.onnx "$root/models/medasr-ctc-en-int8/"
          Copy-Item medasr-model/tokens.txt "$root/models/medasr-ctc-en-int8/"
          if (Test-Path medasr-model/test_wavs/0.wav) {
            Copy-Item medasr-model/test_wavs/0.wav "$root/models/medasr-ctc-en-int8/test_wavs/"
          }
          New-Item -ItemType Directory -Force "$root/runtimes/llama-server-b10068" | Out-Null
          Copy-Item -Recurse -Force llama-runtime-win/* "$root/runtimes/llama-server-b10068/"

          # The fake microphone reads this exact absolute path — it is compiled into the e2e
          # build's browser args, so it cannot be derived from the workspace location.
          New-Item -ItemType Directory -Force 'C:\radiopad-e2e' | Out-Null
          Copy-Item medasr-model/test_wavs/0.wav $env:MIC_WAV
          if (-not (Test-Path $env:MIC_WAV)) { throw "mic sample not staged at $env:MIC_WAV" }

      - name: Install the MSI
        shell: pwsh
        run: |
          $msi = Get-ChildItem artifacts -Recurse -Filter '*.msi' | Select-Object -First 1
          if (-not $msi) { throw 'No MSI in the bundle artifact' }
          Write-Host "Installing $($msi.FullName)"
          $p = Start-Process msiexec -ArgumentList '/i', "`"$($msi.FullName)`"", '/qn', '/norestart', '/l*v', "$env:RUNNER_TEMP\msi-install.log" -Wait -PassThru
          if ($p.ExitCode -ne 0 -and $p.ExitCode -ne 3010) {
            Get-Content "$env:RUNNER_TEMP\msi-install.log" -Tail 60
            throw "msiexec /i exited $($p.ExitCode)"
          }
          echo "RADIOPAD_E2E_MSI=$($msi.FullName)" >> $env:GITHUB_ENV

      - name: Drive the installed app end to end
        id: drive
        # ADVISORY while the harness itself is unproven (see the note in the header): this
        # workflow's CDP attach has never yet succeeded on a runner, and a harness that cannot
        # attach a debugger is not evidence that the PRODUCT is broken. Failing the release on
        # it would strand users on an older build for a test-scaffolding bug. Standalone
        # dispatches default advisory=false so debugging runs still fail hard.
        continue-on-error: ${{ inputs.advisory }}
        shell: pwsh
        env:
          RADIOPAD_E2E_OUT: ${{ runner.temp }}/e2e-out
          # On tag builds this job gates the release, so the script's headless-webview
          # tolerance is disabled — a renderer that never came up must FAIL, not warn.
          RADIOPAD_E2E_STRICT: ${{ startsWith(github.ref, 'refs/tags/') && '1' || '0' }}
          # 'shipped' MSIs have no CDP port by design, so they verify the packaged product
          # only; the throwaway e2e build additionally drives the renderer + microphone.
          RADIOPAD_E2E_PHASES: ${{ env.E2E_MODE == 'e2e-build' && 'full' || 'install' }}
          # The MedASR bundle's own radiology dictation sample, played by Chromium's fake
          # audio device so the REAL mic button records real speech.
          RADIOPAD_E2E_MIC_WAV: ${{ env.MIC_WAV }}
        run: node scripts/desktop-msi-e2e.mjs

      - name: Report an advisory failure loudly
        # A tolerated failure must never read as cover this run did not provide.
        if: always() && steps.drive.outcome == 'failure'
        shell: bash
        run: |
          echo "::warning::MSI E2E FAILED but is advisory — the installed product's UI was NOT verified in this run"
          {
            echo "## ⚠️ MSI E2E failed (advisory)"
            echo ""
            echo "The installed product's renderer was **not** verified in this run."
            echo "Release publication was not blocked, by deliberate configuration."
            echo "See the \`msi-e2e-evidence\` artifact (screenshots, app/sidecar logs,"
            echo "\`webview-diagnostics.txt\` with the WebView2 process command lines)."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Uninstall the MSI
        if: always()
        shell: pwsh
        run: |
          if ($env:RADIOPAD_E2E_MSI) {
            $p = Start-Process msiexec -ArgumentList '/x', "`"$env:RADIOPAD_E2E_MSI`"", '/qn', '/norestart' -Wait -PassThru
            if ($p.ExitCode -ne 0 -and $p.ExitCode -ne 3010) { throw "msiexec /x exited $($p.ExitCode)" }
          }

      - name: Upload E2E evidence (screenshots + logs)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: msi-e2e-evidence
          path: |
            ${{ runner.temp }}/e2e-out/**/*
            ${{ runner.temp }}/msi-install.log
          if-no-files-found: warn
