{
  "name": "radiopad-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "RadioPad mobile shell (Capacitor 6)",
  "scripts": {
    "build:web": "pnpm --filter @radiopad/frontend build:mobile",
    "sync": "pnpm build:web && cap sync && node scripts/inject-android-permissions.mjs",
    "android": "pnpm sync && cap open android",
    "ios": "pnpm sync && cap open ios"
  },
  "dependencies": {
    "@aparajita/capacitor-biometric-auth": "^7.0.0",
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor-mlkit/barcode-scanning": "^6.0.0",
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/network": "^6.0.3",
    "@capacitor/preferences": "^6.0.3",
    "@capacitor/push-notifications": "^6.0.4",
    "@capacitor/splash-screen": "^6.0.2",
    "capacitor-secure-storage-plugin": "^0.10.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
