# PRD v2 — Gap-Closure Traceability Matrix

**Status:** Living  ·  **Owner:** Engineering  ·  **Seeded:** 2026-06-19 (Iteration 51, plan §0.8)

This matrix tracks the **Enterprise PRD v2** (`RadioPad_Enterprise_PRD_v2 (1).md`) gap-closure programme (plan: `~/.claude/plans/d-projects-radiopad-com-radiopad-enterp-logical-ladybug.md`). It is the v2 superset and is **complementary to** the v1 [traceability-matrix.md](traceability-matrix.md) (which closed at 130/130 for the original PRD). Statuses below are the **baseline audit** (19-agent read-only sweep) captured before gap-closure began; the **Completed in gap-closure** section is updated each iteration as items ship.

## Status legend

| Symbol | Meaning |
| --- | --- |
| ✅ | Implemented (verified against code at audit time) |
| 🟡 | Partial — function exists but incomplete vs the PRD clause |
| 🔴 | Missing — no entity/controller/page/test |
| ⏸ | N/A — stack-locked or post-GA |

## Baseline scorecard

| Status | Count |
| --- | --- |
| ✅ Implemented | 112 |
| 🟡 Partial | 258 |
| 🔴 Missing | 261 |
| ⏸ N/A | 4 |
| **Total** | **635** |

## Completed in gap-closure (updated each iteration)

| Iter | Plan ref | Delivered | Evidence |
| --- | --- | --- | --- |
| 0a (Iter 50) | §0.1 | Structured report data model: `StructuredFieldsJson`, `RadsAssessment`, `ReportMeasurement` (RPT-003, RADS/lesion foundation) | `StructuredReportModelTests` (3) |
| 0b (Iter 51) | §0.2–0.5 | Span anchors on `ValidationFinding`; rulebook id/version + temperature in every AI audit+usage row; temperature ceiling (≤0.4); structured-output `response_format` wiring (AI-012/014/015/021/028, RB-009/005) | `Iter0bAiProvenanceTests` (10) |
| 0c (Iter 52) | §0.6–0.8 | 5 RBAC roles (Resident/Fellow/Subspecialist/Researcher/Auditor, AUTH-002); TenantSettings policy scaffold (AUTH-004/009, §17.3, CR-002); this matrix | `Iter0cRbacPolicyTests` |

## Baseline inventory by area

> Generated from the 19-agent audit (`.gap_clean.json`). Effort: S=days · M=1–2 wks · L=2–4 wks · XL=multi-month.

### AI Provider Abstraction + MCP/Tools (PRD v2 §14.9, §14.10)

_RadioPad implements the AI Provider Abstraction (§14.9) and MCP/Tool Integration Layer (§14.10) substantially on the locked stack (ASP.NET Core 8 + EF Core backend, Next.js frontend, .NET CLI). The provider-abstraction core is strong: a per-tenant ProviderConfig registry, IAiProviderAdapter interface with ~13 wired adapters, a single AiGateway choke-point that hard-fails PHI-policy violations before inference, encrypted env:/KMS API-key vaulting, AES-256-GCM OAuth refresh-token vault with rotation, per-provider cost/latency/token/availability telemetry (IAiUsageStore), a composite cost/quality/latency router, sandbox model comparison, retention labels, and a background model-drift watch. The MCP layer is also well-built: a tenant tool registry with admin Submit→Approve→Block lifecycle, least-privilege scopes, default-deny of shell:/fs:/net: scopes, per-call append-only audit with input/output hashes, allowlist connectors, Ed25519 signed-manifest verification with a deny-by-default capability registry, and a sandboxed test runner. Gaps that block 100%: PROV-004 (no explicit equal-or-higher compliance fallback chain — routing just filters eligibility), PROV-012 (no provider conformance test suite), PROV-013 (only a MONTHLY plan quota exists; the per-tenant per-DAY USD cost ceiling with hard cutoff + 80% warning is absent), PROV-014/015 (latency-budget and cheapest-compliant are weighted factors, not the specified hard-budget/mode switches), PROV-016 (no structured BAA/DPA/ZDR/training-on-data compliance matrix — only a single compliance class + free-text retention label), PROV-018 (per-modality default override not implemented; modality param is accepted but unused), PROV-019 (BYO-OAuth-app not surfaced), PROV-020 (no confidential-computing attestation). On MCP: MCP-006 sandbox is in-process only, NOT WASM/WASI (explicitly deferred in code), MCP-008 (no tool conformance suite), MCP-009 (no tool usage quotas), MCP-011 (built-in safe tools — PubMed/ACR/ICD-CPT/Fleischner/RECIST — are manifest-only declarations with no implemented connector binding classes), MCP-012 (no explicit prompt-injection-resistant prompts/output validation layer for tools)._

Coverage: ✅16 · 🟡8 · 🔴8 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| ✅ | PROV-001 Tenant-level model/provider registry | P0 | CE | S | None material. Fully tenant-scoped registry with list/save/health endpoints. |
| ✅ | PROV-002 Mark providers PHI-approved / de-identified-only / blocked | P0 | CE | S | None. All three classes (plus Sandbox/LocalOnly) present and editable. |
| ✅ | PROV-003 Per-provider cost / latency / token / availability telemetry | P1 | CE | S | None material; cost/latency/token/availability all captured per provider. |
| 🟡 | PROV-004 Fallback routing only between providers of equal-or-higher compliance class | P0 | CE | M | There is no explicit multi-provider fallback CHAIN that, on a primary failure, re-routes only to an equal-or-higher compliance-class provider. AiGateway invokes a single selected adapter and throws… |
| ✅ | PROV-005 Model comparison in sandbox evaluation mode | P1 | EE | S | None material. Side-by-side compare with per-run latency/tokens shipped. |
| ✅ | PROV-006 API key vaulting and rotation | P0 | CE | S | Key rotation for static API keys is operator-driven via secret store; no automated rotation scheduler for API keys (OAuth refresh tokens do have automated rotation). Minor. |
| ✅ | PROV-007 OAuth token storage only in encrypted local/tenant vaults | P0 | CE | S | None material; only operator-side real-IdP wiring (IOAuthTokenIssuer is Noop by default) remains, which is out of scope of the locked-stack code. |
| ✅ | PROV-008 Provider policy enforcement before inference (hard fail on violation) | P0 | CE | S | None. Hard fail-closed before inference is the single choke-point. |
| 🟡 | PROV-009 Data retention labeling per provider and endpoint | P0 | CE | S | Label is a single free-text string per provider, not a structured per-endpoint retention record, and it is informational only (never enforced). PRD says 'per provider AND endpoint'; per-endpoint gr… |
| ✅ | PROV-010 Block PHI to providers without approved compliance configuration | P0 | CE | S | None. Core safety boundary, fully enforced and tested. |
| 🟡 | PROV-011 First-class provider adapters (OpenAI, Azure, Anthropic, Vertex, Bedrock, Cohere, Mistral, Groq, Together, Fireworks, Replicate + local OSS llama.cpp/Ollama/vLLM/LocalAI/TGI/Tabby) | P1 | CE | M | Dedicated first-class adapters for Cohere, Mistral La Plateforme, Groq, Together, Fireworks, Replicate are NOT individually implemented (only reachable generically via openai-compatible). Local OSS… |
| 🔴 | PROV-012 Provider conformance test suite | P1 | CE | L | Build a runnable, provider-agnostic conformance test suite (golden prompt/response contract checks) any new adapter can be run against to certify RadioPad compatibility. |
| 🔴 | PROV-013 Cost ceiling per tenant per day with hard cutoff; warning at 80% | P0 | CE | L | Implement a per-tenant per-DAY USD spend ceiling that hard-cuts at 100% and warns at 80%. Current quota is monthly call/token-based, not a daily dollar ceiling, and has no warning threshold. |
| 🟡 | PROV-014 Latency budget routing (pick fastest compliant provider under N ms) | P1 | CE | M | There is no hard latency-BUDGET filter (a configurable N-ms ceiling that excludes slower providers and then picks the fastest compliant one). Latency only influences a soft weighted score, not a bu… |
| 🟡 | PROV-015 'Cheapest-compliant' routing mode for non-clinical drafts | P2 | CE | M | No explicit selectable 'cheapest-compliant' MODE (a switch that, for non-clinical drafts, ignores quality/latency and deterministically picks the lowest-cost compliant provider). Achievable only by… |
| 🟡 | PROV-016 Provider compliance matrix view for admins (BAA, DPA, ZDR endpoints, training-on-data) | P0 | CE | M | No structured compliance-matrix data model or view with discrete BAA-status, DPA-status, ZDR-endpoint, and training-on-data columns. These are collapsed into one ProviderComplianceClass enum plus a… |
| ✅ | PROV-017 Provider drift watch — daily probe with golden cases | P1 | EE | M | Interval defaults to 6h not strictly 'daily' (configurable, so acceptable). Tests sandbox-class providers only to avoid cost; function meets intent. |
| 🔴 | PROV-018 Per-modality default provider override | P1 | CE | M | Implement a per-modality default/override mapping (e.g. mammo→on-prem Llama only) consulted by the router. The plumbing accepts modality but it has zero effect on routing today. |
| 🟡 | PROV-019 Bring-your-own-OAuth-app for subscription-backed providers (non-PHI by default) | P2 | CE | L | BYO-OAuth-app is not realized as a general 'bring-your-own-OAuth-app' for arbitrary subscription-backed AI providers. No generic provider-level BYO-OAuth-app r… |
| 🔴 | PROV-020 Confidential-computing inference attestation surfaced to admins | P2 | EE | XL | Implement capture and admin display of confidential-computing (TEE/enclave) attestation evidence per provider/inference. Entirely absent. |
| ✅ | MCP-001 Tool registry for approved internal tools (MCP-style) | P1 | CE | S | None material; full registry lifecycle present. |
| ✅ | MCP-002 Each tool requires explicit admin approval per tenant | P0 | CE | S | None. Default-deny until per-tenant admin approval. |
| ✅ | MCP-003 Least-privilege tool scopes (read-only, write to specific endpoints, etc.) | P0 | CE | S | None material; scope + capability + connector-path least-privilege all present. |
| ✅ | MCP-004 Every tool call logged with user, study/patient context, tool, input hash, output hash, timestamp | P0 | CE | S | None. Exhaustive append-only per-call ledger with hashes. |
| ✅ | MCP-005 Shell/file/network tools blocked by default in clinical production | P0 | CE | S | None. Compile-time fixed dangerous set, double-gated override. |
| 🟡 | MCP-006 Sandboxed execution for tool tests (WASM with WASI capability allowlist) | P1 | CE | XL | Execution is IN-PROCESS, NOT WASM/WASI with a WASI capability allowlist — explicitly documented as deferred ('no hard OS-level isolation here — that's the next iteration's job, likely WASM via Wasm… |
| ✅ | MCP-007 Allowlist-based connectors for PACS/RIS/EHR (no wildcard egress) | P1 | CE | S | Allowlist mechanism is solid (no wildcard egress). Note connector RUNTIME bindings are not implemented (see MCP-011), but the allowlist gating itself for PACS/RIS/EHR connectors is in place. |
| 🔴 | MCP-008 Tool conformance test suite (third parties can certify) | P1 | CE | L | Provide a published tool conformance suite (manifest schema + scope + sandbox + I/O contract checks) third parties run to certify a tool. |
| 🔴 | MCP-009 Tool usage quotas (per user, per tenant) | P1 | CE | M | Implement per-user and per-tenant tool-invocation quotas with enforcement at McpInvocationService/Invoke, mirroring the AI plan-quota gate. |
| ✅ | MCP-010 Signed tool capability registration; daemon refuses unsigned tools in production tenants | P1 | EE | M | Signature verification and trusted-publisher model are implemented; production-tenant 'refuse unsigned' is enforced for desktop plugins via capability registry/verifier. Server-side McpToolRegistry… |
| 🔴 | MCP-011 Built-in safe tools: PubMed, ACR Appropriateness, ICD/CPT, Fleischner/Lung-RADS, RECIST helper | P1 | CE | L | Implement the named built-in safe MCP tools as runnable connector binding classes (PubMed search, ACR Appropriateness lookup, ICD/CPT lookup, Fleischner/Lung-RADS calculator, RECIST helper). Today … |
| 🔴 | MCP-012 Prompt-injection-resistant tool prompts and output validation | P0 | CE | L | Add prompt-injection-resistant tool prompt templates and structured output validation/sanitization for tool results before they re-enter the model/report context. Currently absent for the MCP tool … |

### AI Safety & Quality / Validation Engine (PRD v2 §16)

_RadioPad implements a solid deterministic core of §16.1's validation matrix plus the human-in-the-loop primitives that protect signing, but a large fraction of the PRD's checks and safety-process controls are missing or only partially built. The deterministic engine (backend/RadioPad.Api/src/RadioPad.Validation/Engine/ReportValidator.cs) genuinely covers laterality, measurement, required-section, negation (NegEx-style), modality-mismatch, critical-finding language, and follow-up allow-list checks, all driven by YAML rulebooks (RulebookSpec) and exercised by golden-case validation packs (ValidationPackService) — these map well to the locked .NET stack regardless of the PRD's NER/Go/Rust wording. The "unsupported-impression / hallucination" check exists but is a deterministic token-overlap heuristic (HallucinationDetector.cs), NOT an AI-assisted, source-span-attested check as §16.1/§16.5 require. Roughly a third of the §16.1 matrix has NO implementation at all: anatomy-mismatch, sex/age-appropriate language, allergy/contrast safety, dose (DLP/CTDIvol) language, spelling/grammar (Hunspell/LanguageTool), acronym expansion, prior-report inconsistency (semantic diff), quantitative RECIST comparison, and standalone ambiguity detection. §16.2 human-in-the-loop is partial: AI text is visibly marked (.ai-mark) and acknowledged, export is gated behind explicit acknowledgement and blockers, and the system never auto-signs (RIS copy via CopyToRisButton) — but there is NO critical-results routing workflow, NO keystroke-aggregation analytics, NO no-silent-retry operator-visibility guarantee, NO accessibility opt-out for the AI mark, and NO always-accessible mandatory "Why this suggestion?" panel. §16.3 confidence presentation is partial (pass/warn/block + unsupported-statement warnings + quality score) but lacks source-span highlights, a provenance/context-trace graph, a visible model+rulebook version pin in the panel, and a "Needs radiologist review" banner. §16.4 safety-RFC has tooling scaffolding (validation packs with approve/run/audit) but no enforced RFC/CSWG/≥99%-golden-gate/30-day-pre-announce process. §16.5 adversarial robustness is largely absent: no prompt-injection stripping in the dictation/daemon path, no output watermarking, no source-span attestation, only a weak refusal fallback, and no toxic-content-filter toggle. Evidence verified against actual source; PROGRESS.md claims around §16 (e.g. Prompt Studio for §16.4) were cross-checked and do not substitute for the missing checks._

Coverage: ✅9 · 🟡12 · 🔴17 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| ✅ | §16.1 Laterality consistency (Deterministic, Blocker, cross-section + impression) | – | – | S | Uses regex left/right tokens rather than true NER; spine-level laterality and multi-side organs not modelled. Functionally meets the clause on the locked stack. |
| ✅ | §16.1 Measurement consistency (Deterministic regex + unit normalization, Warning) | – | – | S | Only Findings↔Impression; does not reconcile against structured fields across all sections (PRD says 'all sections + structured fields'). Biaxial covered by a separate rule. |
| ✅ | §16.1 Required-section check (Deterministic schema, Blocker, per template/rulebook) | – | – | S | None material for the clause. |
| 🟡 | §16.1 Unsupported-impression (AI-assisted + source-span, Blocker — impression traces to findings) | – | – | L | It is a deterministic token-overlap heuristic, not AI-assisted with character-level source-span tracing as the PRD demands; severity defaults to tenant-config not Blocker; no span linkage from impr… |
| 🟡 | §16.1 Critical-finding language (Deterministic lexicon + AI, Blocker, mandatory tenant phrasebook) | – | – | M | Lexicon is hard-coded in the validator, not a tenant-configurable mandatory phrasebook; no AI-assisted layer. PRD wants tenant phrasebook + AI. |
| ✅ | §16.1 Follow-up recommendation (Deterministic allow-list, Warning, tenant-approved phrases only) | – | – | S | Allow-list is per-rulebook rather than a tenant-wide approved set, and empty list disables the rule (back-compat). Acceptable but slightly narrower than 'tenant-approved'. |
| ✅ | §16.1 Negation conflict (Deterministic NegEx/ConText, Blocker — 'No PE' vs 'small PE') | – | – | M | Hand-rolled negation windows rather than a full NegEx/ConText pipeline; scope/family detection limited. Meets the example case. |
| ✅ | §16.1 Modality mismatch (Deterministic template + study meta, Warning — CT language in MR report) | – | – | S | Small fixed pair list (CT/MR/US/XR); does not consult template metadata, only study modality. |
| 🔴 | §16.1 Anatomy mismatch (Deterministic anatomy NER vs body_part, Warning — wrong body part wording) | – | – | L | Implement an anatomy-NER (or lexicon) check that flags narrative anatomy inconsistent with the study body part; add rule id + rulebook wiring. |
| 🟡 | §16.1 Hallucination risk (AI-assisted + provenance, Warning — claims not in input context) | – | – | L | No provenance linkage to the input context and no AI-assisted analysis; effectively a duplicate of the deterministic unsupported-claim heuristic. |
| 🔴 | §16.1 Ambiguity detection (AI-assisted, Info — 'could represent' without follow-up) | – | – | M | Add an (AI-assisted or deterministic) check that flags hedged language lacking an associated follow-up recommendation; emit Info finding. |
| 🔴 | §16.1 Sex/age-appropriate language (Deterministic, Warning — pediatric language in adult report) | – | – | L | Requires patient sex/age in study context plus a lexicon check; neither the data nor the rule exists. |
| 🔴 | §16.1 Allergy / contrast safety (Deterministic, Warning — contrast mentioned with allergy flag) | – | – | L | Needs an allergy flag in study/patient context and a rule that fires when contrast is described against that flag; data + rule missing. |
| 🔴 | §16.1 Dose language CT/IR (Deterministic, Info — DLP / CTDIvol mention check) | – | – | M | Add a deterministic rule that checks for DLP/CTDIvol presence on CT/IR studies; Info severity. Straightforward regex once wired. |
| 🔴 | §16.1 Spelling / grammar (Hunspell + LanguageTool, Info — medical dictionaries layered) | – | – | XL | Integrate a spell/grammar library (or equivalent .NET package) with layered medical dictionaries and surface Info findings; entirely absent. |
| 🔴 | §16.1 Acronym expansion (Tenant lexicon, Info — expand on first use) | – | – | M | Add a lexicon-driven acronym rule that flags unexpanded acronyms on first use; reuse TenantLexicon plumbing. |
| 🟡 | §16.1 Prior-report inconsistency (AI-assisted + semantic diff, Warning — unexpected interval change without note) | – | – | L | This is a literal text diff for the UI, not an AI-assisted semantic diff that flags unexpected interval change lacking an explanatory note; no validation finding is generated. Needs an AI-assisted … |
| 🔴 | §16.1 Quantitative comparison (Deterministic RECIST math, Info — lesion change classification) | – | – | XL | Implement RECIST math (target-lesion sums, %change, PR/SD/PD classification) emitting an Info finding; non-trivial clinical logic. |
| 🟡 | §16.2.1 AI text visibly marked until reviewed (color + screen-reader annotation + accessibility opt-out) | – | – | M | No explicit screen-reader-friendly annotation (e.g. aria-label/visually-hidden 'AI generated') on marked regions and no per-user accessibility opt-out toggle for the color mark; color-only signal i… |
| ✅ | §16.2.2 Final export requires explicit user confirmation (cannot be bypassed by automation) | – | – | S | Confirmation is a single client-side acknowledge step; server also advances status on validate, but export gating is enforced. Minor: a server-side export precondition assertion would harden agains… |
| 🔴 | §16.2.3 Critical-finding suggestions require explicit confirmation + critical-results workflow routing | – | – | XL | Build a critical-results acknowledgement/routing workflow (notify, document, track) and require explicit confirmation when a critical finding is suggested; currently only a text-validation nudge. |
| ✅ | §16.2.4 Report signing happens in customer RIS/EHR unless RadioPad approved as signing endpoint | – | – | M | The 'approved-as-signing-endpoint per tenant' toggle is implicit (signing is always available in-app); a tenant policy flag distinguishing RIS-only vs in-system signing would fully match the clause. |
| 🔴 | §16.2.5 User edits tracked at keystroke-aggregation level for analytics/rulebook eval (raw text stays in tenant) | – | – | L | Add edit-aggregation telemetry (e.g. AI-acceptance %, edit distance) emitted without raw text leaving tenant; entirely absent. |
| 🔴 | §16.2.6 No silent retries on PHI paths — operator visibility mandatory for every retry | – | – | M | The clause is effectively unmet as a stated control: there is no retry-with-visibility mechanism nor an explicit no-retry assertion documented/enforced on PHI paths. Add audited, operator-visible r… |
| 🔴 | §16.2.7 'Why this suggestion?' panel mandatory and always accessible | – | – | L | Build an always-accessible per-suggestion explanation panel (model, prompt/rulebook version, supporting findings/spans, rationale) on the report editor; currently absent. |
| ✅ | §16.3 Confidence presentation — rule status (pass/warn/block) | – | – | S | None for this sub-item. |
| 🟡 | §16.3 Confidence presentation — unsupported-statement warnings with source-span highlights | – | – | L | It quotes the sentence but does not highlight the supporting/contradicting SOURCE spans in Findings; no character-span linkage exists (ValidationFinding only carries Section + Snippet). |
| 🟡 | §16.3 Confidence presentation — missing-input warnings | – | – | M | No explicit 'missing input' category surfaced to the panel distinct from required-section blockers (e.g. missing prior, missing technique metadata) per the clause. |
| 🟡 | §16.3 Confidence presentation — contradiction warnings | – | – | L | Contradiction coverage is limited to a few hand-coded patterns; no general AI-assisted contradiction detector across findings/impression/prior. |
| 🔴 | §16.3 Confidence presentation — source/context trace (provenance graph) | – | – | XL | Build a provenance/context-trace model linking output claims to source spans and render it; absent. |
| 🟡 | §16.3 Confidence presentation — model/rulebook version pin | – | – | M | The Validation panel does NOT display the pinned model + rulebook version next to the result; the pin exists in audit/usage data but is not surfaced as the clause requires. |
| 🟡 | §16.3 Confidence presentation — 'Needs radiologist review' banner | – | – | S | This is an AI-highlight notice, not the specific persistent 'Needs radiologist review' status banner tied to validation/confidence state; wording and trigger differ from the clause. |
| 🟡 | §16.4 Safety RFC process — RFC doc, golden-case regression gate (>=99% blockers / >=95% warnings on >=3 rulebooks), CSWG review, 30-day pre-announce | – | – | L | No enforced RFC artifact requirement, no threshold gate (>=99%/>=95% across >=3 reference rulebooks) blocking a change, no Clinical Safety Working Group sign-off step, and no 30-day tenant pre-anno… |
| 🔴 | §16.5 Adversarial robustness — prompt-injection stripping in dictation before model invocation | – | – | L | Add a pre-model sanitizer that detects/strips suspected injected directives in dictation and a red-team test corpus; absent on the active stack. |
| 🔴 | §16.5 Adversarial robustness — output watermarking (EE: cryptographic provenance + optional perturbation) | – | – | XL | Implement EE output watermarking (cryptographic provenance signature, optional perturbation); not present. |
| 🔴 | §16.5 Adversarial robustness — source-span attestation (model outputs include char spans; UI verifies spans exist) | – | – | XL | Add span-attested output contract (model returns char spans into input), and UI/server verification that spans exist; entirely missing. |
| 🟡 | §16.5 Adversarial robustness — refusal handling (fall back to deterministic template; never silently emit empty content) | – | – | M | No explicit refusal detector with a deterministic template fallback for impression/draft generation; an empty/refused model response in RunAsync is not specifically detected and replaced with a det… |
| 🔴 | §16.5 Adversarial robustness — toxic-content filter off by default, configurable for patient-facing modes | – | – | M | Add a configurable toxic-content filter (default off, enableable for patient-facing modes); absent. |

### Auth, Tenancy & Access Control (PRD v2 §14.1, AUTH-001 … AUTH-015)

_RadioPad has a strong, well-tested authentication core on the locked .NET 8 / Next.js 16 stack, but the §14.1 access-control surface is roughly half-complete. SOLID/implemented: SAML 2.0 SP with signed-assertion validation (SamlController), OIDC Authorization Code + PKCE (AuthController + OidcBearerMiddleware), passwordless magic-link (MagicLinkController) with chained per-email/per-IP rate limiting, TOTP MFA (MfaController, RFC 6238) and WebAuthn/FIDO2 passkeys (WebAuthnController, hand-rolled attestation parser), RFC 8628 device flow (DeviceAuthController), full SCIM 2.0 Users+Groups+role projection (ScimController), account lockout + global session revocation via SessionEpoch (LockoutPolicy + UsersController), and per-tenant + global IP allowlist (IpAllowlistMiddleware). Stateless HMAC bearer tokens with a 12h lifetime (RadioPadBearerTokens), surfaced through AuthSession inventory rows. The frontend has /login, /admin/sso, and /admin/security pages backing these. GAPS: email/PASSWORD sign-in is not wired (PasswordHash field exists but no verify endpoint — only dev-header signin + passwordless flows); RBAC defines only 6 of the 11 PRD roles (missing Resident, Fellow, Subspecialist, Researcher, Auditor/read-only); tenant isolation is row-level TenantId only (no per-tenant DB schema / storage prefix / cache namespace / queue subject); MFA enforcement is a global OIDC env flag rather than a per-tenant policy; and several enterprise controls are entirely absent in code (step-up auth AUTH-008, per-role/per-tenant session limits & idle timeouts AUTH-009, break-glass AUTH-010, service-to-service short-lived JWT + mTLS AUTH-011, tenant-facing API-key lifecycle AUTH-012, geo-restrictions AUTH-014, support impersonation AUTH-015), device trust AUTH-007 captures only an optional fingerprint with no signed cert / posture check. Several gaps are documented in docs/ and ADR-0004 but not built. Effort is judged on the LOCKED stack (function, not the PRD's named OSS library)._

Coverage: ✅2 · 🟡6 · 🔴7 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | AUTH-001 — Email/password, magic link, OIDC, SAML 2.0 | P0 | CE/EE | M | No email/PASSWORD sign-in endpoint: AuthController.SignIn is dev-header-only ('there is no password'); grep for PasswordHash/VerifyHashedPassword across Controllers returns no verification path. Pa… |
| 🟡 | AUTH-002 — RBAC roles (Radiologist, Resident, Fellow, Subspecialist, Admin, Medical Director, Compliance Reviewer, IT Admin, Billing Admin, Researcher, Auditor read-only) | P0 | CE | M | Only 6 of 11 PRD roles exist: Radiologist, ReportingAdmin, MedicalDirector, ComplianceReviewer, ItAdmin, BillingAdmin. MISSING: Resident, Fellow, Subspecialist, Researcher, and Auditor (read-only).… |
| 🟡 | AUTH-003 — Tenant isolation at app, DB schema, storage prefix, cache namespace, audit log, queue subject | P0 | CE | L | Isolation is single-DB ROW-LEVEL (TenantId column) only. No per-tenant DB schema, no storage prefix, no cache namespace, no queue subject partitioning (grep for StoragePrefix/CacheNamespace/QueueSu… |
| 🟡 | AUTH-004 — MFA enforcement by tenant policy; WebAuthn/FIDO2 preferred, TOTP fallback | P0 | CE | M | MFA enforcement is NOT a per-tenant policy: it is a single global env flag RADIOPAD_OIDC_REQUIRE_MFA applied to the OIDC path only; native magic-link/WebAuthn/device flows are not gated by a stored… |
| ✅ | AUTH-005 — SCIM 2.0 provisioning / deprovisioning | P1 | EE | S | Functionally complete for Okta/Azure AD provisioning. Minor: filter support is limited to userName/displayName eq; bulk operations advertised as unsupported. None block the requirement. |
| ✅ | AUTH-006 — Emergency account lockout and global session revocation in ≤30 s | P0 | CE | S | Revocation is effectively immediate (next request fails validation) since tokens are stateless HMAC keyed by epoch — there is no propagation lag to measure. The explicit ≤30s SLA is met by construc… |
| 🟡 | AUTH-007 — Device trust policies for Studio and daemon (signed device cert + posture check) | P1 | EE | L | No signed device CERTIFICATE issuance/validation and no POSTURE check (OS/patch/disk-encryption attestation). Device identity is a self-asserted fingerprint string only, not a trust policy. To clos… |
| 🔴 | AUTH-008 — Step-up auth for high-risk actions (rulebook promotion, billing changes, mass export) | P0 | CE | L | No step-up (fresh re-auth / recent-MFA / WebAuthn challenge) before sensitive operations. Requires: a 'recent auth' / acr-style claim or short-lived elevation token, a [RequireStepUp] gate on ruleb… |
| 🔴 | AUTH-009 — Session limits per role and idle timeouts configurable per tenant | P0 | CE | L | No configurable idle timeout, no per-role session-limit (max concurrent sessions), no per-tenant session policy. grep for IdleTimeout/SessionLimit/SessionTimeout = no matches. Requires TenantSettin… |
| 🔴 | AUTH-010 — Break-glass access with mandatory justification, dual approval, and audit alert | P1 | EE | XL | Entirely unimplemented in code: no break-glass request entity, no mandatory justification capture, no dual-approval workflow, no SecurityAlert on activation/expiry. Requires a full request/approve/… |
| 🔴 | AUTH-011 — Service-to-service auth via short-lived JWTs (≤15 min) with mTLS at the gateway | P0 | CE | L | No internal service-to-service identity: no short-lived (≤15min) service JWT issuer/audience model and no mTLS / client-certificate enforcement at the gateway/Kestrel. Requires a service-principal … |
| 🔴 | AUTH-012 — API key lifecycle: scoped, rotatable, revocable, hashed at rest | P0 | CE | L | No general tenant-facing API KEY product with scopes, rotation, revocation, and creation/listing UI+API. Existing secrets are single-purpose, manually configured, and not lifecycle-managed. Require… |
| 🟡 | AUTH-013 — IP allowlist per tenant and per role | P1 | EE | M | PER-ROLE granularity is absent — allowlist is tenant-wide (and global) only; there is no role-scoped allowlist. Add a role-dimension to the allowlist model + middleware (resolve role before gating,… |
| 🔴 | AUTH-014 — Geo-restrictions (block sign-ins from countries outside tenant policy) | P1 | EE | L | Entirely unimplemented: no IP→country lookup, no per-tenant allowed/blocked-country policy, no enforcement at sign-in. Requires a GeoIP provider integration + TenantSettings country policy + sign-i… |
| 🔴 | AUTH-015 — Impersonation by support requires dual approval and is fully audited | P0 | CE | XL | No support impersonation capability at all: no dual-approval request flow, no time-boxed act-as session, no impersonation banner/marking, and no dedicated audit trail distinguishing impersonated ac… |

### CLI & Local Daemon (PRD v2 §14.8)

_RadioPad ships a real .NET 8 global tool (cli/RadioPad.Cli, command name `radiopad`) that covers the CLI-001..CLI-010 (CE/EE) band well: device-flow login, daemon lifecycle, generate-from-local-input, rulebook/report validation, golden-case rulebook test, template import/export, provider list/test/register, a local PHI policy guard, resumable audit sync, and a global --headless mode. These ten are the ONLY §14.8 items tracked in docs/09-regulatory/traceability-matrix.md (section literally titled "CLI — Command-line tool (10)"), and code confirms them. Beyond CLI-010 there is a steep drop-off: the higher-numbered CLI items (CLI-011 eval-with-metrics, CLI-013 doctor, CLI-014 model pull/list/prune, CLI-015 shell completions, CLI-016 universal JSON output, CLI-017 plugin install, CLI-018 pacs query/fetch, CLI-019 benchmark, CLI-020 migrate) are mostly missing or only stubbed, and the ENTIRE §14.8.3 daemon section (DMN-001..016) is unaddressed AS A DAEMON. Per the locked-stack rule I credited daemon FUNCTIONS that live in the ASP.NET API instead of a separate Rust `radiopadd` binary: local llama.cpp inference (DMN-005 partial), provider-router policy engine (DMN-004), env-var secret resolution (DMN-006 partial), append-only audit log (DMN-009 partial), per-provider/plan quotas (DMN-010 partial), health+ready endpoints (DMN-012 partial), startup auto-migration (CLI-020 function). But there is genuinely no UNIX-socket/named-pipe/mTLS RPC, no gRPC+reflection, no embedded ONNX runtime, no OS-keychain/DPAPI vault, no SQLCipher encrypted cache, no WASM plugin host, no durable offline audit buffer that flushes when the control plane reconnects, no Prometheus metrics endpoint, no signed EdDSA self-update, no hot config reload, no crash-restart backoff supervisor, and no PHI-stripped crash dumps. Key files: cli/RadioPad.Cli/Program.cs, Commands/{DeviceFlow,Daemon,AuditSync,ProviderRegister,PhiGuard,Templates,ValidationPacks,CliRuntime}.cs, PluginVerifier.cs; backend AiGateway.cs, ProviderSecretResolver.cs, LlamaCppProvider.cs, Program.cs (health/ready/migrate), PacsController.cs (server-side QIDO). Note: the daemon/ directory is LEGACY Open Design Node.js, not RadioPad._

Coverage: ✅6 · 🟡18 · 🔴11 · ⏸1

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| ✅ | CLI-001 — `radiopad login` via OAuth Device Authorization Grant or browser PKCE | P0 | CE | M | Only the Device Authorization Grant is implemented. The PRD's alternative 'browser-based PKCE' (authorization-code + PKCE with a local loopback redirect) is not present. The non-device `login` path… |
| 🟡 | CLI-002 — `radiopad daemon start\|stop\|status\|logs\|restart` | P0 | CE | M | The `logs` subcommand from the requirement is NOT implemented (only start/stop/status/restart exist). Process stdout/stderr is not redirected/captured (RedirectStandardOutput=false), so there is no… |
| 🟡 | CLI-003 — `radiopad generate` from local input files (text/JSON/FHIR) | P1 | CE | M | Input is read as raw text only (findings) — there is no JSON or FHIR input parsing/mapping as the requirement enumerates (it just dumps the whole file into Findings). Indication/technique/compariso… |
| ✅ | CLI-004 — `radiopad validate` for rulebook and report validation | P0 | CE | S | None material. A top-level `radiopad validate` alias does not exist (validation is under `rulebook` and `report` subtrees) but both functions are present. |
| ✅ | CLI-005 — `radiopad rulebook test` regression vs golden cases | P0 | CE | S | PRD example shows `--cases ./golden-cases --report html`; the implemented command has --cases but no `--report html` formatted output (console text only). |
| 🟡 | CLI-006 — `radiopad templates import\|export\|list\|diff` | P1 | CE | M | The `diff` subcommand is NOT implemented. Only list/import/export of the four required verbs exist. |
| 🟡 | CLI-007 — `radiopad ai providers list\|test\|set-default` | P1 | CE | M | Command group is `provider` not `ai providers`. The `set-default` verb is NOT implemented (no way to set the default provider from the CLI; default routing is server-side). `register` exists but is… |
| ✅ | CLI-008 — Enforce tenant model policies locally before any request leaves the machine | P0 | CE | M | Guard is defence-in-depth and fail-open when it cannot evaluate (network error returns 0). It only covers the PHI/compliance dimension; broader 'tenant model policies' (e.g. allowed-model lists, re… |
| ✅ | CLI-009 — `radiopad audit export` sync to control plane or local file | P0 | CE | M | PRD example shows `audit export --format ndjson --redact-phi`; the export verb prints raw JSON (no --format / no --redact-phi). Redaction and NDJSON formatting live only in the separate `sync` verb… |
| 🟡 | CLI-010 — Headless mode for enterprise deployment with config file | P1 | EE | M | Headless = no-prompt + non-zero-on-missing-config only. There is no dedicated enterprise config-file flag (e.g. --config <path> / env override) beyond the fixed ~/.radiopad/config.json, and no broa… |
| 🟡 | CLI-011 — `radiopad eval` harness (edit-distance, rule pass-rate, hallucination flags) | P1 | EE | L | Only a binary HTTP-success pass/fail. None of the three required metrics (edit-distance, rule pass-rate, hallucination flags) are computed or reported. Not in the traceability matrix. |
| 🟡 | CLI-012 — `radiopad fhir import\|export` bulk FHIR operations | P1 | EE | L | No dedicated `radiopad fhir` command group and no BULK operations. Single-resource ingest and single-report export exist; bulk import/export (e.g. FHIR $export / batch bundles) is absent. |
| 🔴 | CLI-013 — `radiopad doctor` system diagnostic | P0 | CE | M | Entire diagnostic command missing: no network check, model-availability probe, daemon-health check, or latency probes. Building blocks exist (daemon status, provider health POST /api/providers/{id}… |
| 🔴 | CLI-014 — `radiopad model pull\|list\|prune` (GGUF/ONNX) | P1 | CE | L | No local model lifecycle management at all (pull/list/prune). There is no model store or downloader; the llama.cpp/Ollama adapters assume an already-running external server. |
| 🔴 | CLI-015 — Shell completions: bash, zsh, fish, PowerShell | P0 | CE | M | No way to emit per-shell completion scripts; the four required shells are unsupported as a deliverable. |
| 🟡 | CLI-016 — JSON output mode for every command (for piping) | P0 | CE | L | No universal JSON output mode. Table/line-formatted commands cannot be switched to machine-readable JSON, so 'every command' is not satisfied. |
| 🟡 | CLI-017 — `radiopad plugins install <name>` from registry or local path | P1 | CE | L | No `plugins install` verb and no registry client / local-path installer. Only verify/enable/disable of already-present plugins exists; nothing fetches or installs a plugin by name or path. |
| 🟡 | CLI-018 — `radiopad pacs query\|fetch` against a DICOMweb node | P1 | CE | M | The CLI `pacs` group only exposes `plugins` (no `query`/`fetch` verbs). `dicom fetch` resolves a report's DICOM context, not a free-form DICOMweb query/fetch against an arbitrary node. No CLI surfa… |
| 🔴 | CLI-019 — `radiopad benchmark` for local performance regressions | P2 | CE | M | No workstation benchmark command at all. |
| 🟡 | CLI-020 — `radiopad migrate` for schema migrations on self-hosted deployments | P0 | CE | M | No `radiopad migrate` CLI command — migrations only run implicitly when the API boots. Operators cannot run/inspect/roll migrations from the CLI as the requirement specifies. |
| ⏸ | DMN-001 — Rust, statically-linked daemon, ≤35 MB binary, ≤30 MB RSS idle | P0 | CE | XL | N/A under the locked stack for the Rust/binary-size/RSS specifics. If a true sidecar daemon were ever required, none exists; the ASP.NET API is heavier than the size/RSS budget but those numbers ar… |
| 🔴 | DMN-002 — UNIX socket / named pipe / local TCP+mTLS client RPC | P0 | CE | L | No UNIX-domain-socket or Windows named-pipe transport, and no mTLS on the local TCP channel. Transport is plain loopback HTTP only. |
| 🟡 | DMN-003 — gRPC API with reflection; OpenAPI proxy for browser clients | P0 | CE | L | No gRPC server and no gRPC reflection anywhere (no Grpc.AspNetCore reference, grep for gRPC in backend src found none). The RPC surface is REST/OpenAPI only. |
| ✅ | DMN-004 — Provider router policy engine evaluates per-request before external call | P0 | CE | S | Policy is evaluated in-process inside the API, which is the daemon equivalent under the locked stack — functionally complete. No separate hot-swappable policy file (see DMN-013). |
| 🟡 | DMN-005 — Embeds llama.cpp + ONNX Runtime; auto-selects backend (CPU/CUDA/Metal/ROCm/Vulkan/DirectML) | P0 | CE | XL | llama.cpp is integrated as an EXTERNAL HTTP server adapter, not embedded in-process. No ONNX Runtime integration at all. No backend auto-selection logic (CUDA/Metal/ROCm/Vulkan/DirectML) — that is … |
| 🟡 | DMN-006 — Secret vault via OS keychain (Keychain/DPAPI/Secret Service/kwallet); age-encrypted fallback | P0 | CE | L | No OS keychain integration (macOS Keychain, Windows DPAPI/CNG, Linux Secret Service/kwallet) and no age-encrypted file fallback. Secret material lives in environment variables, not an OS-backed vault. |
| 🔴 | DMN-007 — Encrypted local cache (SQLCipher) with WAL + tenant-keyed encryption | P0 | CE | L | No SQLCipher-encrypted local cache with WAL and tenant-keyed page encryption. At-rest protection is column-level only, in the main DB, not a tenant-keyed local cache. |
| 🟡 | DMN-008 — Plugin host (WASM/WASI preview 2) for connectors/tools, capability-limited | P1 | CE | XL | No WASM/WASI runtime host. Connectors/tools are not executed as capability-sandboxed WASM modules; the model is MCP scope policy + signed manifests, not a WASI preview-2 sandbox. |
| 🟡 | DMN-009 — Audit buffer: durable local SQLite-WAL queue surviving crashes, flushes to control plane | P0 | CE | L | No durable LOCAL OUTBOUND queue that buffers events during control-plane outages and auto-flushes on reconnect. `audit sync` is a manual pull-to-file, not a crash-surviving store-and-forward buffer… |
| 🟡 | DMN-010 — Resource quotas: max RAM/GPU mem/parallel-requests per provider, per tenant | P0 | CE | L | Quotas are usage/plan-based (credits, request counts). There is no enforcement of max RAM, max GPU memory, or max parallel requests per provider — the physical-resource quotas the requirement names… |
| 🔴 | DMN-011 — Self-update channel separate from Studio (signed, EdDSA) | P0 | CE | L | No self-update mechanism at all (no update channel, no signed-release fetch/apply). The CLI is distributed as a dotnet global tool with no built-in updater. |
| 🟡 | DMN-012 — Health, ready, metrics (Prometheus) endpoints | P0 | CE | M | No Prometheus /metrics scrape endpoint is exposed (no prometheus-net/OpenTelemetry exporter mapped; grep found dashboards/docs but no scrape route). Health+ready present; metrics endpoint missing. |
| 🔴 | DMN-013 — Hot reload of policy and provider config without restart | P1 | CE | M | No file-based policy/provider config with live reload. Changing config requires DB writes (which take effect per-request) or a restart for app settings; no explicit hot-reload subsystem. |
| 🔴 | DMN-014 — Auto-restart on crash with exponential backoff; max 5 restarts/5 min before alerting | P0 | CE | M | No crash supervisor: the CLI does not monitor the spawned process, does not auto-restart with exponential backoff, and has no 5-in-5-minutes alert threshold. |
| 🔴 | DMN-015 — Crash dumps PHI-stripped before any optional upload | P0 | CE | L | No crash-dump generation, no PHI-stripping of dumps, and no opt-in upload path. |
| 🔴 | DMN-016 — Confidential computing mode (SGX/SEV) for inference tenants | P2 | EE | XL | Entire confidential-computing inference mode is absent (P2/EE). |

### Data &amp; Audit Model (PRD v2 §17)

_RadioPad implements the spine of §17 — a per-tenant, append-only, hash-chained AuditEvent log with a working VerifyChain endpoint (UI + API + CLI), a retention worker honoring RetentionDays + LegalHold, and an AiRequest usage ledger that already stores input/output as hashes only. However, the implementation is materially shallower than the PRD specifies in three areas. (1) §17.1 entity catalog: ~13 of 21 named entities exist; six clinical/longitudinal entities (Critical Result, Peer Review, Teaching Case, Citation, Worklist Item, Lesion) and dedicated AI Response / Validation Result / Report Final entities are absent. (2) §17.2 audit schema: the rich structured event (uuidv7 event_id, ISO event_time, actor{auth_method}, subject{hashes}, ai{model_version,token counts,phi_class,input/output blake3}, validation[], chain{prev/this}, ed25519 signature) is collapsed into a flat {Action enum, DetailsJson} row; hashing is SHA-256 not blake3, there is NO per-event ed25519 signature, NO RFC 3161 timestamp anchoring, and NO Rekor-style transparency log. (3) §17.3 retention: retention is a single tenant-wide RetentionDays value, NOT configurable per data class; HashOnlyAuditMode is a stored/settable flag that is never enforced at write time; there is no right-to-delete workflow; the NDJSON/CEF export exists but is NOT a signed bundle. Per the CLAUDE.md stack-lock rule, items are judged on function (ed25519/blake3/Rekor naming is not penalized for being absent libraries — they are penalized because the FUNCTION, tamper-evident per-event signing and external anchoring, genuinely does not exist on any stack)._

Coverage: ✅14 · 🟡16 · 🔴11 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| ✅ | §17.1 Tenant entity (isolation boundary) | P0 | Core | S | None. Tenant is the canonical isolation boundary across the codebase. |
| ✅ | §17.1 User entity (radiologist/admin/reviewer/IT/researcher/auditor) | P2 | Core | S | PRD lists six personas; verify UserRole enum covers researcher/auditor distinctly (Radiologist/admin/reviewer/IT present). Minor taxonomy gap only. |
| ✅ | §17.1 Role / Permission (fine-grained, tenant-scoped) | P2 | Core | M | RBAC is a static role→permission map, not a per-tenant editable fine-grained grant table; 'fine-grained, tenant-scoped' custom roles are not persisted as entities. |
| 🟡 | §17.1 Device entity (studio install / daemon, signed cert) | P2 | Core | L | No first-class persistent Device entity with a signed device certificate; device identity is a fingerprint hash on auth/session rows, not a cert-bound enrolled device record. No X.509/signed-cert d… |
| ✅ | §17.1 Study Context (modality/body part/accession/patient ref HL7 PID + FHIR Patient.id) | P3 | Core | S | PatientReference is a single string; PRD wants explicit HL7 PID + FHIR Patient.id binding. Functionally covered by ServiceRequestRef + PatientReference but not structured as two typed fields. |
| 🟡 | §17.1 Report Draft (CRDT document) | P2 | Core | XL | Draft is a plain mutable row, NOT a CRDT document — no conflict-free replicated data type / collaborative merge structure. Multi-editor concurrency relies on versions/signatures, not CRDT. |
| 🟡 | §17.1 Report Version (snapshot per edit/export with content hash) | P1 | Core | S | ReportVersion has NO content-hash field — PRD requires a per-version content hash. Snapshot is stored but not cryptographically fingerprinted on the row. |
| 🟡 | §17.1 Report Final (immutable export record) | P1 | Core | M | No dedicated immutable 'Report Final' export-record entity capturing the exact exported artifact + destination + content hash. Finalization is inferred from status + signatures; the immutable expor… |
| ✅ | §17.1 Template (versioned, bound rulebook(s)) | P3 | Core | S | Template→rulebook binding is via Report.RulebookId/TemplateId at report time, not an explicit bound-rulebook list on the template row. Versioning exists via Status but no semver field on template. |
| ✅ | §17.1 Rulebook (versioned governance package) | P3 | Core | S | None material — full versioned governance lifecycle exists. |
| ✅ | §17.1 Prompt Block (reusable component) | P3 | Core | S | Prompt blocks are tenant overrides keyed to a rulebook, not free-standing globally-reusable components with their own versioning; functionally adequate. |
| ✅ | §17.1 AI Request (input to provider) | P3 | Core | S | None for the request itself; see AI Response (it is merged into this same row rather than a separate entity). |
| 🟡 | §17.1 AI Response (output, token counts, latency, cost) | P3 | Core | S | No separate AI Response entity; response is folded into AiRequest. Per-row cost is not persisted on the AI row (computed at summarise time). Acceptable functionally but diverges from PRD's distinct… |
| 🟡 | §17.1 Validation Result (per check, severity, outcome, span refs) | P2 | Core | M | Validation results are not persisted as a dedicated entity with span references; they are recomputed and embedded in audit DetailsJson / responses. No stored per-check row with span anchors for lon… |
| ✅ | §17.1 Provider Config (compliance class, endpoints, credentials by ref) | P3 | Core | S | None material — credentials stored by reference; compliance class enforced in AiGateway. |
| 🟡 | §17.1 Audit Event (immutable, cryptographically chained per tenant) | P1 | Core | L | Chain exists and is per-tenant, but the EVENT is a flat {Action enum, DetailsJson} not the rich §17.2 schema, and there is NO per-event cryptographic signature (see §17.2 rows). Immutability is by-… |
| ✅ | §17.1 Subscription (plan, seats, usage, billing) | P3 | Core | S | Subscription state is spread across TenantSettings rather than a discrete Subscription entity; seats enforced via plan limits not an explicit seat ledger. Functionally complete. |
| 🔴 | §17.1 Critical Result (notification, ack, escalation chain) | P1 | Core | L | Entire critical-results notification/acknowledgement/escalation-chain entity and workflow is absent. |
| 🔴 | §17.1 Peer Review (case, reviewer, score, rationale) | P2 | Core | L | Peer-review entity and workflow entirely absent. |
| 🔴 | §17.1 Teaching Case (anonymized, categorized) | P3 | Core | L | Teaching-case entity (anonymization + categorization) entirely absent. |
| 🔴 | §17.1 Citation (bibliographic record, persistent identifier) | P3 | Core | M | Citation entity with persistent identifier (DOI/PMID) entirely absent. |
| 🔴 | §17.1 Worklist Item (DICOM UPS or HL7-derived) | P2 | Core | L | No dedicated Worklist Item entity (DICOM UPS / HL7-derived queue). Orders flow straight into Report drafts. |
| 🔴 | §17.1 Lesion (longitudinal cross-study with measurements) | P3 | Core | XL | Longitudinal lesion entity + cross-study measurement tracking entirely absent. |
| ✅ | §17.1 Plugin (manifest, signature, capabilities, version) | P3 | Core | S | Plugin manifest+signature+version+capabilities are fully modeled and verified; minor: 'capabilities' is encoded as scope tokens rather than a typed capability list. |
| 🟡 | §17.2 Structured event envelope (event_id uuidv7, event_time, event_type) | P2 | Core | M | event_id is UUIDv4 not uuidv7 (no time-ordered id); event_type is a fixed enum, not the PRD's open 'ai.request.completed \| report.export.signed \| ...' dotted namespace. event_time is CreatedAt (a… |
| 🔴 | §17.2 actor sub-object (user_id, role, device_id, auth_method) | P1 | Core | M | Structured actor block absent; role, device_id, and auth_method (webauthn\|totp\|saml\|oidc) are not recorded per event. Could be carried in DetailsJson but is not done consistently. |
| 🟡 | §17.2 subject sub-object (report_id, study_ref hash, patient_ref hash, modality) | P2 | Core | M | No structured subject block; study_ref/patient_ref hashing and modality are not consistently stamped on every event. |
| 🟡 | §17.2 ai sub-object (provider, model_id, model_version, prompt_block_versions, rulebook_version, template_version, input/output blake3 hash, token counts, latency, phi_class) | P1 | Core | M | Spread across AiRequest, not embedded in the audit event's ai block. Missing model_version, prompt_block_versions[], template_version, and phi_class (phi\|de-identified\|meta). Hashes are SHA-256 n… |
| 🟡 | §17.2 validation sub-object (results[] with rule/outcome/severity) | P2 | Core | M | Validation results are not a structured, queryable array on the audit event; embedded ad-hoc in DetailsJson at best. |
| 🟡 | §17.2 user_action (accepted \| edited \| rejected) | P3 | Core | S | No single normalized user_action field (accepted/edited/rejected) on the AI/report audit event; acceptance is inferred from separate action types. |
| 🟡 | §17.2 export sub-object (destination ris\|pdf\|docx\|fhir\|hl7v2\|none, destination_id) | P3 | Core | S | No structured export{destination,destination_id} block standardized on the event; destination metadata is free-form in DetailsJson, not a typed enum field. |
| ✅ | §17.2 chain sub-object (prev_event_hash, this_event_hash) — per-tenant hash chain | P2 | Core | S | Functionally satisfies prev/this hash chaining per tenant. Divergence from PRD: single IntegrityChain column conflates prev+this (prev is not separately stored), and digest is SHA-256 not blake3. T… |
| 🔴 | §17.2 Per-event signature (ed25519) | P1 | Core | M | No cryptographic per-event signature at all — the hash chain proves ordering/integrity but not authorship/non-repudiation. The FUNCTION (signed events) is absent on any stack. |
| 🔴 | §17.2 Periodic anchoring to external timestamping authority (RFC 3161) | P1 | Core | L | External tamper-evidence anchoring entirely absent — chain can be wholly rebuilt by a DB-admin without external detection. Function missing on any stack. |
| 🔴 | §17.2 EE transparency log inclusion (Rekor-style) | P3 | EE | L | Enterprise transparency-log inclusion entirely absent. Function missing on any stack. |
| 🟡 | §17.3 Tenant-configurable retention PER data class (drafts, finals, AI logs, audit, telemetry) | P1 | Core | M | Retention is a SINGLE tenant-wide RetentionDays applied to AI logs + report versions only. No per-data-class policy (separate windows for drafts vs finals vs AI logs vs telemetry). Finals/telemetry… |
| ✅ | §17.3 PHI minimization default (do not retain raw AI input/output beyond N days) | P3 | Core | S | Effectively exceeds the default — raw AI bodies are never stored at all, so 'beyond N days' is moot. Compliant. Minor: no per-tenant toggle to opt INTO retaining raw bodies for tenants whose policy… |
| 🟡 | §17.3 'Hash-only audit' mode (store only hashes of input/output) | P2 | Core | S | The flag is NEVER READ/ENFORCED at audit-write time — grep shows HashOnlyAuditMode is only get/set, never branched on in EfAuditLog or AiGateway. Because AI bodies are already hash-only by default,… |
| ✅ | §17.3 Legal hold flag overrides retention | P2 | Core | S | None for the retention-override behavior. Legal hold is tenant-wide only (not scoped to a specific report/case), which is coarser than typical legal-hold granularity but satisfies the clause. |
| 🔴 | §17.3 Right-to-delete workflow (with audit of the delete itself) | P1 | Core | L | No subject-initiated erasure workflow, no per-patient delete with its own audit entry. Retention purge does emit a RetentionPurge audit event, but on-demand right-to-delete is absent. |
| 🟡 | §17.3 Exportable compliance logs (NDJSON, signed bundle) | P2 | Core | M | NDJSON export is present and good, but it is NOT a SIGNED bundle — no detached signature / manifest / sealed archive over the export. SIEM export also deliberately omits DetailsJson, so it is a met… |

### Desktop App / RadioPad Studio (PRD v2 §14.7, DESK-001 … DESK-025)

_RadioPad Studio is a Tauri 2 shell (desktop/src-tauri) that loads the static Next.js export and supervises the bundled .NET API sidecar. The security/data spine of §14.7 is genuinely built and reasonably solid: AES-256-GCM-encrypted offline drafts + local cache keyed off the OS keyring, a sha256+ed25519 plugin/model verifier with OS-level sandbox launch, a PHI-redacting tracing layer, a global-hotkey set, secure-clipboard with TTL + clear-on-blur, RFC-8628 device pairing (backend DeviceAuthController + /pair UI + keyring token), a signed PACS-plugin manifest loader, and ed25519 auto-update + per-OS code-signing documented in runbooks. About 11 of the 25 line items are implemented or close; ~4 are partial; ~10 are entirely missing. The biggest gaps are the day-to-day UX surfaces a reporting-room hero is judged on: there is NO tray/menu-bar daemon control + provider switcher (DESK-014, P0), NO OS-native notifications (DESK-013), NO multi-monitor floating overlay or 380x600 mini-panel (DESK-011/DESK-024), NO crash reporter (DESK-017), NO accessibility/AT compatibility work (DESK-018, P0), and NO power-saver, Wacom/pen, foot-pedal, locale hot-reload, per-user themes/high-contrast, or fleet management. Several "implemented" items are also functionally narrower than the clause (last-write-wins instead of full conflict resolution; regex redactor instead of Presidio + log rotation; SQLite-store encryption instead of SQLCipher; no proven ARM64 build matrix). Where the PRD named a specific OSS library that conflicts with the locked stack (e.g. SQLCipher, Presidio, Sentry-OSS), I judged by function on the Tauri/.NET stack per CLAUDE.md, but I still mark items partial/missing when the FUNCTION (rotation, true conflict resolution, crash capture) is absent — not merely the named library._

Coverage: ✅2 · 🟡12 · 🔴11 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | DESK-001 — Windows (x64+ARM64), macOS (Intel+Apple Silicon), Linux (x64+ARM64) | P0 | CE | M | No build evidence for ARM64 on Windows or Linux: the update manifest in UPDATE_SIGNING.md omits windows-aarch64 and linux-aarch64, and there is no CI matrix file in-repo proving an ARM64 cross-comp… |
| ✅ | DESK-002 — Auto-start and manage radiopadd local daemon | P0 | CE | S | The local daemon managed is the .NET API sidecar (radiopad-api), which satisfies the locked-stack equivalent of `radiopadd`. Minor: restart cap of 3 then gives up with a `failed` state; no user-fac… |
| 🟡 | DESK-003 — Global hotkeys (dictation, generate impression, rewrite, copy, paste, voice command toggle) | P0 | CE | S | Two of the six named actions are missing: there is no PASTE hotkey and no VOICE-COMMAND-TOGGLE hotkey (voiceCommands.ts exists in frontend but is not bound to a global shortcut). 'Copy' is implemen… |
| 🟡 | DESK-004 — Secure clipboard mode with automatic timeout (configurable, default 30s) | P1 | CE | S | The 30s default is NOT enforced in Rust — the TTL is supplied entirely by the frontend caller; no default applied if omitted, and no documented/UI-exposed 'configurable' setting binding the default… |
| 🟡 | DESK-005 — Local encrypted cache (SQLCipher) for drafts and temporary inputs | P0 | CE | M | Implementation is AES-256-GCM over a JSON store, NOT SQLCipher. Per CLAUDE.md stack-lock the encryption FUNCTION is satisfied (at-rest AES-256, OS-keyring key). However it is a key/value store, not… |
| 🟡 | DESK-006 — Offline draft editing with full conflict resolution on reconnect | P1 | CE | L | Conflict resolution is last-write-wins only (offlineDrafts.ts: `rev` is a 'monotonic edit counter for last-write-wins'; sync blindly PATCHes server). 'Full conflict resolution' (server-vs-local dif… |
| 🟡 | DESK-007 — Local PACS/RIS bridge plugin host | P1 | CE | L | It hosts/verifies/enables plugin manifests but does not actually RUN a PACS/RIS bridge: no connector process launch from this path, no DICOM/HL7/RIS transport, no data exchange with a third-party r… |
| 🟡 | DESK-008 — Device authorization and tenant pairing (QR code or device-flow OAuth) | P0 | CE | M | Two gaps: (1) NO QR-code option — only the manual user_code entry path exists (PRD allows 'QR code OR device-flow', so device-flow alone is acceptable, but QR is not implemented). (2) The approval … |
| ✅ | DESK-009 — Local model/plugin execution where enabled | P1 | CE | M | Verification + sandboxed launch are real and tested. Minor: the local Whisper model execution (desktop/whisper.md) is documented but not wired in code (no cpal capture / whisper sidecar binding pre… |
| 🟡 | DESK-010 — Local logs with PHI redaction (Presidio + tenant rules) and rotation | P0 | CE | M | Two gaps vs clause: (1) redaction is a small hand-rolled regex set, NOT Presidio and NOT tenant-rule-driven (no per-tenant pattern config). (2) NO log rotation — the writer forwards to stderr; ther… |
| 🔴 | DESK-011 — Multi-monitor support; floating mini overlay above third-party reporting tools | P1 | CE | L | Entire feature absent: need monitor enumeration (available_monitors), an always-on-top, frameless, click-through-capable overlay window that floats above third-party PACS apps, and per-monitor plac… |
| 🔴 | DESK-012 — Battery-aware power-saver mode | P1 | CE | M | Entire feature absent: need OS power-source/battery detection and a power-saver mode that reduces polling/animation/background work on battery. |
| 🔴 | DESK-013 — OS-native notifications (critical results, peer-review assignments) | P1 | CE | M | Entire feature absent: add tauri-plugin-notification (+ permission), and wire backend events for critical results / peer-review assignments to OS-native toasts. |
| 🔴 | DESK-014 — Tray/menu bar daemon control + provider switcher | P0 | CE | L | Entire P0 feature absent: need a system-tray/menu-bar icon with daemon (sidecar) start/stop/restart controls and an in-tray provider switcher. This is a P0 line item and a defining 'unobtrusive rep… |
| 🟡 | DESK-015 — Auto-update with EdDSA-signed bundles and rollback if first boot fails | P0 | CE | L | Two gaps: (1) plugins.updater.pubkey is empty ('') in the committed config — updates are unverifiable until CI injects TAURI_SIGNING_PUBLIC_KEY (documented, but no default/dev key, so update verifi… |
| 🟡 | DESK-016 — Code-signed installers; macOS notarized; Windows EV cert | P0 | CE | M | All signing is documentation + CI-time injection: tauri.conf.json ships certificateThumbprint=null and macOS signingIdentity=null, and the referenced workflows (.github/workflows/desktop-release.ym… |
| 🔴 | DESK-017 — Crash reporter (opt-in, PHI-stripped) — Sentry self-hosted or Bugsnag-OSS | P1 | CE | L | Entire feature absent: add a panic/crash capture hook with PHI stripping (can reuse log_redactor::redact), an opt-in consent gate, and a self-hosted Sentry/Bugsnag-OSS-equivalent transport. |
| 🔴 | DESK-018 — Accessibility: NVDA, JAWS, VoiceOver, Orca compatibility | P0 | CE | L | P0 with no dedicated work: WebView a11y inherits the documented frontend a11y gaps, and there is no evidence of testing/fixing against NVDA/JAWS/VoiceOver/Orca, nor native accessibility-tree exposu… |
| 🔴 | DESK-019 — Wacom / pen / touch input for finding annotations and structured drawing | P2 | CE | XL | Entire P2 feature absent: need a pressure-aware pen/touch annotation canvas (PointerEvents w/ pressure/tilt) for finding markup and structured drawing. |
| 🔴 | DESK-020 — Foot-pedal support (USB HID) for dictation start/stop and jog | P1 | CE | L | Entire feature absent: add USB-HID device enumeration (e.g. hidapi crate) and map foot-pedal events to dictation start/stop and jog. |
| 🟡 | DESK-021 — Multi-language UI with hot reload of locale | P1 | CE | M | NOT hot reload: LocalePicker.onChange explicitly calls window.location.reload() ('Hard reload so server + client message bundles realign'), and docs/ui-ux-audit notes 'LocalePicker does full page r… |
| 🔴 | DESK-022 — Per-user color themes and high-contrast modes | P1 | CE | L | Feature absent and partly in tension with the design lock (no dark mode / no new accent tokens). A compliant path is a high-contrast/forced-colors accessibility variant (does not add brand accents)… |
| 🟡 | DESK-023 — Local-first plugin sandbox (WASM, capability-limited) | P1 | CE | L | Sandbox is OS-process isolation, NOT a WASM runtime (no wasmtime/wasmer). The manifest 'capabilities' list is surfaced but not enforced as a capability boundary at runtime (it is metadata; no per-c… |
| 🔴 | DESK-024 — Quick mini panel hotkey opens a 380x600 overlay for dictation + impression without leaving PACS | P1 | CE | L | Entire P1 feature absent: add a dedicated global hotkey that opens a 380x600 always-on-top frameless overlay window hosting dictation + impression generation, independent of the main window so the … |
| 🔴 | DESK-025 — Centralized fleet management hook (MDM, Intune, Jamf, Ansible) | P2 | EE | XL | Entire EE feature absent: add managed-configuration ingestion (Windows ADMX/registry policies for Intune, macOS configuration profile/managed prefs for Jamf, a deploy config file for Ansible) so fl… |

### Mobile Companion + Plugin Marketplace (PRD v2 §14.19, §14.20)

_RadioPad delivers a meaningful subset of §14.19/§14.20 but the two flagship clinical mobile features and the entire WASM-plugin runtime are absent. STRONG, production-grade implementations exist for: push notifications (real APNs ES256 token-auth + FCM HTTP v1 OAuth2 in PushSender.cs, registration in PushController), biometric unlock (frontend/lib/biometric.ts + ShellBridge gating token release), a Capacitor 6 shell wrapping the Next.js export, offline drafts, on-device voice dictation, and a fully-functional content marketplace with Stripe Connect revenue-share (MarketplaceController). NOTE the marketplace is a CONTENT marketplace (rulebooks/templates/prompt-packs), and the "plugin SDK" (desktop/plugin-sdk) is a PACS-vendor signed-manifest adapter SDK (Ed25519+SHA-256) — NOT the PRD's Rust+WASM/WASI code-plugin runtime. CRITICAL GAPS: MOB-003 critical-results notifications with WebAuthn acknowledgement (no critical-results flow, no WebAuthn-ack linkage — WebAuthn exists only for login), MOB-004 peer-review assignments/scoring (no controller, no UI), MOB-002 'no editing in MVP for safety' is contradicted (the only dedicated mobile pages — frontend/app/mobile/{dictate,reports/edit,reports/sign} — are editors), MOB-009 screenshot protection (doc-only, no FLAG_SECURE/iOS native code), PL-001/PL-002/PL-006 (no WASM ABI, no WASI sandbox with CPU/memory quotas — the only sandbox, InProcessMcpSandbox, explicitly states 'no hard OS-level isolation' and only enforces a wall-clock timeout), PL-005 per-tenant plugin allowlist/pinning, PL-003 listing reviews/security-scan/SBOM/license, PL-004 certification tiers. Stack-lock note applied: function judged on Next.js/.NET/Tauri/Capacitor stack regardless of OSS libs named in PRD §9._

Coverage: ✅3 · 🟡9 · 🔴7 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | MOB-001 — Read-only worklist with filters | P1 | CE | M | Add a purpose-built read-only mobile worklist view (touch-optimised, filter chips for modality/status/date/assignee) rather than relying on the desktop-shaped table reflowed in the WebView. Confirm… |
| 🟡 | MOB-002 — Draft report viewer (no editing in MVP for safety) | P1 | CE | M | PRD explicitly requires a read-ONLY draft viewer for the MVP 'for safety' and defers editing. Current implementation does the opposite (mobile editing is enabled). Either ship a read-only viewer an… |
| 🔴 | MOB-003 — Critical-results notifications with WebAuthn acknowledgement | P1 | CE | XL | Build the full feature: critical-results notification model/trigger (likely off rulebook critical findings), targeted push dispatch (kind=critical), and a mobile acknowledgement screen that perform… |
| 🔴 | MOB-004 — Peer-review assignments and quick scoring | P1 | CE | XL | Implement peer-review assignment model + endpoints (assign reviewer, list assignments, submit score/rubric) and a mobile quick-scoring screen. Entire feature absent. |
| 🟡 | MOB-005 — Approval flows (rulebook promotion, template publishing) with step-up auth | P1 | EE | L | No mobile-specific approval surface and, critically, no step-up (WebAuthn/re-auth) enforced AT the approval action. Wire a step-up assertion before approve/promote/publish and provide a mobile appr… |
| ✅ | MOB-006 — Biometric unlock; no PHI cached locally beyond session unless explicitly enabled | P0 | CE | S | Minor: there is no explicit per-tenant/per-user 'allow local PHI caching' toggle named in the clause; behaviour is effectively 'no ambient cache' which satisfies the default. Add a settings toggle … |
| 🟡 | MOB-007 — Offline read with encrypted cache (size-limited), opt-in per tenant | P2 | CE | L | Three deficiencies vs clause: (1) mobile cache is NOT encrypted (plaintext Preferences) — only desktop is AES-GCM; (2) NO size limit/eviction policy; (3) NOT opt-in per tenant (no tenant flag gatin… |
| ✅ | MOB-008 — Push notifications via APNs (iOS) and FCM (Android); EE self-hosted relay | P1 | CE | S | EE self-hosted relay option is not implemented (only direct APNs/FCM). PROGRESS.md mislabels push as MOB-007 vs PRD's MOB-008, but the function is present. Add an optional EE relay sender behind th… |
| 🔴 | MOB-009 — Screenshot protection (FLAG_SECURE / iOS guidance) | P0 | CE | M | Add FLAG_SECURE on the Android Activity (or a Capacitor plugin) and iOS screen-recording/screenshot mitigation (blur on background, UIScreen.isCaptured observer). P0 security control currently enti… |
| 🟡 | MOB-010 — Quick voice memo to attach to a study (transcribed locally) | P2 | CE | M | This is dictation-into-report-sections, not a 'quick voice memo attached to a study' as a discrete artifact. No memo entity, no attach-to-study linkage, no audio capture/storage. Implement a memo o… |
| 🔴 | MOB — PWA fallback (section intro requirement) | P1 | CE | M | Add a PWA manifest + service worker (next-pwa or App Router equivalent) so the app is installable and minimally usable as a PWA when native isn't available. |
| 🟡 | PL-001 — Plugin SDK (Rust + WASM ABI; TypeScript shim for connectors) | P1 | CE | XL | PRD's SDK is a code-extension SDK (Rust compiled to WASM with a defined ABI + a TS shim for connector authors). Current SDK is a declarative PACS-bridge manifest format with no executable plugin mo… |
| 🟡 | PL-002 — Plugin manifest with capabilities, permissions, signing, semver | P1 | CE | M | Two PRD elements weak/absent: (1) a general PERMISSIONS model (the schema has fixed PACS-capability enums + a coarse compliance.phi field, not a granular requested-permissions/scopes list a sandbox… |
| 🟡 | PL-003 — Marketplace listing with reviews, downloads, security scan status, license, SBOM | P2 | CE | L | Missing fields/features: user REVIEWS/ratings (ReviewNotes is reviewer approval feedback, not buyer reviews), no security-scan status, no LICENSE field, no SBOM artifact/link. 'Downloads' is partia… |
| 🔴 | PL-004 — Plugin certification track (clinical / connector / utility tiers) | P2 | CE/EE | L | Add a certification-tier field + a certification workflow (clinical/connector/utility) with reviewer gating and badge display. Absent. |
| 🔴 | PL-005 — Per-tenant plugin allowlist and pinning | P1 | EE | L | Implement a tenant-scoped plugin allowlist + version pinning (admin-managed), enforced at plugin load/enable time with audit. Entirely absent for plugins. |
| 🔴 | PL-006 — Plugin sandbox (WASI capability-limited) with CPU/memory/time quotas | P1 | CE | XL | Build a real WASI/Wasmtime (or equivalent) sandbox enforcing capability limits + CPU, memory, and wall-time quotas for executable plugins. Currently only a wall-clock timeout exists and only for in… |
| 🟡 | PL-007 — Hot install/uninstall without daemon restart | P2 | CE | M | Hot ENABLE/DISABLE works without restart, but there is no first-class hot INSTALL/UNINSTALL command (install = manually copy a folder; uninstall = manually delete) and new plugins are picked up on … |
| ✅ | PL-008 — Revenue-share model for paid plugins (post-GA) | P3 | EE | S | Functionally complete for the marketplace; only caveat is it is wired to content listings, not 'plugins' specifically (because executable plugins are not yet sellable artifacts). When plugin listin… |

### NLP & AI Report Generation (PRD v2 §14.3, AI-001 … AI-028)

_RadioPad implements the §14.3 P0 "drafting + impression + validation" core well on the locked ASP.NET Core stack. The AiGateway (backend/.../Application/Services/AiGateway.cs) is the single egress point and enforces PHI routing policy, full audit/usage tracing (input/output SHA-256 hashes, prompt version, model, provider, tokens, latency), and tenant-month quota. Generation modes (impression, cleanup/dictation-to-structured, draft, rewrites) live in ReportingService + DictationCleanupService + ReportRewriteService. The deterministic ReportValidator covers required-section detection, laterality conflict, measurement mismatch, negation/contradiction, modality mismatch, and a deterministic HallucinationDetector covers unsupported-claim detection. Layered prompt overrides with an approval gate (AI-009), composite cost/quality/latency model routing (AI-010), and local providers (Ollama/llama.cpp/vLLM for AI-011) are all real. The biggest gaps are squarely in the AI runtime-control and EE-tier safety features that were never built: there is NO streaming/stop/regenerate (AI-013), NO centralized temperature ceiling plumbed through AiCompletionRequest (AI-014 — temperature is hardcoded only in 2 of ~7 adapters and absent from the OpenAI-family body), NO enforced structured-output / JSON-Schema binding (AI-015 — OutputSchema is parsed from YAML but never used), NO prompt-injection / adversarial-input detection (AI-017), NO per-request/per-user-day token budget (AI-018, only tenant-month quota + rate limit), NO Presidio-style pre-egress PHI scrubbing pass (AI-020 — PhiRedactor only masks log lines; PHI is handled by routing-block, not scrub-then-send), and the entire EE/advanced set is absent: watermarking (AI-016), ensemble + cross-provider disagreement (AI-019/AI-022), quote-it source citation (AI-021), multilingual translation provenance (AI-023), personal style memory (AI-024), and speculative field completion (AI-025). Note: per CLAUDE.md the stack is locked to .NET/Next.js, so absence of Go/Rust/Keycloak/Presidio-the-library is NOT counted against — only the missing FUNCTION is._

Coverage: ✅10 · 🟡9 · 🔴9 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| ✅ | AI-001 — Convert raw dictation into clean structured report sections without altering clinical meaning | P0 | CE | M | Meaning-preservation is enforced only by prompt instruction; there is no automated post-check that the cleaned output did not add/drop a measurement, side, or negation vs the raw dictation (the det… |
| ✅ | AI-002 — Generate report drafts from structured inputs, free text, measurements, prior snippets | P0 | CE | M | Prior-report snippets are available to the UI/validation but are not automatically injected into the draft-generation user prompt (BuildPromptForMode 'draft' uses Study.Comparison text, not the ret… |
| ✅ | AI-003 — Generate impression from findings preserving clinical content | P0 | CE | S | Content preservation is enforced by prompt + the separate HallucinationDetector check, not by a hard guarantee; acceptable for P0 but not span-level traced (see AI-021/AI-028). |
| ✅ | AI-004 — Detect contradictions between findings and impression | P0 | CE | M | Contradiction detection is regex/keyword-based on negation cues and specific concept lists; semantic contradictions without explicit 'no <concept>' phrasing (e.g., 'stable' vs 'enlarging', size up … |
| ✅ | AI-005 — Detect missing required sections based on rulebook | P0 | CE | S | None material. Only top-level named sections are checked; sub-section/field-level completeness within a section is not modeled. |
| ✅ | AI-006 — Detect laterality conflicts, measurement mismatches, modality/body-part mismatch | P1 | CE | M | Body-PART mismatch (vs body REGION in the study context) is not explicitly checked — only modality phrase mismatch. Measurement mismatch is set-difference of normalized strings, so a value that cha… |
| ✅ | AI-007 — Detect uncertain/unsupported/hallucinated claims with confidence band, never shown as clinical certainty | P1 | CE | M | It is a deterministic lexical-overlap heuristic, not an uncertainty/hedging-language detector; 'uncertain' phrasing per se (e.g. 'possibly', 'cannot exclude') is not classified, and the support fra… |
| ✅ | AI-008 — Suggest follow-up language using only tenant-approved phrasebooks | P1 | CE | S | Allow-list lives only at rulebook style scope (ApprovedFollowups); there is no separate tenant-level phrasebook entity independent of a rulebook, and matching is exact normalized-string equality (n… |
| 🟡 | AI-009 — System/specialty/user/case-level prompts with layered overrides | P0 | CE | L | Only two effective layers exist (tenant-override + rulebook) on top of the system default. The PRD's four distinct scopes — specialty-level and per-USER and per-CASE prompt layers — are not modeled… |
| 🟡 | AI-010 — Model routing by tenant, user role, modality, PHI class, cost ceiling, latency budget | P0 | CE | M | Routing keys only on tenant + PHI class + cost/quality/latency scores. There is NO routing by USER ROLE and NO routing by MODALITY (these are not inputs to SelectAsync/ScoreAsync). Cost is a soft s… |
| ✅ | AI-011 — Local model execution for de-identified or PHI-sensitive workflows | P1 | CE | S | None material on the locked stack. Local adapters call a local HTTP endpoint (OpenAI-compatible) rather than in-process inference, which is the expected architecture. |
| 🟡 | AI-012 — Full traceability of prompt version, rulebook version, model id, provider, input hash, output hash, edits | P0 | CE | M | RULEBOOK VERSION is not part of the AI audit payload — the gateway records promptVersion + model but not the resolved rulebook id@version used for that run. Input hash is over UserPrompt only (syst… |
| 🔴 | AI-013 — Streaming token output to UI with stop/regenerate controls | P0 | CE | L | Entire feature absent: need a streaming endpoint (SSE/chunked) on the gateway+adapters, AiCompletionRequest streaming flag, a frontend token-stream renderer, a Stop (cancel) control wired to a Canc… |
| 🟡 | AI-014 — Deterministic temperature ceiling for clinical outputs (default 0.2) overridable per rulebook | P0 | CE | M | No centralized, enforced temperature ceiling: the OpenAI-direct/Azure/OpenAI-compatible/CLI adapters do not set temperature, so the 0.2 ceiling is neither uniform nor enforced. There is no per-rule… |
| 🟡 | AI-015 — Structured output enforcement (JSON Schema / FHIR shape) for downstream sections | P0 | CE | L | OutputSchema is parsed but NEVER enforced — grep shows zero usages outside its own declaration; it is not passed to providers as response_format/json_schema, not validated against model output, and… |
| 🔴 | AI-016 — Output watermarking (cryptographic provenance, optional perturbation) [EE] | P1 | EE | L | Entire EE feature absent: need a provenance signer (sign output hash with tenant/KMS key — KMS providers exist under Infrastructure/Kms and could be reused) and an optional perturbation pass, plus … |
| 🔴 | AI-017 — Adversarial input detection (prompt-injection in clinical text, e.g. 'ignore previous instructions') | P0 | CE | M | Entire P0 control absent: need a pre-prompt detector that scans dictation/free-text for injection patterns (override-instruction phrases, role hijacks, fenced system directives) and flags/strips th… |
| 🟡 | AI-018 — Token budget enforcement per request and per user/day | P0 | CE | M | Granularity mismatch with the PRD: enforcement is per TENANT per MONTH, not per-REQUEST token budget nor per-USER-per-DAY. AiRequest rows carry UserId capability but the quota query (GetOkAiUsageAs… |
| 🔴 | AI-019 — Multi-model ensemble for impression generation (majority + diff), opt-in [EE] | P2 | EE | L | Entire EE feature absent: need an opt-in ensemble runner that fans out to N providers, computes majority/consensus impression and a structured diff. SandboxCompare provides a starting point for the… |
| 🟡 | AI-020 — Local PII/PHI scrubbing pass before any cloud egress (Presidio + tenant rules) | P0 | CE | L | Architecture is detect-and-BLOCK, not scrub-then-SEND. There is no de-identification pass that strips/pseudonymizes PHI from the prompt before egress to a cloud provider (PhiRedactor is used only f… |
| 🔴 | AI-021 — 'Quote-it' mode: AI cites the source sentence in findings for each impression bullet | P1 | CE | L | Entire feature absent: need an impression-generation variant that emits, per bullet, the supporting Findings span/offset, an AiResult shape that carries citations, and UI to render bullet→source li… |
| 🔴 | AI-022 — Disagreement detection between two providers when ensemble enabled [EE] | P2 | EE | L | Entire EE feature absent: need, on top of the ensemble runner, a comparison that detects clinically material divergence between two provider outputs and surfaces it. SandboxCompare gives raw output… |
| 🔴 | AI-023 — Multilingual report generation with translation provenance | P1 | CE | L | Entire feature absent for report CONTENT: need a translate mode that records source text, target language, model/provider used, and a provenance audit linking the translated report to its source, d… |
| 🔴 | AI-024 — Personal style memory (encrypted, per-user, exportable, deletable, opt-in) | P1 | CE | XL | Entire feature absent: need a per-user StyleMemory entity (opt-in flag), KMS-encrypted storage (KMS providers exist and can be reused), capture of accepted edits to learn style, injection into prom… |
| 🔴 | AI-025 — Speculative completion of structured fields based on study metadata | P1 | CE | L | Entire feature absent: need a service that, from study metadata (modality, body part, protocol, DICOM context), speculatively prefills technique/structured fields, clearly marked (.ai-mark) and eas… |
| ✅ | AI-026 — 'Explain like I'm the referring physician' mode | P1 | CE | S | Tone is fixed in code per mode rather than tunable to the referrer's specialty; output is not separately persisted/labeled as a distinct artifact, only returned for accept/reject. Minor vs the PRD … |
| 🟡 | AI-027 — Length and tone controls per tenant default | P0 | CE | M | Tone is stored on the rulebook style but is NOT injected into the generation prompt (BuildPromptForMode does not read Style.Tone) — so the tone control is declarative only and not actually applied … |
| 🟡 | AI-028 — Hallucination guardrail: every impression claim must trace to a source span; unsupported claims surfaced before export | P0 | CE | L | It surfaces unsupported claims by token-overlap but does NOT establish a positive source-SPAN trace for each claim (no claim→exact-source-offset mapping; that is AI-021). The guardrail is opt-in pe… |

### PRD v2 §14.11 Subspecialty Modules + §14.12 Structured Reporting & RADS Engine

_RadioPad implements a PARTIAL, text-centric version of these two sections. What exists: (1) a flat library of ~20 approved subspecialty rulebooks in rulebooks/ (chest_ct, chest_xray, lung_screening/lung_lungrads, abdomen_ct/us, liver_lirads/liver_mri, brain_mri, head_ct_trauma, mammo_birads/mammography, prostate_pirads/prostate_mri, thyroid_us, knee/shoulder/pelvis/spine MRI, musculoskeletal_xr, cardiac_mri, paediatric_chest_xray) each with style/rules/prompt_blocks/output_schema; (2) a curated RADS terminology lookup (rulebooks/_terminology/rads.yaml) exposing BI/LI/PI/Lung/TI/C-RADS category codes via RadsService + /api/terminology/rads + a Terminology UI page; (3) regex-based RADS validation rules in ReportValidator (require a category in the impression, require it on its own line, enforce critical-result language for high categories, enforce follow-up language for TI-RADS>=3); (4) a RadLex sublexicon and FHIR CodeSystem export; (5) ValidationPack (golden-case) infrastructure with ~2 cases per rulebook. CRITICAL GAPS vs the PRD: the entire engine is FREE-TEXT, not structured. The Report entity has only narrative sections (Indication/Technique/Comparison/Findings/Impression/Recommendations) with NO structured RADS fields, NO key-image refs, NO RADS category column. Therefore the headline RADS-engine clauses are essentially unbuilt: RADS-001 auto-derivation from structured fields (NONE — RadsService is a passive lookup, validator only checks a category is *mentioned*), RADS-002 forbid contradicting manual category (NONE — no structured field to contradict), RADS-004 Fleischner calculator (no match anywhere), RADS-005 RECIST/iRECIST/mRECIST/Lugano/Cheson lesion tracker (no match anywhere), RADS-006 structured key images (none), RADS-007 bidirectional structured↔narrative sync (none — there is only narrative), RADS-008 RADS analytics/category churn/inter-reader variance (only a generic quality-score dashboard exists), RADS-009 TIRADS/O-RADS scoring forms (no scoring forms; O-RADS not even in terminology), RADS-011 NI-RADS (absent), RADS-012 "Why this category?" justification panel (none). O-RADS, CAD-RADS, NI-RADS are entirely absent from rads.yaml/rulebooks despite RADS-001 naming them. For §14.11, the six per-module deliverables are largely unmet: rulebooks fall far short of ≥5 per module, templates far short of ≥10 (only 5 templates total exist), golden cases far short of ≥50 (≈2 each), and there is NO module-bundle/manifest concept, NO RadElement CDE bindings, NO society-guideline link metadata beyond a single public ACR URL per RADS system. Roughly: §14.12 is ~25% complete (RADS-003/010 partial via terminology+rulebook checks; the structured-engine core is missing); §14.11 is ~30% complete (rulebook breadth exists but as flat text rulebooks, not versioned modules meeting the quantitative bars)._

Coverage: ✅0 · 🟡8 · 🔴14 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | §14.11 SUB-* — Subspecialty module catalogue (15 named modules: Chest, Abdomen, Neuro, MSK, Breast, Cardiac, Pediatric, IR, Nuclear, Emergency, OB/Gyn, General US, Dental, Vascular, Head&Neck) | P1 | GA | XL | No coverage rulebooks at all for several named modules: Nuclear/Molecular (SUB-NM), Dental/Maxillofacial (SUB-DT), Vascular (SUB-VS CTA/MRA/aneurysm), Head & Neck (SUB-HN), Interventional/procedure… |
| 🔴 | §14.11 — Module = versioned BUNDLE of rulebooks+templates+terminology+checks+evidence+golden cases | P1 | MVP | L | Build a Subspecialty Module concept: a versioned manifest that aggregates its rulebooks, templates, terminology bindings, validation checks, evidence links, and golden-case packs, with install/vers… |
| 🟡 | §14.11 — ≥5 subspecialty rulebooks per module | P1 | MVP | XL | Far below the ≥5-per-module bar. Need to author roughly 4+ additional rulebooks per subspecialty module. |
| 🔴 | §14.11 — ≥10 subspecialty templates per module | P1 | MVP | XL | Author ~10 templates for each of the 15 modules (≈150 templates). Essentially unbuilt. |
| 🟡 | §14.11 — Terminology bindings (RadLex sublexicon + RadElement CDEs) | P2 | Beta | M | No RadElement Common Data Element (CDE) bindings anywhere (grep for RadElement/CDE finds only docs/PRD, no code or data). RadLex subset is generic, not partitioned into per-subspecialty sublexicons. |
| 🟡 | §14.11 — Society guideline links (ACR/RSNA/ESR endorsements) | P2 | Beta | M | No structured per-module society-guideline/evidence-link metadata, no ACR/RSNA/ESR endorsement registry, no per-rule evidence citation field. Only top-level ACR URLs for the 6 RADS systems. |
| 🟡 | §14.11 — ≥50 golden cases per module | P1 | MVP | XL | Orders of magnitude short of ≥50 per module; need ~50 synthetic golden cases for each of 15 modules. |
| 🟡 | §14.11 — Subspecialty validation checks (e.g., LI-RADS category-derivation rules) | P1 | Beta | L | Checks are text/regex presence checks (does the impression mention a category?), NOT derivation rules. The PRD's example 'LI-RADS category derivation rules' (compute category from imaging features)… |
| 🔴 | RADS-001 — Auto-derive RADS category from structured fields (BI-RADS 0-6, LI-RADS LR-1..LR-M, PI-RADS 1-5, Lung-RADS 1-4X, TI-RADS 1-5, O-RADS US/MRI, NI-RADS, CAD-RADS) | P1 | CE | XL | No structured field model and no derivation engine. Also O-RADS, NI-RADS, CAD-RADS are entirely absent from rads.yaml (only bi/li/pi/lung/tirads/c_rads defined). Must add structured input fields pe… |
| 🔴 | RADS-002 — Forbid manually-entered category that contradicts structured fields | P1 | CE | L | Depends on RADS-001 (structured fields + derivation). Then add a guard rule that blocks a manual category disagreeing with the derived one. |
| 🟡 | RADS-003 — Embed RADS explanation snippet (society-approved wording) | P1 | CE | M | Snippets are short non-proprietary labels, not full society-approved explanation prose, and are not auto-embedded into the report at the point of category selection; the user must look them up manu… |
| 🔴 | RADS-004 — Calculate Fleischner Society follow-up recommendation for lung nodules | P1 | CE | M | Build a Fleischner calculator (nodule size/type/solid-vs-subsolid + risk → follow-up interval) and surface its recommendation. |
| 🔴 | RADS-005 — RECIST 1.1 / iRECIST / mRECIST / Lugano / Cheson lesion tracker across studies | P2 | EE | XL | Entirely unbuilt: needs a longitudinal lesion-measurement model, per-criteria response computation, and cross-study comparison UI. |
| 🔴 | RADS-006 — Structured key images & key-finding placeholders (text-only MVP; image refs in beta) | P1 | CE | M | Even the text-only MVP form (structured key-finding placeholders) is absent. Add a structured key-finding list to the report model and templates. |
| 🔴 | RADS-007 — Bidirectional sync of structured fields ↔ narrative text | P1 | CE | XL | Requires the structured-field model first, then two-way binding so editing a field updates the narrative and vice-versa. |
| 🔴 | RADS-008 — RADS analytics: distribution per radiologist, category churn draft→final, inter-reader variance | P2 | EE | L | Build RADS-specific analytics: requires persisted RADS categories per report version (currently none) plus distribution/churn/variance computations and views. |
| 🔴 | RADS-009 — TIRADS, O-RADS-US/MRI scoring forms | P1 | CE | L | Build interactive scoring forms (ACR TI-RADS feature points → TR category; O-RADS US/MRI inputs → category) and add O-RADS data. |
| 🟡 | RADS-010 — C-RADS scoring for CT colonography | P2 | CE | M | Only the category lookup exists. No C-RADS scoring form, no CT-colonography rulebook/template, no derivation of the C/E category from structured polyp/extracolonic inputs. |
| 🔴 | RADS-011 — NI-RADS for treated head & neck cancer | P2 | EE | M | Add NI-RADS category data, a treated-H&N rulebook/template, and scoring; fully absent. |
| 🔴 | RADS-012 — 'Why this category?' justification panel | P1 | CE | M | Depends on RADS-001/RADS-002; build a panel showing which structured features drove the derived category with society references. |
| 🔴 | Cross-cutting — Structured report data model (prerequisite for RADS-001/002/006/007/008/012) | P1 | CE | XL | Introduce a structured findings/RADS field model on Report (or a related entity) so categories, key findings, and measurements are first-class data — the foundation the entire §14.12 engine is buil… |
| 🔴 | Cross-cutting — DICOM SR export of structured/RADS content | P2 | EE | L | If DICOM SR is required for structured-reporting interop, it is unbuilt (note: image module is out of scope per PRD MVP, so this is lower priority). |

### PRD v2 §14.13 Peer Review & Quality, §14.14 Teaching File & Education, §14.15 Critical Results Workflow

_Across all 28 requirements in these three modules, only the Critical Results "detector" function has any real code: a rule-based regex detector (CheckCriticalLanguage in backend/RadioPad.Api/src/RadioPad.Validation/Engine/ReportValidator.cs) wired to `critical_result_language` / `critical_finding_language` rules in 12 rulebooks. That detector flags a critical finding that lacks communication/escalation phrasing, giving PARTIAL coverage of CR-001 (rule-only, no AI ensemble) and CR-005 (flags-but-does-not-insert). Everything else in §14.15 (criticality classes/SLAs, communication-tracking record, auto-page/SMS, audit-grade critical-result events, WebAuthn ack, read-back script, escalation, Joint-Commission reports) is MISSING. The entire Peer Review module (§14.13, PR-001..010) and the entire Teaching File module (§14.14, TF-001..008) are MISSING: there are no domain entities (Enums.cs/Entities.cs contain none), no RBAC permissions (PermissionCatalog.cs), no AuditAction kinds, no controllers, and no frontend pages for any of them. Two adjacent-but-distinct assets exist and were verified NOT to satisfy the requirements: frontend/app/analytics/quality/page.tsx is a validation-quality-score dashboard (heuristic QualityScore trends), not a RADPEER concordance/discrepancy dashboard; and PhiRedactor (backend .../Api/Logging/PhiRedactingLoggerProvider.cs) scrubs MRN/DOB/SSN/name but only in log output, not as teaching-file de-identification. WebAuthn (WebAuthnController.cs) is auth/sign only and is not bound to any critical-result acknowledgment. PROGRESS.md and docs/09-regulatory/traceability-matrix.md contain no entries for these modules. Note: per CLAUDE.md the locked stack (ASP.NET/Next) governs, so absence of the PRD's OSS libraries is not itself a gap — but here the FUNCTIONS themselves are absent, not merely a different library._

Coverage: ✅0 · 🟡3 · 🔴25 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🔴 | PR-001 — Random and rule-based selection of cases for peer review | P1 | CE | L | Build PeerReviewCase entity + random sampler (e.g. configurable %) and rule-based selectors (by modality/RADS/STAT), a background job to enqueue cases, and storage of selection provenance. |
| 🔴 | PR-002 — Anonymized assignment to peer reviewer | P1 | CE | L | Add reviewer assignment (round-robin/load-aware), strip author/patient identifiers from the review view, and enforce reviewer != author. |
| 🔴 | PR-003 — Reviewer scoring (concordance / minor / major discrepancy) with structured rationale | P1 | CE | M | Add RADPEER-style score enum + structured rationale entity, scoring API, and a reviewer UI inside AppShell. |
| 🔴 | PR-004 — Learning loop: discrepancies inform rulebook improvement suggestions | P2 | EE | L | Aggregate discrepancy patterns and surface rulebook-edit suggestions; depends on PR-003 data first. |
| 🔴 | PR-005 — Quality dashboard for medical director | P1 | EE | M | Build a director dashboard over peer-review outcomes (concordance rate, major/minor discrepancy rates by reader/subspecialty). Could extend the existing quality page shell but needs new data. |
| 🔴 | PR-006 — Second-read workflow for STAT/high-risk findings | P2 | EE | L | Detect STAT/high-risk (could reuse critical-language detector) and force a second reviewer before finalization, with state tracking. |
| 🔴 | PR-007 — Mobile review approvals for time-sensitive cases | P1 | CE | M | Add a mobile peer-review/approval surface bound to PR-002/PR-003 backend; depends on those existing first. |
| 🔴 | PR-008 — Educational mode for residents (attending feedback, structured comments, scoring of resident drafts) | P1 | CE | L | Add resident/attending relationship, structured attending feedback + scoring on resident drafts, and an educational review UI. |
| 🔴 | PR-009 — Discrepancy analytics by subspecialty, modality, reader, and rulebook | P2 | EE | M | Capture discrepancy events (PR-003) and add analytics slices by subspecialty/modality/reader/rulebook. |
| 🔴 | PR-010 — Export-only mode for external peer-review services | P2 | EE | M | Add an anonymized case-bundle export targeted at external peer-review vendors. |
| 🔴 | TF-001 — One-click 'Add to teaching file' with mandatory de-identification | P1 | CE | L | Add TeachingFileCase entity + 'Add to teaching file' action that runs mandatory de-identification before persisting. |
| 🟡 | TF-002 — Auto-anonymization of report text, dates, and study identifiers | P1 | CE | M | Repurpose/extend PhiRedactor into a teaching-file anonymizer covering accession/study UID/dates and confirm completeness; wire it into the TF-001 add action. Primitive exists, feature does not. |
| 🔴 | TF-003 — DICOM image attachment with pixel-level de-identification | P2 | EE | XL | Add image attachment + pixel/header de-id; gated on image module. Largest sub-item. |
| 🔴 | TF-004 — Case categorization by modality, diagnosis, RADS, teaching pearls | P1 | CE | M | Add categorization fields (modality/diagnosis/RADS/pearls) + tagging UI on teaching cases. |
| 🔴 | TF-005 — Quiz / question generator from teaching file metadata | P2 | CE | L | Generate quiz questions from teaching-file metadata (likely via AiGateway); depends on TF-004 metadata. |
| 🔴 | TF-006 — Export bundle compatible with MIRC-style teaching file formats | P2 | CE | L | Implement MIRC-compatible export bundle (manifest + de-identified content). |
| 🔴 | TF-007 — Per-tenant private teaching library; opt-in public sharing with attribution | P1 | CE | M | Add per-tenant teaching library with opt-in public sharing + attribution metadata. |
| 🔴 | TF-008 — Resident learning analytics (cases viewed, quiz performance) | P2 | EE | M | Track case views + quiz scores per resident and surface analytics; depends on TF-005/PR-008. |
| 🟡 | CR-001 — Critical-result language detector (rule + AI ensemble) | P0 | CE | M | Add the AI half of the ensemble (model-based critical-finding classification) and combine with the rule output for higher recall; today it is rule-only. |
| 🔴 | CR-002 — Tenant-defined criticality classes (red/orange/yellow) with required communication SLAs | P0 | CE | L | Add tenant-configurable criticality classes + per-class communication SLA, and map detector hits to a class. |
| 🔴 | CR-003 — Communication workflow: who/when/by what means + acknowledgement timestamp | P0 | CE | L | Add a CriticalResultCommunication record (notifier, recipient, channel, timestamp, ack timestamp) with create/ack endpoints and UI. |
| 🔴 | CR-004 — Auto-page / SMS / app notification to ordering physician (via integration) | P1 | EE | L | Integrate paging/SMS/app push to the ordering physician triggered by a confirmed critical result; reuse push plumbing but new recipient model + channels. |
| 🟡 | CR-005 — Mandatory text block insertion for confirmed critical findings | P0 | CE | M | It only flags absence; it does not *insert* a mandatory structured communication/text block, nor enforce a specific tenant-defined block. Add auto-insertion + required structured block (ties to CR-… |
| 🔴 | CR-006 — Audit-grade record of every critical-result event | P0 | CE | M | Define critical-result AuditAction kinds and append append-only records for detection/communication/ack/escalation events. |
| 🔴 | CR-007 — Mobile companion acknowledgement with WebAuthn | P1 | CE | M | Add a mobile critical-result ack screen requiring a WebAuthn assertion, writing the ack timestamp to the CR-003 record. WebAuthn primitive reusable; feature absent. |
| 🔴 | CR-008 — Configurable callback / read-back script | P1 | EE | S | Add tenant-configurable read-back script surfaced during the communication step. |
| 🔴 | CR-009 — Escalation policy if no acknowledgement within N minutes | P1 | EE | L | Add a background escalation worker that re-notifies/escalates when ack is overdue per the CR-002 SLA. |
| 🔴 | CR-010 — Compliance reports for The Joint Commission / external accreditation | P1 | EE | M | Build a compliance report over CR-006 events (timeliness, ack rates, escalations) suitable for accreditation; depends on the event record existing. |

### PRD v2 §14.16–14.18 — Worklist & Workflow Optimization, Comparison & Longitudinal Tracking, Evidence & Citation Module

_Of the 23 discrete requirements across the three sections, this audit finds 0 fully implemented, 3 partial, and 20 missing. §14.16 Worklist (WL-001..010): there is NO worklist application feature, view, route, or nav entry anywhere in the locked stack. The only "worklist" code is FetchWorklistAsync inside three PACS vendor adapters (Sectra/Visage/Carestream, backend\RadioPad.Api\src\RadioPad.Infrastructure\Pacs\*Adapter.cs) — a vendor pull primitive with no UI, no UPS-RS/ORM-to-worklist ingestion, no filtering, smart-assign, fatigue, RVU, hanging-protocol hooks, pause/resume reads, multi-tenant switcher UI, or idle warnings. §14.17 Comparison/Longitudinal: a genuine prior-report auto-link + section diff DOES exist (ReportsController.ComparePrior/Prior at backend\...\Controllers\ReportsController.cs:341 & :647; frontend\app\reports\[id]\PriorComparePanel.tsx; frontend\lib\textDiff.ts LCS diff), satisfying COMP-001 and COMP-002 partially, but there is NO RECIST/iRECIST/Lugano/mRECIST/PERCIST/Cheson tracker, NO cross-study lesion table, NO trend chart, NO stable/increased/decreased auto-suggest language, and NO cross-modality correlation. §14.18 Evidence & Citation: entirely absent — no PubMed lookup, ACR Appropriateness, society-guideline library, citation insertion, per-tenant evidence rules, or provenance trail (the RadsService at backend\...\Services\RadsService.cs is a RADS scoring-code lookup, NOT a citable guideline/evidence library). PROGRESS.md contains no claims for any of these section IDs, consistent with the code. CLAUDE.md stack-lock was honored in judgment: no requirement was downgraded merely for missing the PRD's named OSS libraries — the gaps are genuine functional absences on the locked stack. Existing measurement extraction (MeasurementExtractionService.cs) is regex finding/size capture only and is NOT wired into any longitudinal lesion tracker._

Coverage: ✅0 · 🟡5 · 🔴18 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🔴 | WL-001 — Read-only worklist view ingesting DICOMweb UPS-RS or HL7 ORM | P1 | CE | XL | Build a worklist domain model + ingestion from DICOMweb UPS-RS and/or HL7 ORM, a WorklistController, and a read-only worklist page rendered in AppShell with a nav entry. The vendor-adapter pull cou… |
| 🔴 | WL-002 — Filtering by modality, body part, priority, age, prior status, subspecialty | P1 | CE | L | Add a filterable worklist list (server query + UI filter controls) covering modality, body part, priority, age, prior status, and subspecialty. Depends on WL-001. |
| 🔴 | WL-003 — 'Smart assign' suggestion (subspecialty match, fatigue, fairness), informational only | P2 | EE | L | Implement an assignment-suggestion service (subspecialty match + fatigue + fairness heuristics) returning informational suggestions, plus UI. Depends on WL-001/WL-002 and WL-004. |
| 🔴 | WL-004 — Reader load and fatigue heuristics (hours read, breaks suggested) | P2 | EE | L | Track per-reader read time/volume and compute fatigue/break heuristics; expose via API + UI. New feature. |
| 🔴 | WL-005 — RVU and procedure-mix analytics | P2 | EE | L | Add RVU mapping per procedure code and procedure-mix aggregation to analytics; new data model + dashboard panel. |
| 🟡 | WL-006 — Prior-study auto-linker (HL7 + DICOM patient ID + accession-based) | P1 | CE | M | Add patient-ID (MRN/DICOM PatientID) and HL7-based prior linking in addition to accession; the controller comment itself notes MRN matching is a TODO ('swap this for report.Study.PatientMrn when po… |
| 🔴 | WL-007 — Hanging-protocol hooks for major viewers (OHIF, Weasis, Horos, Radiant, PACS APIs) | P2 | EE | L | Implement deep-link/launch hooks (URL schemes or PACS APIs) to open studies in OHIF/Weasis/Horos/Radiant with a hanging protocol. New integration layer. |
| 🟡 | WL-008 — Pause/resume reads with state preserved across sessions and devices | P1 | CE | M | Introduce an explicit per-reader 'read in progress / paused / resumed' state on worklist items, persisted server-side and resumable cross-device. Depends on WL-001. |
| 🟡 | WL-009 — Multi-tenant worklist switcher for teleradiologists | P1 | CE | M | Add a tenant-membership list endpoint + a switcher control in the shell that re-scopes the worklist; depends on WL-001 for the worklist itself. |
| 🔴 | WL-010 — Configurable timeouts and idle-state warnings | P1 | CE | M | Add a configurable idle timeout setting and a client idle-warning/auto-pause behavior for active reads. New feature. |
| 🟡 | COMP-001 — Auto-link prior reports for the same patient and similar modality | P1 | CE | S | Match priors by stable patient identity (MRN/PatientID) and add a similar-modality constraint/ranking. Moderate change to the two existing queries. |
| 🟡 | COMP-002 — Semantic diff of prior vs current findings | P1 | CE | L | Add semantic comparison (finding/concept-level: new/resolved/changed findings, measurement deltas) beyond line/section text diff. Could reuse MeasurementExtractionService for measurement deltas. Me… |
| 🔴 | COMP-003 — RECIST 1.1 / iRECIST / Lugano / mRECIST / PERCIST / Cheson tracker | P2 | EE | XL | Build the full longitudinal response-criteria engine (target/non-target lesion selection, sum-of-diameters, response classification per criterion family) — the single largest gap in §14.17. |
| 🔴 | COMP-004 — Lesion table across studies (date, size, location, response classification) | P2 | EE | XL | Add a persistent Lesion entity keyed to patient, captured per study (date/size/location), aggregated into a multi-study table with response classification. Depends on COMP-003 for classification. |
| 🔴 | COMP-005 — Auto-suggest 'stable / increased / decreased' language with thresholds | P1 | CE | M | Compare current vs prior measurements (MeasurementExtractionService is available) against configurable thresholds and emit suggested stable/increased/decreased phrasing (with .ai-mark per design lo… |
| 🔴 | COMP-006 — Visualization: lesion trend chart per patient | P2 | EE | M | Add a per-patient lesion size-over-time chart. Depends on COMP-004 lesion data existing first. |
| 🔴 | COMP-007 — Cross-modality comparison (e.g., CT lesion correlated to MR finding) | P3 | EE | XL | Implement lesion/finding correlation across modalities (spatial/anatomic matching). Depends on COMP-003/COMP-004. Lowest priority (P3). |
| 🔴 | EV-001 — One-click PubMed lookup with abstract preview | P1 | CE | M | Add a PubMed (E-utilities) lookup service + UI panel with abstract preview, respecting AiGateway/provider egress policy if proxied. New feature. |
| 🔴 | EV-002 — ACR Appropriateness Criteria search (where licensed) | P1 | EE | L | Integrate or ingest ACR Appropriateness Criteria with a search UI, gated by licensing/feature flag. New feature. |
| 🔴 | EV-003 — Society guideline library (Fleischner, RSNA, ACR, ESR, ESUR, JBR, etc.) | P1 | CE | L | Create a guideline library (content + metadata for Fleischner/RSNA/ACR/ESR/ESUR/JBR) with browse/search UI and link-out. New feature/data model. |
| 🔴 | EV-004 — Citation insertion as footnote-style references in report | P2 | CE | M | Add a citation/reference data model on the report and an insert-as-footnote UI that emits numbered references. Depends on EV-001/EV-003 to source citations. |
| 🔴 | EV-005 — Per-tenant evidence library and rules ('only cite within these journals') | P2 | EE | M | Add per-tenant evidence-source allow-lists and library scoping enforced at lookup time. Depends on EV-001/EV-003. |
| 🔴 | EV-006 — Provenance trail: which evidence informed which suggestion | P2 | EE | M | Record, per AI suggestion, the evidence items that informed it, and expose the trail (likely via the existing audit log + a new linkage table). Depends on EV-001/EV-003/EV-004. |

### PRD v2 §14.4 — Rulebooks and Prompt Engineering System (RB-001 … RB-023)

_RadioPad has a genuinely substantial rulebook subsystem on the locked .NET 8 / Next.js stack: a YAML rulebook model (RadioPad.Validation/Rulebook/RulebookSpec.cs), a rich deterministic validation engine (ReportValidator.cs, ~35 rule resolvers incl. RADS families), full CRUD/approve/deprecate/rollback API (RulebooksController.cs), a visual + YAML frontend editor (frontend/app/rulebooks/...), a CLI validate/test/approve toolchain, versioned validation packs (ValidationPackService.cs + packs CLI), a production gate that blocks non-Approved rulebooks (ReportingService.ResolveRulebookEntityAsync + RulebookGovernanceException, RB-010), CI that validates every YAML and runs every golden-case suite (.github/workflows/ci.yml), and a marketplace publish/install pipeline (MarketplaceController.cs). Of the 23 requirements, roughly 8 are implemented, 6 partial, and 9 missing. The biggest concrete gaps: RB-009 (the AI audit event in AiGateway.cs does NOT record rulebook id/version — only provider/model/hashes), no rulebook `extends:` inheritance/composition (RB-016; RB-007 is satisfied only by department-tag row selection, not YAML inheritance), no .rpkg signed bundle import/export (RB-019), no per-rulebook PHI policy override (RB-020), no Ed25519 signing/daemon verification of rulebooks specifically (RB-012 — Ed25519 infra exists but is wired to PACS/MCP plugins, not rulebooks), no semantic diff + reviewer comments (RB-013), no rulebook telemetry (RB-017), no deprecation grace period (RB-021), and no cross-tenant masked share (RB-022). Several anatomy fields from §14.4.1 (extends, clinical_owners, license, structured_fields, per-rule implementation, evidence_links, embedded test_cases, signed_by, applies_to.procedures/age_ranges) are not modeled in RulebookSpec. NOTE: a doc (docs/05-clinical/rulebook-authoring.md) references RulebookSpec.WithPromptOverrides which does not exist in code (doc drift)._

Coverage: ✅5 · 🟡9 · 🔴9 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | RB-001 — Create, edit, clone, archive, version rulebooks (P0/CE) | – | – | S | Add a Clone action (server-side copy of SourceYaml into a new draft row with bumped version, and a UI button on the list/detail pages). Decide whether Archive is distinct from Deprecated; if so add… |
| ✅ | RB-002 — YAML/JSON source editing AND visual editing (P1/CE) | – | – | S | JSON (vs YAML) source editing is not separately offered, but the PRD intent (source + visual) is met. Visual editor edits a structured subset; some §14.4.1 fields (structured_fields, evidence_links… |
| 🟡 | RB-003 — Approval workflow Draft→Review→Approved→Deprecated with electronic signatures (P0/CE) | – | – | M | No transition INTO the InReview state (no submit-for-review endpoint for rulebooks; the enum value is defined but unused for rulebooks). No electronic signature captured on approval (no signer iden… |
| 🟡 | RB-004 — Test cases (golden cases) per rulebook with diff viewer (P0/CE) | – | – | M | No visual diff viewer comparing expected vs actual output text per case (only set-difference of fired rule ids is shown). Golden cases are stored separately (disk + ValidationPack rows), not embedd… |
| ✅ | RB-005 — Prompt blocks, output schemas, style rules, forbidden language, required sections, validation rules (P0/CE) | – | – | M | output_schema is parsed but no evidence it is enforced against AI output (no JSON-schema validation step found). Per-rule 'implementation: deterministic\|hybrid\|ai_assisted' from §14.4.1 is not pa… |
| ✅ | RB-006 — Modality- and subspecialty-specific rulebooks (P0/CE) | – | – | S | applies_to.procedures and applies_to.age_ranges from §14.4.1 are not modeled in AppliesToSpec (only modalities/body_parts/report_types). Selection is by modality+body_part+department, not procedure… |
| 🟡 | RB-007 — Tenant/department/user inheritance with explicit override semantics (P1/CE) | – | – | M | Two of three inheritance levels: tenant + department covered; per-USER inheritance/override is not implemented. Override semantics are limited to row-selection + prompt-block overrides; there is no… |
| ✅ | RB-008 — Rollback to prior approved version (single-command) (P0/CE) | – | – | S | None material. (Rolled-back version string carries a +rollback suffix rather than restoring the exact prior semver, which is a deliberate non-mutating design choice.) |
| 🔴 | RB-009 — Rulebook version captured in every AI event audit log (P0/CE) | – | – | M | Thread the resolved rulebook RulebookId + Version (and ideally the prompt-block/prompt version) into AiCompletionRequest and include them in every AiResponse/ProviderBlocked/PolicyViolation audit D… |
| ✅ | RB-010 — Block unapproved rulebooks from production; sandbox mode explicit (P0/CE) | – | – | S | None material for rulebooks. (Production gate is per-AI-run; there is no separate guard preventing a draft rulebook from being attached to a report, but the AI path — the production usage — is gated.) |
| 🟡 | RB-011 — Rulebook regression CI; promotion gated on golden-case pass rate ≥ threshold (P0/CE) | – | – | M | The gate is an implicit 100%-pass requirement, not a configurable threshold (e.g. ≥95%). There is no explicit 'promotion' pipeline that ties an approve/promote action to a pass-rate check — approva… |
| 🔴 | RB-012 — Cryptographic signing of approved rulebooks (Ed25519); daemon verifies before activation (P1/EE) | – | – | L | Add an Ed25519 signature field + signer metadata to the Rulebook entity, sign on approval, and verify the signature in ReportingService.ResolveRulebookEntityAsync / daemon activation before a ruleb… |
| 🔴 | RB-013 — Rulebook diffing (semantic) with reviewer comments (P1/CE) | – | – | L | Implement structured diff between two rulebook versions (sections/rules/prompt-blocks/style added/removed/changed) and a reviewer-comment thread persisted per version/field. Net-new feature. |
| 🟡 | RB-014 — Rulebook A/B testing in sandbox tenant (no PHI) (P2/EE) | – | – | M | This is provider A/B, not RULEBOOK A/B — there is no path to run two different rulebook versions against the same input and compare validation/draft outputs, and no dedicated no-PHI sandbox tenant … |
| 🟡 | RB-015 — Rulebook marketplace (publish/install/rate); curated endorsement badges (P2/CE) | – | – | M | No rating/review system (no rating entity/endpoint; only InstallCount). No curated 'clinical-society endorsement' badge concept on listings. Add ratings + a curation/endorsement badge field and sur… |
| 🔴 | RB-016 — Bilateral rulebook composition (compose by extends + override) (P1/CE) | – | – | L | Add an 'extends: <id>@<version>' field to RulebookSpec, resolve the parent at load time, and deep-merge style/required_sections/rules/prompt_blocks/output_schema with explicit child-override semant… |
| 🔴 | RB-017 — Rulebook telemetry: which rules fire how often, which are most overridden (P1/CE) | – | – | L | Persist per-rule fire counts and per-rule override/dismiss events (radiologist acknowledging/ignoring a finding), then expose an analytics endpoint + governance dashboard panel. Net-new telemetry p… |
| 🟡 | RB-018 — Rulebook editor with inline lint (YAML schema, prompt token estimation, breaking-change warnings) (P1/CE) | – | – | M | No prompt token estimation in the editor (grep token/estimat in PromptBlocksPanel.tsx → none). No breaking-change warnings (e.g., removing a required section or changing a rule severity vs the prio… |
| 🔴 | RB-019 — Rulebook import/export as a single signed .rpkg bundle (zip + signed manifest) (P0/CE) | – | – | L | Implement .rpkg create/import: zip the rulebook YAML + schemas + golden cases + a manifest, sign the manifest (reuse Ed25519/PluginVerifier), and add import that verifies the signature before insta… |
| 🔴 | RB-020 — Per-rulebook PHI policy override (e.g., this rulebook may only use local models) (P0/CE) | – | – | M | Add a per-rulebook PHI/compliance policy (e.g., RequiredComplianceClass = LocalOnly) to the entity + RulebookSpec, and enforce it in AiGateway/provider routing so a rulebook can pin itself to local… |
| 🔴 | RB-021 — Rulebook deprecation grace period (default 90 days) with usage warnings (P1/CE) | – | – | M | Add DeprecatedAt + GraceDays (default 90) to the Rulebook entity, allow continued use during grace while emitting a warning finding/audit, and hard-block after the window. Net-new lifecycle behavior. |
| 🔴 | RB-022 — Cross-tenant share with selective masking (org-internal sharing) (P1/EE) | – | – | L | Add an org/tenant-group sharing grant for rulebooks with field-level masking (e.g., hide owner/clinical_owners or proprietary prompt text) for recipient tenants. Net-new EE feature; must preserve t… |
| 🟡 | RB-023 — Conformance test runner ensures rulebooks comply with RadioPad rulebook spec (P0/CE) | – | – | M | Validation is shallow: it checks a handful of required top-level fields but not full spec conformance (no JSON-schema for the rulebook itself, no validation of rule ids against the engine's known r… |

### PRD v2 §14.5/§14.6

_see items_

Coverage: ✅3 · 🟡14 · 🔴18 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | TMP-001 | – | – | M | 5 not 80+ |
| 🟡 | TMP-002 | – | – | L | no cond |
| 🟡 | TMP-003 | – | – | S | post-proc? |
| 🔴 | TMP-004 | – | – | L | no bind |
| ✅ | TMP-005 | – | – | S | - |
| ✅ | TMP-006 | – | – | S | - |
| 🟡 | TMP-007 | – | – | L | no rpkg |
| 🟡 | TMP-008 | – | – | S | no sample |
| 🔴 | TMP-009 | – | – | L | no ver |
| 🟡 | TMP-010 | – | – | M | no packs |
| 🔴 | TMP-011 | – | – | M | - |
| 🔴 | TMP-012 | – | – | L | - |
| 🔴 | TMP-013 | – | – | L | locale |
| 🔴 | TMP-014 | – | – | M | rb |
| 🟡 | TMP-015 | – | – | M | not direct |
| 🔴 | TMP-016 | – | – | M | - |
| 🔴 | TMP-017 | – | – | M | - |
| 🟡 | STD-001 | – | – | M | lookup |
| 🟡 | STD-002 | – | – | M | read-only |
| 🟡 | STD-003 | – | – | L | no R5 |
| 🟡 | STD-004 | – | – | M | no UPS-RS |
| 🟡 | STD-005 | – | – | L | no SR exp |
| 🔴 | STD-006 | – | – | M | LOINC |
| 🔴 | STD-007 | – | – | L | SNOMED |
| 🔴 | STD-008 | – | – | L | ICD |
| ✅ | STD-009 | – | – | S | - |
| 🟡 | STD-010 | – | – | M | autotext |
| 🔴 | STD-011 | – | – | XL | IHE |
| 🔴 | STD-012 | – | – | XL | AIW |
| 🔴 | STD-013 | – | – | L | CDE |
| 🟡 | STD-014 | – | – | M | no send |
| 🔴 | STD-015 | – | – | L | bulk |
| 🔴 | STD-016 | – | – | XL | openEHR |
| 🔴 | STD-017 | – | – | L | fed |
| 🔴 | STD-018 | – | – | M | clog |

### PRD v2 §20 UX Requirements + §21 Billing & Subscription + §22 Analytics & KPIs

_RadioPad implements a strong, locked-stack slice of these three PRD sections. §21 Billing is the most complete area: a full Stripe surface exists (checkout with 14-day trial, customer portal, signed/deduped webhook, invoices list, refund with tenant-ownership check, AI-credit balance, plan-feature flags, plan quota enforcement with grace→suspend lifecycle, and BILL-003 bulk invoice export). §22 analytics computes nearly all product+governance KPIs from real data through AnalyticsService + AnalyticsController, with frontend dashboards at /analytics, /analytics/quality, /admin/usage and /admin/billing, plus a synthetic availability monitor for §22.3. §20 UX is partially realized: the canonical left-sidebar shell carries most §20.1 modules, the reporting workspace has Study Context/Editor/AI Actions/Validation/Prior/Export panels, the Tauri desktop registers global hotkeys + secure-clipboard, the Prompt Studio (§20.4) is largely complete, and Capacitor biometric+push helpers exist. The biggest gaps are: several §20.1 modules have NO page (Worklist, Peer Review, Teaching Files, Users & Roles, Integrations, Plugins, general Settings), no command palette (§20.3.1), no PACS mini-overlay, no Provenance Inspector / Notes panel, no TipTap (plain textareas), no live token counter or "Explain this prompt" mode, no mobile bottom-nav / critical-result free-text-+-biometric flow / DND push, and accessibility §20.6 is thin (6 LTR-only locales, NO RTL for AR/HE/UR, no high-contrast theme, no dyslexia font, no WCAG CI gate, no AI-streaming aria-live announcements). Several KPIs are proxies (edit distance hardcoded to 0; vTTQR = created→updated delta), and BILL alternates (Lago/Killbill), multi-currency PO invoicing, auto-recharge, edu/non-profit tier, and marketplace revenue share are absent. Verdict per CLAUDE.md stack-substitution rule: function-presence judged on the locked stack (Stripe counts for BILL-013's "open billing engine" intent only partially since no alternate engine exists; biometric/push count even though native, etc.)._

Coverage: ✅21 · 🟡31 · 🔴23 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | §20.1 Web app navigation — primary modules present in shell | P1 | All | L | No dedicated nav entries/pages for: Worklist, Peer Review, Teaching Files, Users & Roles, Integrations (consolidated), Plugins (separate from Marketplace), and a general Settings page (only /admin/… |
| ✅ | §20.2 Reporting Workspace — Study Context panel | P2 | All | S | No prior-report summary line or RVU class field in the Study Context panel as the PRD table specifies. |
| 🟡 | §20.2 Reporting Workspace — Editor (TipTap, section-aware, structured fields) | P2 | All | L | Editor is plain <textarea>, not TipTap/rich structured editor. No rich-text, no inline source-span highlighting inside the editor body, no structured field widgets. |
| 🟡 | §20.2 Reporting Workspace — AI Actions panel (generate/rewrite/summarize/impression/validate/compare/evidence) | P2 | All | M | No explicit 'summarize' or 'evidence' action surfaced; 'compare' is a prior-compare toggle not a general AI compare. No grouped AI Actions side panel — actions are inline toolbar buttons. |
| 🔴 | §20.2 Reporting Workspace — Rulebook Panel (active rules, required fields, 'why this suggestion?') | P2 | All | M | Build a Rulebook side panel showing active rules, required fields, and per-suggestion rationale. |
| 🟡 | §20.2 Reporting Workspace — Prior Report Panel (findings, semantic diff, lesion tracker) | P2 | Pro+ | L | Diff is per-section changed/unchanged flag, not true semantic diff; no lesion tracker (longitudinal measurement tracking). |
| ✅ | §20.2 Reporting Workspace — Validation Panel (contradictions, missing sections, style, unsupported claims, source-span) | P3 | All | S | Source-span is sentence-level quote, not token-level highlight inside the editor. |
| 🟡 | §20.2 Reporting Workspace — Export Panel (Copy, PDF, DOCX, FHIR, HL7 v2, RIS) | P2 | All | M | No HL7 v2 export button in the workspace export panel (PRD lists HL7 v2). Exports are toolbar buttons, not a dedicated Export panel. |
| 🟡 | §20.2 Reporting Workspace — Worklist (filterable queue with priority) | P2 | All | M | No priority column/sort, no dedicated Worklist module; it is the generic Reports list, not a priority-ordered worklist queue. |
| 🔴 | §20.2 Reporting Workspace — Notes / Comments collaboration sidebar | P3 | Pro+ | M | No collaboration sidebar for notes/comments on a report. |
| 🔴 | §20.2 Reporting Workspace — Provenance Inspector (token-level provenance graph, model, prompt, rulebook) | P2 | Pro+ | L | No Provenance Inspector UI; token-level provenance graph not surfaced anywhere. |
| 🔴 | §20.3.1 Global hotkey opens command palette in ≤200 ms | P2 | All | L | No command palette component; hotkeys map to fixed actions, not a searchable palette. No ≤200ms perf assertion. |
| 🔴 | §20.3.2 Mini overlay floats above PACS without stealing focus | P2 | Pro+ | XL | No floating mini-overlay window above PACS. |
| 🟡 | §20.3.3 Dictation starts within ≤300 ms of hotkey | P2 | All | M | Hotkey routes to a dictation page (navigation), not an in-place ≤300ms dictation start in the desktop workspace; no latency budget/measurement. |
| 🟡 | §20.3.4 AI actions accessible by keyboard alone | P2 | All | M | In the web app (no desktop shell) there is no keyboard shortcut layer for AI actions; full keyboard reachability/logical order not verified or CI-gated. |
| 🟡 | §20.3.5 Secure paste into PACS with timeout indicator | P3 | All | S | Clipboard TTL exists but no visible countdown/timeout indicator in the UI for the secure paste. |
| 🟡 | §20.3.6 Offline mode clearly indicated (badge + tooltip + tray icon) | P2 | All | M | No tray icon indicator; badge+tooltip+tray triad not fully realized (no system tray entry in main.rs). |
| 🟡 | §20.3.7 Provider/compliance mode visible (e.g. 'Local-only' badge) but not intrusive | P3 | All | S | No explicit non-intrusive 'Local-only'/compliance-mode badge shown in the workspace chrome tied to the active provider. |
| 🟡 | §20.3.8 Recovery from sleep/suspend resumes last draft state | P3 | All | M | No explicit sleep/suspend resume handler restoring the exact last in-memory draft/cursor state; relies on autosave + reload. |
| 🟡 | §20.4 Prompt Studio — visual prompt-block editor with live token counter | P2 | Pro+ | S | No live token counter on prompt blocks. |
| ✅ | §20.4 Prompt Studio — rulebook preview + sample outputs against golden cases | P3 | Pro+ | S | None material; golden-case run is present. |
| ✅ | §20.4 Prompt Studio — test-case runner with pass/fail and diff | P3 | Pro+ | S | Test runner validates against the first existing report as context rather than isolated test fixtures. |
| 🟡 | §20.4 Prompt Studio — output diff viewer (semantic + literal) | P3 | Pro+ | M | Diff is literal line-diff only; no semantic diff mode. |
| 🟡 | §20.4 Prompt Studio — golden-case library editor | P3 | Pro+ | M | No in-UI editor to create/edit golden cases (run-only). |
| 🟡 | §20.4 Prompt Studio — approval workflow with electronic signatures | P2 | Pro+ | M | Approval records approver+timestamp but no cryptographic electronic-signature attestation on the prompt approval. |
| 🟡 | §20.4 Prompt Studio — prompt/rule version comparison and rollback | P3 | Pro+ | M | No rollback action to restore a prior version from the Prompt Studio UI. |
| 🔴 | §20.4 Prompt Studio — 'Explain this prompt' plain-language mode | P3 | Pro+ | M | Add an 'Explain this prompt' mode that renders a plain-language description of the prompt text. |
| 🔴 | §20.5 Mobile — one-handed reachable bottom navigation | P2 | All | M | No bottom navigation bar for mobile companion. |
| 🟡 | §20.5 Mobile — push notifications respect DND except tenant-defined critical classes | P1 | All | L | No DND-aware delivery logic or tenant-defined critical-class bypass; backend critical-result push channel with DND exceptions not implemented. |
| 🟡 | §20.5 Mobile — biometric unlock; lock screen shows only non-PHI badge | P2 | All | M | Biometric app-unlock is implemented, but no enforcement that the lock screen / background snapshot shows only a non-PHI badge (no privacy-screen overlay). |
| 🔴 | §20.5 Mobile — critical-result acknowledgement with mandatory free-text + biometric | P1 | All | L | Implement a critical-result acknowledgement requiring biometric AND a mandatory free-text confirmation. |
| 🔴 | §20.6 Accessibility — WCAG 2.2 AA conformance is CI-gated | P1 | All | L | Add automated WCAG 2.2 AA checks in CI (e.g. axe-core/pa11y) and fix the audit findings. |
| ✅ | §20.6 Accessibility — color never the sole carrier of meaning | P3 | All | S | Not formally verified across every surface, but the pattern is consistently followed. |
| 🟡 | §20.6 Accessibility — keyboard reachability in logical order + labeled controls | P2 | All | M | No skip-link, no documented focus order, no automated keyboard-traversal test; AppShell.tsx has no skip-to-content link. |
| 🔴 | §20.6 Accessibility — screen-reader announcements for AI streaming ('AI is generating'… 'AI completed') | P2 | All | S | Add an aria-live polite region announcing AI generation start/completion. |
| 🔴 | §20.6 Accessibility — high-contrast theme switchable per-user | P2 | All | M | Add a per-user high-contrast theme toggle (tokens permitting). |
| 🔴 | §20.6 Accessibility — dyslexia-friendly font option | P3 | All | S | Add a per-user dyslexia-friendly font option. |
| 🔴 | §20.6 Accessibility — RTL layouts complete for AR/HE/UR locales | P2 | All | XL | No AR/HE/UR locale bundles and no RTL (dir=rtl) layout support. |
| ✅ | BILL-001 Seat-based subscription | P0 | Pro+ | S | Seat count is a plan ceiling, not actively metered/enforced per-active-user at checkout quantity time. |
| ✅ | BILL-002 Usage-based AI credits | P0 | Pro+ | M | Credits are quota-style caps, not purchasable metered credit balances; no credit top-up. |
| 🟡 | BILL-003 Enterprise invoicing (PO, net 30/60, multi-currency) | P1 | EE | L | No purchase-order field, no net-30/60 terms configuration, no multi-currency selection/quoting flow — only Stripe's default currency per customer. |
| ✅ | BILL-004 Tenant-level usage dashboard | P0 | All | S | reportsTotal/reportsExported columns are hardcoded 0 in the per-month rows (full report counts only on main analytics page). |
| ✅ | BILL-005 Provider cost attribution | P1 | Pro+ | S | None material. |
| ✅ | BILL-006 Plan-based feature flags | P0 | All | S | Flag set is hardcoded per plan in the controller rather than data-driven; fine for current scope. |
| 🟡 | BILL-007 Trial tenants and sandbox environments | P1 | All | M | Trial countdown + provisioning exist, but no separate sandbox environment concept; PROGRESS.md lists BILL-007 auto-trial provisioning UI as remaining. |
| 🟡 | BILL-008 Hard budget ceilings with grace + notifications | P0 | Pro+ | M | Ceilings are fixed plan quotas (no tenant-configurable hard $ budget); grace/notifications are payment-failure dunning, not budget-overrun notifications; no proactive 'approaching budget' email/not… |
| 🔴 | BILL-009 Pre-paid credits + optional auto-recharge | P1 | Pro+ | L | Implement purchasable pre-paid AI credit balances with optional auto-recharge. |
| 🟡 | BILL-010 Tax handling (Stripe Tax / VAT / GST) | P1 | Pro+ | S | Relies on Stripe automatic tax only; no explicit VAT/GST id collection or validation flow surfaced. |
| 🔴 | BILL-011 Educational / non-profit pricing tier | P2 | Pro+ | M | Add an educational/non-profit plan tier with appropriate limits/pricing. |
| 🔴 | BILL-012 Marketplace revenue share for plugin authors | P3 | EE | XL | Implement Stripe Connect onboarding + revenue-share payout engine for plugin authors. |
| 🔴 | BILL-013 Open billing engine — Lago or Killbill as alternates to Stripe | P0 | All | XL | No pluggable billing-provider abstraction; no Lago/Killbill adapter. (Per CLAUDE.md, Stripe is the chosen engine, but BILL-013's intent of an open/alternate engine is unmet.) |
| 🟡 | §21.2 Usage metering — full metric set (seats, AI requests, tokens, dictation minutes, report gens, validation runs, storage, integration calls, daemon activations, rulebook test runs, plugin executions, federated rounds) | P1 | All | L | Not metered/surfaced: dictation minutes, validation runs, storage, integration calls, daemon activations, rulebook test runs, plugin executions, federated rounds. Only AI requests/tokens + report c… |
| ✅ | §22.1 KPI — Draft acceptance rate | P1 | All | S | Acceptance = reached Acknowledged status (proxy for 'used after review'), not measured against actual reject events. |
| 🟡 | §22.1 KPI — Impression acceptance rate | P1 | All | M | ImpressionsLightlyEdited is a proxy set equal to aiDraftsAcknowledged (OtherControllers.cs comment: 'assumed <20% edit') — not an actual edit-distance threshold on the impression. |
| 🟡 | §22.1 KPI — Edit distance per section | P1 | All | L | OtherControllers.cs sets EditDistanceSum=0 and EditDistanceSampleCount=0 (comment: 'edit-distance tracking requires version diff; future iteration') — so the KPI always reports 0; not actually comp… |
| 🟡 | §22.1 KPI — vTTQR (verified time to quality report) | P1 | All | M | Uses created→updated delta as a proxy for 'study open to clean export'; no true study-open timestamp, no baseline comparison to show -40% target. |
| ✅ | §22.1 KPI — Report validation pass rate | P2 | All | S | Pass = status reached Validated, not strictly 'passed all blocker rules at first validation' (proxy). |
| ✅ | §22.1 KPI — Contradiction detection rate | P3 | All | S | Counts audit-event keyword hits rather than persisted finding rows; reasonable proxy. |
| 🔴 | §22.1 KPI — Critical-finding capture rate | P2 | Pro+ | M | Add a critical-finding capture-rate KPI (confirmed criticals flagged / total). |
| ✅ | §22.1 KPI — Active radiologists (WAU/MAU) | P3 | All | S | Single active-count for the window, not split into WAU vs MAU. |
| ✅ | §22.1 KPI — Rulebook adoption | P2 | All | S | Counts any attached rulebook, not specifically an APPROVED rulebook as the PRD target implies (≥90% under approved rulebook). |
| ✅ | §22.1 KPI — Provider cost per report | P2 | Pro+ | S | None material. |
| 🟡 | §22.1 KPI — TAT impact (turnaround before/after, -20% at 90 days) | P2 | All | M | No before/after baseline comparison or 90-day trend to express the -20% target; single median only. |
| 🔴 | §22.1 KPI — Peer-review discrepancy rate | P3 | EE | L | Requires a Peer Review feature first; then a discrepancies/reviews KPI. |
| ✅ | §22.2 Governance KPI — Unapproved-prompt usage attempts | P1 | All | S | None material. |
| ✅ | §22.2 Governance KPI — PHI policy violations blocked | P1 | All | S | None material. |
| ✅ | §22.2 Governance KPI — Rulebook regression failures | P2 | Pro+ | S | None material. |
| ✅ | §22.2 Governance KPI — Model drift alerts | P2 | Pro+ | S | Counts alerts; the drift-detection harness producing them is referenced in PROGRESS.md as Enterprise-GA polish (model-eval page exists). |
| ✅ | §22.2 Governance KPI — Audit completeness (% AI events with complete provenance) | P1 | All | S | None material. |
| 🔴 | §22.2 Governance KPI — Time-to-incident-acknowledgement | P3 | EE | M | Add a time-to-incident-ack metric (alert → first response). |
| 🔴 | §22.2 Governance KPI — Vulnerability MTTR / Open critical CVEs / SBOM freshness | P3 | EE | L | Surface dependency-security KPIs (CVE MTTR, open critical CVEs=0, SBOM age) — likely sourced from CI/SBOM tooling. |
| 🟡 | §22.3 Reliability KPI — Web app & AI gateway availability | P2 | All | M | Single-target health probe with a burn-rate alert; no per-edition availability SLA targets (99.95/99.9/99.5), no AI-gateway-specific availability number, no availability dashboard panel in the UI. |
| 🔴 | §22.3 Reliability KPI — Critical-result notification delivery (99.99% within SLA) | P2 | EE | L | Implement delivery tracking + SLA metric for critical-result notifications. |
| 🔴 | §22.3 Reliability KPI — Audit event durability (RPO ≤1 min; RTO ≤5 min) | P3 | EE | L | Define + measure RPO/RTO for the audit/control plane. |
| 🟡 | §22.3 Reliability KPI — Error budget burn (tracked weekly) | P3 | All | M | Burn rate is a 5-min rolling window with alerting, not a weekly error-budget accounting/report. |

### PRD v2 §23 Integration Requirements + §24 Technical Architecture

_RadioPad has implemented a large, genuine slice of §23/§24 on the locked ASP.NET Core 8 + Next.js 16 + Tauri 2 + Capacitor 6 stack — judged on function, not on the OSS libraries the PRD named. Strongly covered: DICOMweb QIDO/WADO/STOW client + tenant-scoped proxy (DicomWebClient.cs, PacsController.cs); FHIR R4 DiagnosticReport export + bidirectional ServiceRequest/DiagnosticReport ingest (FhirDiagnosticReportSerializer.cs, IngestController.cs); HL7 v2 inbound MLLP listener + ORU export + DICOM SR→ORU conversion (Hl7MllpListener.cs, OrthancBridgeController.cs, Hl7OruSerializer.cs); clipboard/desktop bridge (Tauri secure_copy); vendor connector adapters + signed-plugin SDK (Sectra/Visage/Carestream + PacsVendorRouter + desktop/plugin-sdk); SSO/OIDC + SAML 2.0 + SCIM 2.0 (OidcBearerMiddleware, SamlController, ScimController); Stripe billing; SIEM continuous push + snapshot (SiemPushService, Splunk/Sentinel/Elastic/Syslog sinks); REST + OpenAPI (4281-line spec, ~191 operations); Postgres+SQLite data layer; local llama.cpp/Ollama inference; Tauri installers (msi/dmg/deb/appimage/rpm) + signed auto-updater; Docker Compose deployment. Notable GAPS where the FUNCTION (not just the OSS name) is absent: FHIR R5 / Bulk FHIR $export; HL7 ADT message type + in-app MLLP/TLS + outbound ORU delivery (outbox is in-memory only, no MLLP sender); report→DICOM SR export; standalone DICOM UPS-RS worklist/prioritization service; 8 of 11 named enterprise connectors; Lago/Killbill billing alternatives; Slack/Teams/Webhook critical-results FANOUT (only inbound webhooks + push test exist); gRPC, GraphQL, AsyncAPI, and SSE/WebSocket streaming/real-time collaboration in the backend (no .proto, no text/event-stream — SSE exists only in the legacy Node daemon); published TS/Python/Go/Rust SDKs (documented as Planned only); IHE XDS-I.b/SWF.b/AIW-I; MDM (Intune/Jamf/WS ONE); analytics/audit ClickHouse, Redis, NATS, OpenSearch, pgvector/Qdrant, MinIO/S3 (substituted by Postgres/local); vLLM/TGI hosted inference, speculative decoding, NPU acceleration; Helm/k3s/Nix deployment artifacts; frontend module federation, PWA service worker, web workers; desktop gRPC-over-UNIX-socket daemon bridge (uses HTTP sidecar health instead). PROGRESS.md claims (iter-14/15/16/19/20/31/32/33) were verified against real code and are largely accurate, though it counts several rows as fully closed that are actually partial against the PRD clause (e.g. SIEM sink set, vendor connector breadth, HL7 directionality)._

Coverage: ✅8 · 🟡16 · 🔴14 · ⏸1

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| ✅ | §23.1 DICOMweb for study metadata + worklist (QIDO/WADO/STOW) | low | – | S | DICOMweb metadata/WADO/STOW are complete. The 'worklist (UPS-RS)' half is NOT met by this client — see UPS-RS row. |
| 🟡 | §23.1 / §23.2 DICOM UPS-RS worklist + worklist service/prioritization (§24.1 worklist module) | medium | – | L | Implement UPS-RS (workitem subscribe/retrieve/change-state) and a worklist/prioritization service that aggregates DICOM UPS + HL7 ingest into a prioritized worklist; currently inbound work arrives … |
| ✅ | §23.1 / §23.2 FHIR R4 DiagnosticReport export | low | – | S | None for R4 DiagnosticReport export. Output is a single resource (no Bundle/Provenance) — acceptable for the clause. |
| 🔴 | §23.1 FHIR R4 ImagingStudy exchange | low | – | M | Add FHIR ImagingStudy read/emit so study-level imaging metadata can be exchanged with the EHR (PRD lists ImagingStudy alongside DiagnosticReport). |
| 🔴 | §23.2 FHIR R5 + Bulk FHIR ($export) | low | – | L | Add R5 resource variants and a Bulk Data $export (group/system-level async NDJSON) endpoint. PRD targets this for Clinical Beta/GA. |
| 🟡 | §23.1 / §23.2 HL7 v2 ORU (results) + ORM (orders) over MLLP | medium | – | M | (a) Inbound handler explicitly rejects everything except ORU/ORM (no ADT — see ADT row). (b) Outbound result delivery: ORU export is download-only; the Orthanc-derived ORU goes to InMemoryHl7Outbox… |
| 🔴 | §23.1 HL7 v2 ADT (admit/discharge) | low | – | M | Add ADT message handling (patient demographics/encounter context) to the MLLP handler and parser. |
| 🟡 | §23.1 HL7 MLLP transport secured with TLS | medium | – | M | Either terminate TLS in-app (SslStream + client-cert/mTLS) or document+enforce a TLS-terminating reverse proxy; today remote exposure would be cleartext MLLP. |
| 🟡 | §23.1 DICOM SR for measurement-bearing reports | low | – | L | Add report→DICOM SR encoding/STOW so measurement-bearing reports can be written back to PACS as SR objects (PRD frames SR as a report exchange format, not just an inbound bridge). |
| ✅ | §23.1 Clipboard / desktop bridge (universal fallback) | low | – | S | None material. |
| 🟡 | §23.1 Custom enterprise connectors (plugin) + open connector SDK | low | – | XL | Only 3 of the 11 named vendors implemented (Epic Aura, Cerner, GE Centricity, Philips IntelliSpace, Merge, Change Healthcare, Fujifilm Synapse, Intelerad absent). SDK exists but vendor breadth is f… |
| ✅ | §23.2 SSO / OIDC | low | – | S | None material for the clause. |
| ✅ | §23.2 SAML 2.0 | low | – | M | Single-IdP, hand-rolled SignedXml (no multi-IdP federation, no SP-initiated AuthnRequest/SLO). Adequate for the matrix row; extend if multi-IdP needed. |
| ✅ | §23.2 SCIM 2.0 provisioning | low | – | M | User resource covered; Group provisioning (schema declared but) not fully implemented. Minor vs GA target. |
| 🟡 | §23.2 Stripe / Lago / Killbill billing | low | – | L | Stripe satisfies the primary path. Lago/Killbill (self-hosted billing alternatives) absent — would need an IBillingProvider abstraction. Likely fine for GA if Stripe is the supported provider. |
| 🟡 | §23.2 SIEM export (Splunk, Sentinel, Elastic, Chronicle, Loki) | low | – | M | 3 of 5 named SIEMs as native sinks (Splunk/Sentinel/Elastic); Chronicle and Loki not as dedicated sinks (Syslog/CEF can partially cover Loki/QRadar). Add Chronicle + Loki push sinks for full parity. |
| 🔴 | §23.2 Slack / Teams / Webhook for critical results | high | – | L | Build a critical-results notification service: detect critical/Blocker findings, fan out to configurable Slack/Teams/generic webhook channels with acknowledgement tracking. PRD marks this MVP-β. |
| 🔴 | §23.2 IHE XDS-I.b, SWF.b, AIW-I | low | – | XL | Implement IHE XDS-I.b (imaging doc sharing), SWF.b (scheduled workflow), AIW-I (AI workflow) actors. Large, standards-heavy; GA target. |
| 🔴 | §23.2 MDM (Intune, Jamf, Workspace ONE) | low | – | L | Add MDM compliance attestation / managed-config consumption for mobile + desktop. GA target. |
| ✅ | §23.3 Public API — REST + OpenAPI for all CRUD | low | – | S | None material; ensure all controllers stay reflected in the spec. |
| 🔴 | §23.3 WebSocket / SSE for streaming AI + real-time collaboration | medium | – | L | Add SSE or WebSocket token streaming for AI generation and a real-time collaboration channel (PRD §24.1 reporting 'CRDT relay'). Currently AI is request/response only. |
| 🔴 | §23.3 gRPC between internal services and to the daemon | low | – | L | Backend is a single monolith so internal gRPC is moot, but the desktop↔daemon gRPC bridge described in §24.7 is not implemented (HTTP only). Add gRPC IPC if/when the daemon split is real. |
| ⏸ | §23.3 GraphQL (optional, post-GA) | low | – | M | None required pre-GA; implement only if read-heavy admin views need it. |
| 🔴 | §23.3 AsyncAPI for event channels (NATS subjects, webhooks) | low | – | M | Author an AsyncAPI spec for webhook/event channels. Lower value while there is no event bus. |
| 🔴 | §23.3 SDKs: TypeScript, Python, Go, Rust | low | – | L | Generate/publish at least TypeScript + Python SDKs from the OpenAPI spec; Go/Rust later. Currently documentation-only. |
| 🟡 | §24.1 Service modules (identity, tenant, reporting, rulebook, template, integration, audit, billing, etc.) | low | – | M | Per locked stack, function over microservice topology is the correct judgment — implemented as a modular monolith. Gaps are the few responsibilities not built at all: federation/FL (EE), desktop-sy… |
| 🔴 | §24.1 federation (EE) — FL coordinator, DP accountant | low | – | XL | Implement FL coordinator + DP accountant for the EE tier. Large, research-grade; lowest priority. |
| ✅ | §24.2 Data stores — Postgres primary OLTP (per-tenant) | low | – | M | Per-tenant schema separation / dedicated EE clusters are not implemented (single shared schema with TenantId scoping). Acceptable for current scale; revisit for EE isolation. |
| 🟡 | §24.2 Data stores — ClickHouse, Redis, NATS, OpenSearch, pgvector/Qdrant, MinIO/S3, SQLite+SQLCipher | low | – | XL | These are scale/feature substrates: add object storage (S3/MinIO) for exports, a cache/rate-limit store (Redis), event bus (NATS), analytics columnar store (ClickHouse), and vector search (pgvector… |
| 🟡 | §24.3 Inference — local daemon embeds llama.cpp + ONNX, backend switches per platform | low | – | M | llama.cpp/Ollama covered; ONNX Runtime backend and per-platform backend switching not implemented. NPU/quantization rows below. |
| 🔴 | §24.3 Inference — hosted vLLM/TGI autoscaling, speculative decoding, prefix caching, continuous batching | low | – | XL | Stand up self-hosted vLLM/TGI with queue-depth autoscaling if hosted GPU inference is a product requirement; otherwise rely on external providers. |
| 🟡 | §24.3 Inference — model registry (ids, versions, hashes, license, eval, drift baseline) | low | – | M | Implement a first-class model registry table tracking model id/version/hash/license/eval scores/drift baseline distinct from rulebook governance. |
| 🔴 | §24.3 Inference — quantized models (GGUF/INT8/FP16) + NPU acceleration (CoreML/DirectML/OpenVINO/ROCm/Vulkan) | low | – | L | Add explicit quantized-model selection and NPU/accelerator backend switching per platform. |
| 🟡 | §24.4 Twelve-factor adherence (stateless, env config, stdout JSON logs, disposable workers, parity) | low | – | M | OTel-aware stdout JSON logging is partial (structured logging yes; confirm OTel exporter). 'Build=release=run via signed artifacts' is enforced for desktop (signed Tauri bundles) but server artifac… |
| 🟡 | §24.5 Deployment — Helm, Docker Compose, k3s air-gapped, Nix flakes, Tauri installers, CLI distribution | low | – | L | No Helm charts, no k3s air-gapped bundle, no Nix flakes (find → none). CLI distribution channels (Homebrew/Scoop/winget/apt/dnf/AUR) not set up. MSIX/PKG(notarized)/Flatpak/Snap not in bundle targets. |
| 🟡 | §24.6 Frontend — React+Vite (locked→Next.js), route code-splitting ≤200KB, CSP+Trusted Types+SRI | low | – | M | No explicit ≤200KB initial-JS budget gate; Trusted Types and SRI (integrity=) not configured; CSP allows 'unsafe-inline' for hydration. Add Trusted Types policy, SRI on external assets, and a bundl… |
| 🔴 | §24.6 Frontend — module federation for plugin UI, Service Worker PWA + offline drafts, Web Workers, WASM NLP | low | – | L | Add a PWA service worker + offline draft cache for the web client, web workers for parsing/validation/FHIR transforms, and module federation (or App-Router equivalent) for plugin UIs. |
| 🟡 | §24.7 Desktop (Tauri) — hotkeys, native notifications, auto-updater, crash reporter, sidecar, daemon gRPC bridge | low | – | M | Daemon bridge is HTTP sidecar health-check, NOT gRPC over UNIX socket / named pipe (§24.7). Native notifications and a crash reporter are not evidenced in the Rust modules. rdev-based OS hotkeys ar… |
| 🟡 | §24.8 Mobile (Capacitor) — biometrics, WebAuthn passkeys, push (APNs/FCM), screenshot protection, encrypted local store | medium | – | M | Push: APNs sender present but FCM (Android) sender not confirmed. Screenshot protection (FLAG_SECURE / iOS screen-capture block) not found in mobile/. Encrypted local store is via Capacitor plugin … |

### Performance/Reliability + Observability + DR (PRD v2 §25, §26, §27)

_RadioPad has a genuinely functional observability *metrics* spine and a SLO/burn-rate stack on the locked .NET stack, plus a real synthetic availability probe and extensive operational documentation — but tracing, continuous profiling, a status page, and most DR/HA machinery are absent or doc-only. Concrete code: OpenTelemetry metrics (PerfBudgets.cs — 5 histograms; OTLP exporter behind RADIOPAD_OTEL_OTLP_ENDPOINT in Program.cs ~L334-354), per-route PerfBudgetMiddleware, AvailabilityMonitorService (synthetic health probe + burn-rate audit), ObservabilityController (Alertmanager webhook + availability snapshot, RBAC-gated), PhiRedactingLoggerProvider (log PHI redaction), RequestCorrelationMiddleware (X-RadioPad-RequestId), deploy/observability/{slo-recording-rules.yaml,grafana-radiopad-slo.json} (multi-window 2x/5x burn-rate rules + dashboard), perf/k6 scripts + .github/workflows/perf-smoke.yml. Strong doc layer: slo-sla.md, disaster-recovery.md, business-continuity.md, incident-response.md, postmortem-template.md, status-page-policy.md, backup-restore.md, capacity-planning.md, runbook.md, observability.md. KEY GAPS vs PRD v2: (1) NO distributed tracing at all — only request-id correlation; no ActivitySource/spans, no W3C trace context propagation browser→gateway→service→daemon, no radiopad.* span namespace, no high-cardinality attribute hashing (§26.3 missing). (2) NO continuous profiling (§26.4 missing). (3) NO status page implementation, policy doc only (§26.7 missing). (4) SLO instrument set covers validate/sign/AI-draft/QIDO + per-route HTTP but does NOT match the PRD §25.1 surface table (no AI-gateway routing≤80ms, FHIR≤500ms, daemon RPC≤20ms/99.99%, critical-result E2E≤2s, audit P99≤500ms SLO instruments). (5) Capacity targets, scalability strategy (Postgres sharding, NATS JetStream, OpenSearch ILM, PgBouncer, edge caching), and DR architecture (active-active, logical+physical replication, pgBackRest PITR, CRR, DNS failover, restore drills) are documentation/Phase-3 only — actual deploy runs SQLite or single-region Postgres+Caddy. (6) Synthetic monitoring probes only health endpoints, not the golden paths (login/open-study/generate/export), critical-result delivery, or DICOMweb/FHIR per-integration probes. (7) Runbooks are general, not per-service runbooks/<service>.md, and no chaos drills exist. NOTE on stack-swap rule: items judged on FUNCTION not OSS-library identity — e.g. .NET OTel SDK satisfies "OpenTelemetry"; absence of Loki/NATS/Pyroscope only counts where the *function* (tracing, profiling, tiered log storage, event replay) is genuinely absent, which it is for several clauses._

Coverage: ✅0 · 🟡13 · 🔴3 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | §25.1 SLOs and error budgets (per-surface availability + latency SLOs, 2x/5x multi-window burn-rate alerts) | high | Pro/EE | L | Instrumented SLOs (validate 250ms, sign 500ms, AI draft 4s, QIDO 600ms) do NOT match the PRD §25.1 surface table: missing dedicated SLO instruments/budgets for AI-gateway routing P95≤80ms, FHIR/HL7… |
| 🟡 | §25.2 Capacity targets (≥250k reports/mo/node, ≥200 concurrent reporters, ≥10k audit/s/shard, ≥500 AI req/s/pod, ≥8 daemon concurrent, ≤1MB draft) | medium | Pro/EE | L | None of the specific PRD capacity numbers (250k/mo, 200 concurrent, 10k audit/s/shard, 500 AI req/s/pod, 8 daemon concurrent, ≤1MB CRDT draft) are codified, load-tested, or enforced; capacity-plann… |
| 🟡 | §25.3 Scalability strategy (stateless horizontal scaling, Postgres tenant sharding, logical read replicas, NATS JetStream clustering, OpenSearch ILM, edge caching, PgBouncer) | medium | EE | XL | Functionally most strategy is doc/Phase-3 only. No Postgres sharding, no logical replica routing, no event-bus clustering (NATS JetStream function = an event stream with replication/replay; not pre… |
| 🟡 | §25.4 Resource budgets / stretch (Studio idle RAM ≤90MB, daemon idle ≤22MB, CLI startup ≤25ms, cold local 7B Q4 gen <3.5s P95) | low | Pro/EE | M | The specific stretch thresholds (90MB / 22MB / 25ms / 3.5s P95) are not measured, asserted, or gated anywhere; protocol records 'baseline, no regression >20%' rather than the absolute v2.0-LTS targ… |
| 🟡 | §26.1 Instrumentation — OpenTelemetry end-to-end (traces+metrics+logs), per-tenant + per-modality Grafana dashboards, SLO dashboards with burn alerts | high | Pro/EE | L | Only the metrics signal is wired; no traces and (by §26.2) no JSON/OTel log pipeline. Dashboards are a single global SLO board — no per-tenant templated-variable dashboards and no per-modality clin… |
| 🟡 | §26.2 Logs — structured JSON only, PHI redaction layer in logger, CI PHI-template scan, sampling + tiered Loki storage | high | Pro/EE | M | Logging uses Microsoft.Extensions.Logging AddSimpleConsole (single-line text), not structured-JSON-only — logging.md and monitoring.md both list Serilog/JSON as Phase 2. No CI scan that rejects str… |
| 🔴 | §26.3 Tracing — W3C Trace Context everywhere, OTel semantic conventions, radiopad.* clinical span namespace, hash high-cardinality (study_id/tenant_id) trace attributes | high | Pro/EE | XL | No distributed tracing at all: no ActivitySource/spans, no OTel WithTracing()/exporter, no W3C traceparent propagation across browser→gateway→service→daemon→provider, no radiopad.* span attribute n… |
| 🔴 | §26.4 Continuous profiling — CPU+heap from all services and daemon (Pyroscope/Phlare), profiles in post-incident runbook | low | EE | L | No continuous profiling capability of any kind (function absent, not just the named OSS tool). Profiles are not part of any runbook step. |
| 🟡 | §26.5 Runbooks — per-service runbooks/<service>.md with on-call playbook, dashboards, alerts, escalation matrix, dependency map; tested in chaos drills | medium | Pro/EE | M | Runbooks are organized by failure mode, not as the PRD-mandated per-service runbooks/<service>.md committed at repo root (there is NO top-level runbooks/ directory). No explicit per-runbook dashboa… |
| 🟡 | §26.6 Synthetic monitoring — multi-region golden-path probes (login/open study/generate impression/export), critical-result delivery probe, DICOMweb/FHIR per-integration probes | medium | Pro/EE | L | By design the monitor probes ONLY platform health endpoints (PHI-policy forbids report routes), so it does NOT execute the golden user paths (login, open study, generate impression, export), the cr… |
| 🔴 | §26.7 Status page — public per-component status + planned-maintenance schedule (Cachet/Statuspage OSS equivalent) | medium | Pro/EE | M | Implementation is entirely absent — only a policy document. No status-page service/component (no Cachet/Statuspage deploy, no public per-component page, no maintenance-schedule surface). status-pag… |
| 🟡 | §27.1 RTO/RPO targets (per class: drafting RTO 0/RPO 0 local WAL; control plane 15min/1min; audit RPO 1min; FHIR 30min/5min; marketplace 2h/15min; FL 24h/1h) | medium | Pro/EE | M | disaster-recovery.md gives a single coarse target (hosted RTO 4h / RPO 15min) that does NOT match the PRD per-class table (control plane 15min/1min, audit 1min, FHIR 30min/5min, marketplace 2h/15mi… |
| 🟡 | §27.2 DR architecture (active-active EE+ / active-warm Pro, logical+physical replication, pgBackRest PITR 15min, NATS mirror streams, object-storage CRR, DNS health-checked failover, quarterly restore drills) | medium | EE | XL | Almost entirely doc/Phase-3: no active-active or warm-standby deployment, no implemented logical+physical replication, no pgBackRest/PITR automation, no event-bus mirror streams, no object-storage … |
| 🟡 | §27.3 Degradation modes (tested, not aspirational): cloud-unreachable, all-AI-down→local, daemon-down banner, Postgres failover, NATS partition replay, OIDC-down cached sessions, DICOMweb-down manual entry | medium | Pro/EE | L | Not 'tested, not aspirational': no automated chaos/fault-injection tests prove these modes. 'All cloud AI providers down → daemon routes to local model with failover notice' is explicitly NOT imple… |
| 🟡 | §27.4 Backups (daily full + WAL archiving, tested PITR, cross-region replication, tenant-scoped key encryption, weekly random-restore integrity check) | medium | Pro/EE | L | Backup automation itself is not implemented in-repo (it relies on managed-DB features / planned jobs): no scheduled full+WAL job, no cross-region replication wired, no weekly random-restore integri… |
| 🟡 | §27.5 Incident response (Sev1–Sev4 with response times; Sev1 ack ≤5min, comms ≤15min; postmortems Sev1/Sev2 within 72h, external for tenant-affecting) | low | Pro/EE | S | Process is documented (not code), but specifics diverge from PRD: only SEV-1..3 defined (no SEV-4); SEV-1 ack documented as 30 min (PRD requires ≤5 min) and comms ≤15 min only via the status-page p… |

### Reporting Workspace (PRD v2 §14.2, RPT-001 .. RPT-030)

_The RadioPad codebase implements a substantial, functional reporting workspace on the locked stack (Next.js 16 + ASP.NET Core 8 + Tauri 2). The fixed six-section editor, AI impression/draft/cleanup, multi-mode rewrite, "rewrite in my style", AI-mark visual marking, prior-report compare, copy-to-RIS, acknowledgement-gated export (text/PDF/DOCX/JSON/FHIR R4/HL7 ORU), full multi-radiologist signing + addenda, per-report versioning, and dictation (Web Speech + voice command mode) are all present and wired end-to-end. The biggest gaps versus §14.2 are: NO command palette (RPT-018, P0), NO real-time CRDT collaboration (RPT-015), NO comments/@-mentions (RPT-016), NO macros/autotext or snippet library (RPT-021/RPT-022, RPT-021 is P0), NO worklist sidebar with prioritization (RPT-029, P0), NO multi-study reporting view (RPT-030), NO citation insertion (RPT-023), NO bilingual side-by-side rendering (RPT-025), NO "Why this suggestion?" provenance panel (RPT-026, P0), NO peer-review/anonymization action (RPT-028), NO QR-provenance print PDF (RPT-027). Several "implemented" items are partial: autosave is on-blur PATCH + offline-draft buffer, not the 2s WAL-style encrypted autosave RPT-013 specifies; export lacks PDF/A-2u conformance, FHIR R5, and DICOM SR (RPT-011); change history has no undo/redo UI (RPT-014); AI lacks streaming/stop/regenerate (relevant to RPT-017 expectations from AI-013); structured/table/numeric input is template-declared but not rendered as real field/table widgets (RPT-003); conditional-by-modality sections are not engine-evaluated (RPT-002). Of 30 requirements: ~10 implemented, ~10 partial, ~10 missing. Several P0 items (RPT-015 is P1 but RPT-018, RPT-021, RPT-024 partial, RPT-026, RPT-029 are P0) remain missing._

Coverage: ✅4 · 🟡13 · 🔴13 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | RPT-001 Editor with sections (Indication, Technique, Comparison, Findings, Impression, Recommendations), configurable per template | P0 | CE | L | The six sections are HARD-CODED in the frontend SECTIONS const, not derived from the template's sectionsJson. Templates can only seed placeholder text for the fixed six keys; they cannot add/remove… |
| 🔴 | RPT-002 Tenant-specific section layouts with conditional sections by modality | P0 | CE | L | No conditional-section engine. Need template schema for conditional sections (when modality==X show section Y), tenant-scoped layout selection, and a frontend renderer that honors it. Currently zer… |
| 🟡 | RPT-003 Free text, template-based, structured fields, table input, numeric measurement input | P0 | CE | XL | Only free-text + template scaffolding are real input modes. There are NO structured-field widgets, NO table input UI, and NO numeric measurement INPUT controls — measurements are only parsed out of… |
| ✅ | RPT-004 Generate Draft Report from dictation + notes + measurements + study metadata | P0 | CE | S | Functionally present. Minor: the editor toolbar surfaces 'Generate impression' but not a dedicated 'Generate Draft' button (draft mode is reachable via API/voice but not a primary UI button); measu… |
| ✅ | RPT-005 Generate Impression from Findings | P0 | CE | S | None material. |
| 🟡 | RPT-006 Rewrite in my style (personal style memory, per-user, opt-in, encrypted) | P1 | CE | L | Style is learned ad-hoc from pasted samples per request — there is NO persisted, per-user, encrypted, opt-in/deletable/exportable 'style memory' store (that is AI-024's intent and is not built). No… |
| 🟡 | RPT-007 Modes: concise, formal, patient-friendly, referring-physician summary, bilingual, structured | P1 | CE | M | Only 4 of 6 listed modes exist. 'bilingual' and 'structured' rewrite modes are NOT implemented (TryParseMode rejects them, ReportsLifecycleController.cs 350-362). |
| 🟡 | RPT-008 AI-generated text visibly marked (color + screen-reader-friendly) until reviewed/edited | P0 | CE | S | Color marking + clear-on-edit is solid, but there is NO explicit screen-reader annotation (no aria-label/visually-hidden 'AI generated' text) on the .ai-mark regions, which the clause explicitly re… |
| 🟡 | RPT-009 Side-by-side prior report comparison with semantic diff | P1 | CE | L | Diff is COARSE (whole-section 'changed' boolean, not a semantic/word-level diff). No clinical-semantic diff (e.g., laterality/measurement deltas). Prior matching is heuristic (accession stem or bod… |
| ✅ | RPT-010 One-click copy to RIS/PACS via secure clipboard or PACS bridge | P0 | CE | S | Secure clipboard path is complete. The 'PACS bridge' write-back path (push report text into the PACS reporting system) is not clearly wired from this button — adapters exist mainly for study/workli… |
| 🟡 | RPT-011 Export to plain text, PDF/A-2u, DOCX, JSON, FHIR R4/R5 DiagnosticReport, HL7 v2 ORU, DICOM SR | P0 | CE | L | Three sub-formats are MISSING/incomplete: (1) PDF is plain Letter PDF, NOT PDF/A-2u conformant (QuestPDF default, no PDF/A flag). (2) FHIR is R4 ONLY — no R5 serializer (FhirDiagnosticReportSeriali… |
| ✅ | RPT-012 Radiologist acknowledgement required before final export/signing | P0 | CE | S | None material; matches safety-boundary 'AI never auto-signs'. |
| 🟡 | RPT-013 Autosave to encrypted local cache every 2s with WAL-style durability | P0 | CE | L | This is NOT the specified behavior. There is NO 2-second autosave timer — the web editor saves only onBlur (ReportClient.tsx textarea onBlur PATCH). The encrypted offline store is reconnect-driven … |
| 🟡 | RPT-014 Per-report change history (full undo/redo across sections, with edit attribution) | P0 | CE | L | History is recorded with attribution, but there is NO undo/redo UI and no restore-version endpoint. The frontend never lists versions or lets the radiologist undo/redo across sections. 'Full undo/r… |
| 🔴 | RPT-015 Collaboration: real-time multi-user editing via CRDT (Yjs) | P1 | CE | XL | Entirely unimplemented. Would require a CRDT document layer (Yjs or equivalent) + a realtime transport on the locked stack (e.g. ASP.NET SignalR or a websocket hub) + editor binding. None exists. |
| 🔴 | RPT-016 Comments and @-mentions tied to text ranges; resolvable | P1 | CE | XL | No comment system at all. Need a comment entity (with text-range anchors), @-mention resolution against tenant users, resolve/unresolve state, and editor UI. Zero implementation. |
| 🟡 | RPT-017 Per-section AI actions with section-scoped prompts | P0 | CE | M | Section scoping exists for rewrite/style, but there is no general per-section action menu with arbitrary section-scoped prompts (e.g., inline 'AI on this section' affordance on each section block).… |
| 🔴 | RPT-018 Command palette (Ctrl/Cmd-K) with fuzzy actions and rulebook-aware suggestions | P0 | CE | L | Completely missing — and this is a P0. Need a command palette component (keyboard-triggered), an action registry, fuzzy search, and rulebook-aware suggestion sourcing. None present. |
| 🟡 | RPT-019 Dictation: continuous, push-to-talk, hands-free voice commands | P0 | CE | M | Continuous + a hands-free voice-command set exist. But there is NO explicit push-to-talk (hold-to-talk) mode — only a click toggle. lang is hard-coded 'en-US' (flagged in UI-UX audit), limiting mul… |
| 🟡 | RPT-020 Auto-formatting (bullet impression, sentence case, measurement normalization) | P0 | CE | M | Auto-formatting is achieved only indirectly via the AI prompt and is not a deterministic client-side formatter. There is NO explicit sentence-case transformer, NO automatic bullet reformatting of t… |
| 🔴 | RPT-021 Macros / autotext (user, tenant, subspecialty scopes) | P0 | CE | L | No macros/autotext at all — and this is P0. Need a Macro entity with user/tenant/subspecialty scoping, CRUD endpoints, and editor-side trigger/expansion. Zero implementation. |
| 🔴 | RPT-022 Snippet library with versioning | P1 | CE | L | No snippet library and no snippet versioning. Distinct from templates. Zero implementation. |
| 🔴 | RPT-023 Citation insertion (linked to evidence module) | P1 | CE | L | No citation insertion and no evidence module to link to. Zero implementation. |
| 🟡 | RPT-024 Keyboard-first navigation and full screen-reader support | P0 | CE | L | Not 'keyboard-first' or 'full' screen-reader support. The UI-UX audit (PROGRESS Iter 43) explicitly found: no skip link, missing focus traps, EmptyState lacks aria-live, inconsistent role semantics… |
| 🔴 | RPT-025 Bilingual side-by-side rendering (e.g., AR + EN) | P1 | CE | L | No side-by-side bilingual report content rendering. Locale switching changes UI chrome only, not report body. Zero implementation of the clause. |
| 🔴 | RPT-026 Why this suggestion? panel (rulebook, prompt, model, input excerpt) | P0 | CE | M | No 'Why this suggestion?' UI panel exposing rulebook + prompt + model + input excerpt to the radiologist. The data is largely available server-side; it is simply not surfaced. P0. Needs a frontend … |
| 🔴 | RPT-027 Print-optimized PDF with QR code linking to provenance hash (audit) | P1 | EE | M | No QR code generation, no provenance-hash embedding, no print-optimized layout variant. EE-tier feature entirely unimplemented. |
| 🔴 | RPT-028 Send to peer review action with anonymization toggle | P1 | CE | L | No peer-review workflow and no anonymization toggle. Zero implementation. |
| 🔴 | RPT-029 Worklist sidebar with prioritization (STAT/urgent/routine/screening) | P0 | CE | L | No priority field/enum on reports, no worklist sidebar component, no prioritization sort/filter. P0 and entirely missing as a workspace feature. |
| 🔴 | RPT-030 Multi-study reporting view (combined exam reads, e.g., CT chest+abdomen+pelvis) | P1 | CE | XL | No multi-study/combined-exam reporting model or view. Zero implementation. |

### Security, Privacy, Compliance + Regulatory (PRD v2 §18, §19)

_RadioPad implements the core, P0 security/privacy controls of §18 well on the locked .NET/Next.js/Tauri stack: PHI policy enforcement (AiGateway.EnforcePhiPolicy + ProviderPolicyException, fail-closed, audited), AES-256-GCM column encryption with per-tenant KMS envelope encryption and real AWS/Azure/GCP CMK adapters, a SHA-256 hash-chained append-only audit log with a verification routine, fine-grained RBAC/ABAC (PermissionCatalog + EndpointPermissionMatrix), SSO (OIDC presets + SAML ACS), MFA via WebAuthn with hand-rolled attestation parsing + FIDO MDS3 root pinning, SCIM 2.0 provisioning, IP allowlists + anomaly detection/SIEM push, signed desktop binaries (Tauri ed25519 via OIDC->SSM), env-ref/OAuth-vault secret storage with a 15-minute OAuth rotation worker, CSP/HSTS/X-Frame security headers, and a model-drift BackgroundService for post-market monitoring. The compliance/regulatory posture (§18.3, §19) is documented thoroughly (22 security docs + 12 regulatory docs: BAA template, EU AI Act/GDPR profile, PMS plan, SaMD classification, ISO 14971 risk register, IEC 62304 SDLC, intended-use, traceability matrix). The principal gaps are supply-chain automation and process-as-code: SBOM (SEC-019) and SLSA-3 provenance (SEC-020) are documented but NOT generated in CI; the §19.5 claims-policy is doc-only (no CI lint gate for banned words like 'diagnose'/'interpret'); §18.1 mTLS/SPIFFE-SPIRE and the full secrets-rotation cadence (90d API keys / 30d mTLS certs) are aspirational; Trusted Types + SRI (SEC-018), confidential computing (SEC-023), container image signing/admission (SEC-021), honeypots (SEC-022), and BYOK+HYOK depth (SEC-003) are partial/missing. Note CLAUDE.md locks the stack, so Go/Rust/Keycloak absence is not counted against function; judgments are by capability. Several P2/EE controls and AI Act runtime artifacts (PCCP runtime, post-market field-failure workflow UI) remain documentation rather than enforced code._

Coverage: ✅7 · 🟡16 · 🔴9 · ⏸0

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | SEC-001 — TLS 1.3 only on perimeter (1.2 for legacy MLLP) | P0 | CE | S | TLS version (1.3-only with 1.2 legacy exception) is delegated to the operator proxy config, not pinned/asserted in repo. Add a documented hardened nginx/Caddy TLS block (ssl_protocols TLSv1.3; lega… |
| ✅ | SEC-002 — AES-256-GCM at rest; per-tenant KMS keys | P0 | CE | S | Production requires operator to set RADIOPAD_COLUMN_KEY_REF + wrapped DEK; dev fallback key is deterministic (guarded out of Production). Full DB/volume-level at-rest encryption is delegated to the… |
| 🟡 | SEC-003 — Customer-managed keys (BYOK + HYOK) | P1 | EE | L | BYOK (customer-supplied CMK) is covered via CmkKeyRef. HYOK (hold-your-own-key, customer-side key never leaving customer HSM with split-knowledge / remote unwrap proxy) is not implemented — current… |
| ✅ | SEC-004 / SEC-012 — Per-tenant PHI policy + provider compliance enforcement | P0 | CE | M | PRD says 'enforced in daemon + gateway'; on the locked stack the gateway is the enforcement point (no separate Go/Rust daemon). Desktop/daemon-side pre-flight PHI gate (defense-in-depth before the … |
| 🟡 | SEC-005 — Audit log immutability (hash-chained, append-only, optional timestamping) | P0 | CE | M | Immutability is by-convention (DB rows are technically mutable; no WORM object store / DB row-level immutability trigger). Optional RFC-3161 trusted timestamping is not implemented. |
| ✅ | SEC-006 — Least-privilege RBAC + ABAC | P0 | CE | M | ABAC is attribute-influenced (clinical flag, tenant scope) rather than a full policy-engine (e.g., OPA/Cedar) for arbitrary resource attributes. Sufficient for stated needs; richer attribute condit… |
| ✅ | SEC-007 — SSO (OIDC/SAML), MFA (WebAuthn preferred), SCIM | P0/P1 | CE/EE | S | TOTP MFA + WebAuthn both present; SAML is SP-side ACS (no full IdP-metadata signing-cert rotation UI). Minor: SCIM filter beyond userName eq is limited. |
| 🟡 | SEC-008 — IP allowlists + device posture checks | P1 | EE | L | IP allowlist is complete; 'device posture checks' (OS patch level, disk-encryption, EDR attestation for Studio + daemon) are only a device-pairing token, not true posture assessment. |
| 🟡 | SEC-009 — Secret storage in OS keychain + Vault; never plaintext on disk | P0 | CE | M | Env-var + encrypted-DB vault is the implemented path; no integration with HashiCorp Vault or OS keychain (desktop keychain) as named. Add Vault provider + OS-keychain binding for desktop. |
| ✅ | SEC-010 — PHI redaction in debug logs; structured logging only | P0 | CE | S | Redaction relies on hashing-at-source + a desktop redactor; no central allow-list-based PHI scrubber middleware over all log sinks to catch accidental PHI in arbitrary log statements. |
| 🟡 | SEC-011 — Intrusion detection + anomaly alerts | P1 | EE | M | Custom anomaly detection exists; the PRD-named Falco (host/container runtime IDS) is not present (out-of-process infra concern). Add infra-layer IDS deployment manifests. |
| 🟡 | SEC-013 — Signed binaries; verified updates; rollback on failed health check | P0 | CE | M | Signing + verified updates implemented; automatic rollback if first run fails health checks is not evidenced in the desktop updater flow. Add post-update health-probe + rollback-to-previous-version… |
| 🟡 | SEC-014 — Crypto: Ed25519 sigs, X25519 KEX, AES-GCM-SIV (nonce-misuse resistance) | P0 | CE | M | X25519 key exchange not used (no in-app E2E channel needing it; TLS handled by proxy). AES-GCM-SIV is NOT used — the implementation uses plain AES-GCM with random 12-byte nonces (acceptable but not… |
| 🟡 | SEC-015 — Encrypted backups; quarterly restore drills | P0 | CE | M | No automated encrypted-backup job or restore-drill harness in repo; restore drills are a documented process only. Add backup automation + a scheduled restore-verification job. |
| 🟡 | SEC-016 — Secrets rotation policy (API keys 90d, mTLS certs 30d, session tokens 15m) | P0 | CE | M | Concrete enforced cadences for API keys (90d) and mTLS certs (30d) are policy-doc only — no automated 90d API-key expiry/rotation job and no mTLS at all (see SEC-014/§18.1). Session-token 15m TTL n… |
| 🟡 | SEC-017 — Phishing-resistant MFA enforced for admin roles | P0 | CE | S | WebAuthn exists but there is no policy that REQUIRES WebAuthn (vs TOTP) specifically for admin/SecurityManage roles. Add a role-conditional MFA-strength enforcement gate. |
| 🟡 | SEC-018 — Browser CSP (strict), Trusted Types, SRI for scripts | P0 | CE | L | CSP currently allows script-src 'self' 'unsafe-inline' (relaxed for Next.js hydration) — NOT strict. No Trusted Types (require-trusted-types-for 'script') and no Subresource Integrity (integrity= h… |
| 🔴 | SEC-019 — SBOM (SPDX + CycloneDX) published per release | P0 | CE | M | No automated SBOM generation in CI (cyclonedx-dotnet/cyclonedx-npm not wired), no SPDX format, no per-release publication as artifacts. Add a CI job generating + attaching CycloneDX and SPDX SBOMs. |
| 🔴 | SEC-020 — SLSA-3 provenance attestations | P1 | CE | L | No build provenance (slsa-github-generator / in-toto attestations), no hermetic/reproducible build pipeline. Add SLSA provenance generation + verification to the release workflows. |
| 🔴 | SEC-021 — Container images signed; admission rejects unsigned | P1 | EE | M | No container image signing (cosign) and no Kubernetes admission policy rejecting unsigned images. Add cosign signing + admission policy. |
| 🔴 | SEC-022 — Per-tenant honeypot endpoints for credential probing | P2 | EE | M | Honeypot/canary endpoints not implemented. Add tenant-scoped decoy endpoints feeding AnomalyDetector/SIEM. |
| 🔴 | SEC-023 — Confidential computing (SGX/TDX, SEV-SNP) | P2 | EE | XL | No confidential-computing / enclave attestation path. Highest-tier EE feature; absent. Requires deployment-target enclave support + remote attestation integration. |
| 🔴 | SEC-024 / SEC-025 — Network segmentation (data/control plane VPCs); egress allowlist | P0 | EE | L | Data-plane/control-plane VPC separation and Kubernetes NetworkPolicy egress allowlists are not codified as deployable manifests. Add IaC + NetworkPolicy/firewall rules. |
| 🔴 | SEC-026 — Hardware security keys for break-glass | P1 | EE | M | No break-glass emergency-access procedure backed by hardware keys (split-knowledge / dual approval / time-boxed). Add break-glass flow. |
| 🔴 | §18.1 — Zero-trust + mTLS between internal services; SPIFFE/SPIRE workload identity | P1 | EE | L | Service-to-service mTLS + SPIFFE/SPIRE not implemented. Largely N/A for a single API process, but EE multi-service deployments would need a service mesh (mTLS) + workload identity. Document archite… |
| 🟡 | §18.1 — Defense in depth (WAF, RASP, dependency scanning, SAST, DAST, fuzzing) | P0 | CE | M | No dependency scanning (Dependabot/Snyk), SAST (CodeQL), DAST, or fuzzing jobs in .github/workflows/ci.yml. No WAF/RASP. Add CodeQL + dependency scanning + (optionally) DAST to CI. |
| ✅ | §18.4 — AI provider compliance profiles (BAA/DPA/ZDR) surfaced; first-class object; no-downgrade fallback; exportable risk registry | P0 | CE | S | BAA/DPA/ZDR statuses + jurisdictional notes are modeled and doc-tracked; surface 'endpoint eligibility' + ZDR attestation explicitly per-provider in the admin UI and ensure the risk registry export… |
| 🟡 | §18.3 — Compliance posture (HIPAA, HITECH, GDPR, ISO 27001/13485, IEC 62304/82304, NIST, SOC2, etc.) | P0 | CE/EE | L | Frameworks are addressed as documentation/mappings (appropriate for many), but several technical safeguards they require are still partial in code (backups SEC-015, rotation SEC-016, breach-notific… |
| ✅ | §19.1 / §19.2 — Intended-use statement + risk-classification matrix (non-SaMD posture) | P0 | CE | S | Captured as policy + reflected in product safeguards. To reach 100%, bind the intended-use/claims to the UI (display intended-use disclaimer in-app) and assert the risk-tier triggers in CI. |
| 🟡 | §19.3 — Predetermined Change Control Plan (PCCP) | P1 | EE | L | PCCP is a document; the runtime change-envelope (architecture/training-data/hyperparameter bounds), acceptable-performance-bounds gating, and re-training triggers are not enforced as code/config. E… |
| 🟡 | §19.4 — Post-market monitoring (drift watch, field-failure reporting, incident SLAs, annual review) | P1 | CE/EE | M | Drift watch is real. Field-failure reporting workflow (clinician-submitted defect intake), incident classification + notification SLAs, and the annual Clinical Safety Working Group review are doc-o… |
| 🔴 | §19.5 — Claims governance (banned-word CI gate for 'diagnose'/'interpret'/'decide'/'automated reading') | P1 | CE | S | The claims policy is not enforced — no CI check flags 'diagnose/interpret/decide/automated reading' in UI strings, docs, or marketing copy for human review. Add a claims-lint CI job over frontend s… |

### Testing Strategy + DevEx/APIs + Acceptance Criteria (PRD v2 §28, §29, §31)

_RadioPad has a genuinely substantial test and DevEx foundation on the locked .NET/Next.js/Tauri/Capacitor stack, but it is well short of PRD v2 §28/§29/§31 as written. STRONG: ~85-test xUnit backend suite (unit + WebApplicationFactory integration against SQLite), 24 frontend Vitest component suites, golden-case CI (22 rulebooks + 52 cases via `radiopad rulebook test`), k6 perf scripts with P95/P99 thresholds wired to `perf-smoke.yml`, nightly live SIEM/KMS suites, an in-repo OpenAPI spec, multi-OS desktop signing + post-release installer signature verification, CONTRIBUTING/SECURITY/CODE_OF_CONDUCT, a rich 130-row traceability matrix, plus real implementations behind many acceptance criteria (SCIM 2.0, KMS/CMK, OIDC/SAML/WebAuthn, SIEM CEF/NDJSON, governance dashboard, validation packs, FHIR DiagnosticReport serializer, locale scaffolding, audit hash-chain, PHI policy enforcement, provider fallback by compliance class). GAPS vs the PRD text: NO branch-coverage gate in CI (≥80% / ≥90% ungated — only a documented target); NO property-based, fuzz, mutation, chaos, load, or AI-snapshot/visual-regression tests; NO Playwright/tauri-driver e2e (explicitly "planned"), so every acceptance criterion verified "by UI e2e" is unverified; NO SAST/DAST/Trivy/grype/Cosign/SBOM(CycloneDX) wired into CI (security-test-plan marks them "planned", no scanning workflow exists); NO testcontainers integration matrix (Postgres/Redis/NATS/MinIO/Orthanc/HAPI), NO public Conformance Suite or dashboard; NO SDK packages (@radiopad/sdk, python/go/rust), NO AsyncAPI specs, NO schema-first codegen, OpenAPI is 3.0.3 not 3.1; NO docs site (Pagefind/MDX) — only a `docs/` markdown tree; NO ARCHITECTURE.md/GOVERNANCE.md; local-dev bootstrap differs from the §29.1 recipe (no `./scripts/bootstrap`, no `docker compose up` of the OSS infra, no Makefile targets). Many §31 criteria are clinically/operationally aspirational and absent in code: bias eval, hallucination/prompt-injection corpora, contradiction-recall ≥95% on a 500-case set, WER≤12% whisper benchmark (whisper is documented sidecar wiring only), RECIST 1.1 tracking, peer review/concordance, MIRC teaching files, federated learning, air-gapped install test, SOC2/ISO/HITRUST/EU-AI-Act packs, signed model-eval reports, fleet-config API, `radiopad migrate` N→N+2, contract tests, reproducible builds. CLAUDE.md stack-substitution rule was applied throughout — items are NOT marked missing merely for lacking Go/Rust/Keycloak/Vite; they are marked on whether the FUNCTION exists._

Coverage: ✅2 · 🟡40 · 🔴34 · ⏸2

| St | Requirement | Pri | Ed | Eff | Gap to 100% |
| --- | --- | --- | --- | --- | --- |
| 🟡 | §28.1 Unit tests — every package, ≥80% branch coverage gate | – | – | M | Wire coverlet + a CI-enforced branch-coverage threshold (fail under 80%); add vitest coverage gate for frontend logic. No package-completeness audit exists. |
| 🔴 | §28.1 Property-based tests (proptest/hypothesis) for parsers, schema, validators | – | – | L | Add a property-based testing library and generators for RulebookSpec.FromYaml, the validation rule engine, and FHIR/schema serialization. |
| 🟡 | §28.1 Integration tests with testcontainers (Postgres, Redis, NATS, MinIO, Orthanc, HAPI FHIR) | – | – | L | Add Testcontainers-based integration matrix against real Postgres + an Orthanc + HAPI FHIR reference server (function = integration coverage on real backing services). |
| 🔴 | §28.1 End-to-end tests with Playwright (web) and tauri-driver (Studio) | – | – | L | Implement the 7 documented e2e flows in Playwright + a CI job; add a tauri-driver Studio smoke. This blocks every §31 criterion verified 'by UI e2e'. |
| ✅ | §28.1 Performance tests (k6/Vegeta) with enforced perf budgets | – | – | S | Manual-trigger only (not gated per-PR) and runs against the Mock provider, not staging; Vegeta not used. Function (k6 perf budgets enforced) is present. |
| 🔴 | §28.1 Monthly load tests (synthetic 10× steady-state burst) | – | – | M | Add a scheduled high-volume k6 load profile (10× steady-state) and a monthly workflow. |
| 🔴 | §28.1 Chaos tests (Litmus/Chaos Mesh); SLOs hold under failure | – | – | L | Add chaos/fault-injection experiments and assert SLOs under tested failure modes (equivalent tooling acceptable per stack-substitution). |
| 🔴 | §28.1 Fuzz tests for FHIR/HL7/DICOMweb parsers (cargo-fuzz/go-fuzz/atheris) | – | – | L | Add coverage-guided fuzzing (e.g. SharpFuzz) for the HL7/FHIR/DICOMweb parsing surface. |
| 🔴 | §28.1 Mutation tests for safety-critical validators | – | – | M | Add Stryker.NET against RadioPad.Validation with a mutation-score gate. |
| 🟡 | §28.1 Snapshot tests for AI outputs vs golden cases (rulebook regression) | – | – | M | Add deterministic golden-output snapshots with tolerance bands for AI-generated text (overlaps MVP-β-04); current harness checks flags, not output diffs. |
| 🟡 | §28.2 Per-subspecialty golden-case packs (≥50 at GA → ≥500 at LTS) | – | – | XL | Cases are ~1-5 per subspecialty, far below ≥50/subspecialty at GA and ≥500 at LTS. Needs a large clinically-curated case-pack expansion. |
| 🟡 | §28.2 Hallucination harness with crafted cases | – | – | M | No dedicated crafted-case hallucination test corpus or eval harness measuring detection rate; detector exists but is not exercised by an adversarial fixture set. |
| 🔴 | §28.2 Adversarial prompt-injection corpus | – | – | M | Build an adversarial prompt-injection corpus and an automated eval asserting the gateway/validator resists it. |
| 🔴 | §28.2 Bias evaluation across cohorts (sex/age/language) | – | – | L | Implement a bias-evaluation harness over public+synthetic cohorts with reported metrics. |
| 🟡 | §28.3 SAST (semgrep/gosec/cargo-audit/npm audit) | – | – | M | Add a SAST + dependency-audit CI job (semgrep or equivalent + dotnet/pnpm vulnerable-package scan) that fails the build. |
| 🔴 | §28.3 DAST (OWASP ZAP) against staging | – | – | M | Stand up a staging DAST scan (ZAP baseline/full) on a schedule. |
| 🔴 | §28.3 Dependency vuln scan (Trivy/Grype/Dependabot) | – | – | S | Add Dependabot config and a Trivy/Grype scan job with a high/critical CVE gate (covers MVP-α-13). |
| 🔴 | §28.3 Container scan in CI; admission controller rejects unsigned/vuln images | – | – | L | Add image scanning in CI and (where K8s is used) a signature/vuln admission policy. |
| ⏸ | §28.3 Annual external pen test; quarterly internal red-team (EE) | – | – | S | Process item — no in-repo deliverable expected; track via security tracker. |
| 🟡 | §28.3 Bug bounty / responsible disclosure with safe-harbor | – | – | S | No explicit safe-harbor language and no bug-bounty program/scope; mailbox is a placeholder (security@radiopad.example). |
| 🔴 | §28.4 Public RadioPad Conformance Suite (runnable by providers/connectors/rulebook authors) | – | – | XL | Build a packaged, externally-runnable conformance suite for providers/PACS connectors/rulebooks. |
| 🔴 | §28.4 Public conformance dashboard per integration | – | – | L | Publish a per-integration conformance status dashboard (depends on the conformance suite). |
| 🟡 | §29.1 Local development bootstrap (./scripts/bootstrap, docker compose up, make targets) | – | – | M | Function (a documented one-command local setup) partly exists but diverges from §29.1; deploy/docker-compose.prod.yml is production-only. Add a dev compose + bootstrap/make-equivalent if exact reci… |
| 🔴 | §29.2 SDKs (@radiopad/sdk TS, python, go, rust) | – | – | XL | At minimum publish a typed TS client SDK (function the locked stack can satisfy); Python for analytics/export. Go/Rust SDKs optional under stack-substitution but the client-SDK function is absent. |
| 🟡 | §29.3 OpenAPI 3.1 specs in repo, rendered to docs/api/ | – | – | S | Spec is OpenAPI 3.0.3, not 3.1 as required; no rendered docs/api/ output published. |
| 🔴 | §29.3 AsyncAPI specs for NATS subjects and webhooks | – | – | M | Author AsyncAPI specs for the webhook/event surface (NATS subjects are stack-substituted, but the event-contract function is missing). |
| 🔴 | §29.3 Schema-first development (code generated from specs) | – | – | L | Adopt schema-first codegen or at least a spec/code drift-check in CI. |
| 🟡 | §29.4 Conventions — Semver, Conventional Commits, release-please, edition/version pins, MSRV CI-checked | – | – | M | Add release-please (or equivalent automated changelog/release), an MSRV/toolchain-version CI check, and explicit semver gates for the rulebook spec + plugin ABI. |
| 🟡 | §29.5 Contributor docs — CONTRIBUTING.md, ARCHITECTURE.md per subdir, SECURITY.md, CODE_OF_CONDUCT.md, GOVERNANCE.md | – | – | S | Add GOVERNANCE.md (steering committee/RFC/working groups) and per-subdir ARCHITECTURE.md (or a single architecture doc satisfying the function). |
| 🔴 | §29.6 Documentation site (docs.radiopad.dev, MDX, Pagefind, versioned, public reference) | – | – | L | Stand up a generated, searchable (Pagefind), versioned docs site publishing API reference/tutorials/runbooks. |
| 🔴 | §31 P0 gating — no release until every P0 criterion green in CI for two consecutive nightly runs | – | – | L | Encode §31 criteria as automated checks and a release gate requiring two green nightlies. |
| 🟡 | MVP-α-01 Draft from free-text/paste in ≤2 clicks (UI e2e) | – | – | S | Add the Playwright flow to objectively verify the ≤2-click path. |
| 🟡 | MVP-α-02 Findings→Impression 100% on 200-case fixture, P95≤5s local model (perf+golden) | – | – | M | No 200-case fixture asserting 100% output coverage; perf runs against Mock provider, not the bundled local model. |
| 🟡 | MVP-α-03 AI text distinct visual treatment until edited/accepted (visual regression + unit) | – | – | M | No automated visual-regression suite (test-strategy.md notes Storybook snapshot is future work). |
| 🟡 | MVP-α-04 Export TXT/PDF/DOCX/JSON/FHIR DiagnosticReport passing schema validation | – | – | M | No automated FHIR-validator/DOCX-schema gate in CI asserting produced files pass schema (acceptance-criteria F-06 only checks non-empty text). |
| ✅ | MVP-α-05 Audit events record full provenance + hash-chain verifier | – | – | S | None material for the in-repo function; confirm every listed field is asserted in one consolidated test. |
| 🟡 | MVP-α-06 Provider policy blocks PHI to non-PHI-approved providers w/ active BAA/DPA metadata | – | – | S | PHI blocking is solid; but 'active BAA/DPA metadata' is a free-text RetentionLabel, not structured BAA/DPA validity gating. |
| 🟡 | MVP-α-07 Desktop Studio ≤80MB install, ≤1.5s P95 start (bundle-size CI gate + perf) | – | – | M | No 80MB bundle-size CI gate and no 1.5s startup perf harness; current target documented as ≤5s, weaker than the criterion. |
| 🔴 | MVP-α-08 CLI single-binary ≤25MB compressed across 4 targets (artifact size gate) | – | – | M | Add a CLI release workflow with cross-target single-file publish + a ≤25MB compressed artifact-size gate. |
| 🔴 | MVP-α-09 Daemon resident memory ≤30MB idle / ≤120MB load (memory benchmark in CI) | – | – | M | Add a memory-benchmark job asserting the idle/steady-state ceilings. |
| 🟡 | MVP-α-10 P0 unit tests ≥90% line coverage on safety-critical modules (coverage report) | – | – | M | No coverage report generated/gated in CI to prove ≥90% on validation/rulebook/audit/AI-gateway. |
| 🔴 | MVP-α-11 Sigstore Cosign signing, offline-verifiable | – | – | M | Cosign is named specifically; current OS-native signing is a partial functional substitute for installers but no Cosign/keyless signing+offline verify exists for containers/binaries. |
| 🔴 | MVP-α-12 SBOM (CycloneDX 1.5) per release, published per artifact (Syft+grype) | – | – | S | Generate and publish a CycloneDX SBOM per release (Syft) and attach to each artifact. |
| 🔴 | MVP-α-13 No high/critical CVEs at release (grype CI gate) | – | – | S | Add a grype/Trivy CVE gate failing the release on high/critical (with documented exceptions). |
| 🟡 | MVP-α-14 Generate-Impression emits OTel span tree (gateway/rulebook/validator/provider/audit) | – | – | M | No test asserts a connected span TREE with all five named spans for the impression round-trip (only metric histograms verified). |
| 🟡 | MVP-α-15 Admin can create/edit/version/approve a rulebook end-to-end via UI (UI e2e) | – | – | S | Backend+component coverage strong; the end-to-end UI verification (Playwright) is missing. |
| 🟡 | MVP-β-01 DICOMweb QIDO/WADO against dcm4che reference server with mTLS | – | – | M | Tests are unit/stub-level; no integration against a dcm4che reference server and no mutual-TLS test. |
| 🟡 | MVP-β-02 FHIR DiagnosticReport validates against US Core + IHE Radiology profiles | – | – | L | No US Core / IHE profile packs and no FHIR validator run; grep found no us-core/IHE/StructuredDefinition in code. Validation is not profile-conformant. |
| 🟡 | MVP-β-03 HL7 v2 ORU^R01 round-trips through Mirth Connect channel | – | – | M | No integration against a Mirth Connect test channel; round-trip is in-process only. |
| 🟡 | MVP-β-04 Rulebook regression: golden-case execution, deterministic diffs, AI tolerance bands | – | – | M | No deterministic text-diff viewer and no tolerance bands for AI-generated free text; current diffs are flag-based, not output-based. |
| 🟡 | MVP-β-05 Contradiction checker ≥95% recall on curated 500-case adversarial set | – | – | L | Checker exists but no 500-case adversarial set and no recall measurement; only a handful of conflict fixtures. |
| 🟡 | MVP-β-06 Critical-results notify referring user via a channel ≤30s (E2E timing test) | – | – | L | No dedicated critical-results workflow with referring-user notification channel and a ≤30s E2E timing test. |
| 🟡 | MVP-β-07 Enterprise audit export to SIEM via syslog/CEF and S3 NDJSON with full provenance | – | – | M | Syslog + CEF + NDJSON present; S3 NDJSON sink not implemented; provenance fields present but not the S3 destination. |
| 🟡 | MVP-β-08 SSO OIDC+SAML production-ready vs Keycloak/Okta/Entra/Google (IDP matrix test) | – | – | M | No live IDP-matrix integration test across all four IdPs (live IdP wiring is operator-gated per traceability); Entra/Google not separately verified. |
| 🟡 | MVP-β-09 Provider fallback impossible from PHI-approved to non-approved (property-based test) | – | – | S | Enforcement exists but verified by example tests, not property-based as the criterion specifies. |
| 🔴 | MVP-β-10 Voice dictation (whisper.cpp) WER≤12% on radiology corpus (English) | – | – | L | Bundle whisper.cpp, add a radiology test corpus and an automated WER≤12% benchmark. |
| 🔴 | MVP-β-11 Peer review queue, blinded review, concordance scoring operational | – | – | XL | Build the peer-review workflow (queue, blinding, concordance) with service + UI tests. |
| 🔴 | MVP-β-12 Teaching files export RSNA MIRC-compatible packages | – | – | L | Implement MIRC-compatible teaching-file packaging + schema validator. |
| 🔴 | MVP-β-13 RECIST 1.1 lesion tracking (target/non-target sums, response categories, timeline) | – | – | XL | Implement RECIST 1.1 tracking with numerical regression tests against published examples. |
| 🟡 | GA-01 SCIM 2.0 provisioning end-to-end with ≥3 IDPs (integration matrix) | – | – | M | No multi-IdP integration matrix proving end-to-end provisioning against ≥3 real IdPs. |
| 🟡 | GA-02 SIEM export supports CEF, LEEF, OCSF, and S3 NDJSON (format conformance) | – | – | M | LEEF, OCSF, and S3 NDJSON formats not implemented; no format-conformance test suite for all four. |
| 🟡 | GA-03 Customer-managed keys (KMS/HSM/Vault Transit) verifiable via key-id metadata (key-rotation test) | – | – | M | HSM and HashiCorp Vault Transit not implemented; key-id stamped on artifacts but a full key-rotation test across providers is partial. |
| 🟡 | GA-04 On-prem deploy runbook 0→working tenant ≤4h on documented hardware (install drill) | – | – | M | No documented hardware reference or a timed ≤4h zero-to-tenant install-drill record. |
| 🟡 | GA-05 AI governance dashboard (model inventory, prompt/rulebook versions, drift, PHI posture, audit completeness) | – | – | M | Does not clearly surface a full model inventory and drift alerts + PHI-routing posture in one dashboard; partial coverage of the required panels. |
| 🟡 | GA-06 Model-eval harness vs any registered provider/model → published SIGNED evaluation report (Eval CLI + signed artifact) | – | – | M | Produces console pass-rate only — no published, SIGNED evaluation report artifact. |
| 🟡 | GA-07 Rulebook approval auditable, signed, with evidence packs for medical-director sign-off | – | – | M | Approvals are audited but not cryptographically SIGNED and no bundled 'evidence pack' for sign-off. |
| 🔴 | GA-08 Desktop/daemon centrally manageable via MDM config files + fleet-config API (fleet test) | – | – | L | Add fleet configuration API + MDM-distributable config and a fleet-management test. |
| 🟡 | GA-09 Multilingual UI (8 langs) + report generation in English plus ≥2 more (i18n test suite) | – | – | L | Only 6 of the 8 required UI locales (missing Arabic + Japanese + simplified Chinese; pt/hi present but zh/ar/ja absent); report-generation multilingual not demonstrated for ≥2 non-English languages. |
| 🔴 | GA-10 Federated learning rounds complete on FL test rig without leaking raw PHI (privacy audit) | – | – | XL | Implement an FL test rig + privacy-audit script; entirely absent. |
| 🟡 | GA-11 Plugin marketplace: ≥5 reference plugins, signature verification, conformance suite | – | – | L | Signature verification present; need ≥5 shipped reference plugins and a plugin conformance runner (ties to §28.4). |
| 🔴 | GA-12 Air-gapped deploy: offline install, no egress, model bundles + SBOM mirror (network-blackhole test) | – | – | XL | Document and test a fully offline install including bundled models and SBOM mirror. |
| ⏸ | GA-13 SOC2 Type II / ISO 27001 in progress or complete; HITRUST i1 readiness complete | – | – | S | Process/audit evidence item — no in-repo software deliverable; track externally. |
| 🟡 | GA-14 EU AI Act technical documentation pack (risk mgmt, data governance, transparency, oversight, accuracy, robustness, cybersecurity) | – | – | M | Not assembled as a single EU AI Act technical-documentation pack covering all seven named pillars explicitly; scattered across docs. |
| 🔴 | GA-15 Conformance program publishes signed reports for ≥3 external authors/vendors (conformance registry) | – | – | L | Build the conformance program/registry and publish ≥3 signed external reports. |
| 🟡 | LTS-01 ≥24 months security backports + bug-fix releases committed in writing (public LTS policy) | – | – | S | Stated support is 12 months, not the ≥24-month LTS commitment; no dedicated public LTS policy. |
| 🔴 | LTS-02 Automated N→N+2 upgrade path via `radiopad migrate` (migration suite) | – | – | L | Implement a `radiopad migrate` command with an automated N→N+2 migration test suite. |
| 🔴 | LTS-03 Backward-compatible API guarantees enforced via contract tests on every PR (contract test gate) | – | – | M | Add contract tests / OpenAPI breaking-change detection as a required PR check. |
| 🔴 | LTS-04 Reproducible builds verified by ≥2 independent build farms (reproducible-build attestation) | – | – | L | Add reproducible-build flags + provenance attestation and a second independent build verification. |
